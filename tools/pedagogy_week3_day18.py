"""High-touch pedagogy overrides for Week 3 / Day 18.

Day 18 introduces dynamic programming by repeatedly asking the same question:
what is the smallest state that completely summarizes the solved prefix?  The
sequence grows from counting paths, to minimizing path cost, to binary
choose/skip decisions, and finally to breaking a cycle into two linear cases.
"""

PEDAGOGY_WEEK3_DAY18 = {
    70: {
        "pattern": "线性 DP 入门：从重复递归到只保存前两个已解子问题",
        "model": "先不要把 DP 当成一张表。站在第 i 阶，只看最后一步：它只能从 i-1 走 1 阶过来，或从 i-2 走 2 阶过来。于是“到 i 的全部方法”天然由两个更小、同类型问题组成。DP 的核心不是公式本身，而是发现递归会反复询问同一个 ways(i)，所以把每个子问题只算一次；进一步观察当前只依赖前两项，又可以把整张表压成两个滚动变量。",
        "derivation": """最自然的正确解法是直接递归：

ways(i) = ways(i-1) + ways(i-2)

它为什么正确？任何到达 i 的方案，最后一步不是 1 阶就是 2 阶，两类互斥且覆盖全部方案。

问题出在递归树会重复计算同一个子问题。例如 ways(5) 会展开 ways(4) 和 ways(3)，而 ways(4) 内部又会再次计算 ways(3)。随着 n 增大，同一 ways(k) 会从很多祖先路径反复出现，时间呈指数增长。

第一次优化是把已经算过的 ways(k) 保存下来：重复子问题 -> 记忆化 / DP。若自底向上计算，ways[1], ways[2] 已知后，每个 ways[i] 只算一次，时间降为 O(n)。

再看依赖关系：计算 ways[i] 时只读取 ways[i-1] 和 ways[i-2]，更早状态以后永远不会再被使用。因此整张 O(n) 数组也不是必须的，只保留 previous1/previous2 即可。

优化链：指数递归中的重复子问题 -> 每个状态只算一次 -> 只依赖前两项 -> O(1) 滚动状态。这最后才叫线性 DP + 状态压缩。""",
        "visual": """以 n=5 为例，只画“最后一步从哪里来”：

ways(3) ----\\
              +--> ways(5)
ways(4) ----/

更具体地：
到第 5 阶的方案最后一步只有两类：

... -> 4 -> 5   （最后走 1 阶）
... -> 3 -> 5   （最后走 2 阶）

所以：
ways(5) = ways(4) + ways(3)

滚动计算时只需要一条很短的传送带：

step=3 前： previous2=ways(1), previous1=ways(2)
算 current=ways(3)
然后整体前移：
previous2 <- old ways(2)
previous1 <- ways(3)

下一轮再用 ways(2), ways(3) 算 ways(4)。
更早的 ways(1) 已经失去未来用途，可以永久丢弃。""",
        "core": "把“到当前阶的全部走法”按最后一步拆成两个互斥来源；每个子问题只算一次，而未来只依赖最近两项，所以只滚动保留两个状态。",
        "formula": """状态定义：
ways[i] = 恰好到达第 i 阶的方法数。

边界：
ways[1] = 1
ways[2] = 2

对 i >= 3：
ways[i] = ways[i-1] + ways[i-2]

滚动不变量：进入 step=i 时：
previous2 = ways[i-2]
previous1 = ways[i-1]

计算：
current = previous1 + previous2
然后前移：
previous2 = previous1
previous1 = current。""",
        "steps": [
            "先处理 n<=2，答案就是 n；这同时固定递推的两个初始状态",
            "令 previous2=ways(1)=1、previous1=ways(2)=2；明确两个变量对应的状态位置",
            "从 step=3 到 n，先用旧 previous1/previous2 计算 current，不能在计算前覆盖任一旧值",
            "计算完成后再执行 previous2=previous1、previous1=current，让两个变量向前滚动一格",
            "循环结束时 previous1 恰好代表 ways(n)，直接返回",
        ],
        "proof": "任何到 i 的完整路径按最后一步长度唯一分成两类：最后走 1 阶的路径，与到 i-1 的路径一一对应；最后走 2 阶的路径，与到 i-2 的路径一一对应。两类不相交，因此数量可直接相加。自底向上按 i 递增时两个依赖状态已经正确；滚动变量只删除以后不会再访问的更早项，不改变递推结果，所以既不会漏方案也不会重复计数。",
        "pitfalls": "状态必须定义成“到达第 i 阶的方法数”，不要在中途又把变量解释成“还剩多少阶”。n=1 不能强行访问第二个基线。滚动更新必须先算 current，再移动 previous2/previous1；若先覆盖 previous2 或 previous1，就会丢失当前递推仍需要的旧值。n<=45 时 int 足够。",
        "transfer": "这是 Day 18 的 DP 母模型：先定义一个能完整描述前缀子问题的状态，再找它依赖哪些更小状态。LC-746 保留完全相同的“只依赖前两项”骨架，只把“方案数相加”改成“两个来源取最小成本”；LC-198 则把两个来源改成“选当前 / 不选当前”。",
        "memory": "先问最后一步从哪来；重复子问题只算一次；未来只看两项，就只留两项。",
    },

    746: {
        "pattern": "路径 DP：复用 LC-70 的前两状态，把计数改成最小累计代价",
        "model": "这题与 LC-70 的结构几乎一样：到位置 i 的最后一步仍只可能来自 i-1 或 i-2。变化在于我们不再统计两类路径的数量，而是比较两类路径的总代价。最容易混淆的是费用发生在哪里：cost[j] 是从第 j 阶继续向上走时支付，所以从 i-1 跳到 i 要在此前最低成本上再加 cost[i-1]。把楼顶想成虚拟位置 n，就能统一处理。",
        "derivation": """自然的正确做法是递归求“到达位置 i 的最低成本”：最后一步只可能从 i-1 或 i-2 来，因此分别递归求这两个位置的最低成本，再加上离开对应台阶的费用，最后取 min。

和 LC-70 一样，直接递归会重复计算同一个 minCost(i)。例如求顶部时会递归到 n-1、n-2，而 n-1 的递归内部又会再次求 n-2。重复子问题完全相同，只是聚合操作从加法变成了 min。

所以沿用 LC-70 的优化：每个位置最低成本只算一次。再观察 dp[i] 仍然只依赖 dp[i-1]、dp[i-2]，整张表可以继续压成 previous1/previous2。

真正需要先澄清的是状态边界：题目允许从 0 或 1 开始，这等价于“到达位置 0 和位置 1 都不需要支付任何费用”；费用是在从某个真实台阶离开时加入，而不是一到达就加入。

优化链仍是：重复递归 -> 线性 DP -> 两状态滚动；LC-70 的骨架可以直接复用。""",
        "visual": """cost = [10,15,20]，把楼顶画成虚拟位置 3：

位置:   0 ---- 1 ---- 2 ---- TOP(3)
费用:  10     15     20

到 TOP 的最后一跳只有：

从 2 来：到达 2 的最低成本 + cost[2]
从 1 来：到达 1 的最低成本 + cost[1]

而题目允许一开始站在 0 或 1：
dp[0]=0
dp[1]=0

于是：
dp[2] = min(dp[1]+15, dp[0]+10) = 10
dp[3] = min(dp[2]+20, dp[1]+15) = 15

这里 dp[3] 才是“到达楼顶”，不是 dp[2]。""",
        "core": "把楼顶当成位置 n；到 i 的最低成本只来自 i-1/i-2，两条来源分别加上离开那个台阶的费用，再取较小者。",
        "formula": """状态：
dp[i] = 到达位置 i 的最低累计花费，位置 n 是楼顶。

起点：
dp[0] = 0
dp[1] = 0

对 2 <= i <= n：
dp[i] = min(
    dp[i-1] + cost[i-1],
    dp[i-2] + cost[i-2]
)

滚动不变量：进入 i 时：
previous2 = dp[i-2]
previous1 = dp[i-1]。

计算 current 后再整体前移。""",
        "steps": [
            "把 cost.size() 记作 n，并把 n 当成楼顶这个虚拟位置；目标是 dp[n]",
            "初始化 previous2=dp[0]=0、previous1=dp[1]=0，因为可以免费选择从 0 或 1 开始",
            "对 i=2..n，比较从 i-1 离开与从 i-2 离开两种最后一步成本，得到 current",
            "先完成 current=min(...)，再把 previous2/previous1 向前滚动，保持 LC-70 相同的更新顺序",
            "循环处理到 i=n 后，previous1 就是到达楼顶的最低花费",
        ],
        "proof": "任意到达 i 的合法路径最后一步只能从 i-1 或 i-2 发出。若最后从 i-1 来，那么此前部分用 dp[i-1] 的最优路径不会比任何其它到 i-1 的路径更差，再支付 cost[i-1] 即得到这一类的最优值；i-2 同理。所有路径恰好落入这两类，因此取二者最小值不会漏掉更优路径。滚动压缩只丢弃未来递推再也不会读取的状态。",
        "pitfalls": "cost[i] 表示离开台阶 i 的费用，不要把它误加在“到达 i”时。楼顶是虚拟位置 n，因此循环必须允许 i==n；若只算到 n-1 会少最后一步。起始 dp[0]=dp[1]=0，不是 cost[0]/cost[1]。更新顺序仍要先算 current，再覆盖旧状态。",
        "transfer": "直接复用 LC-70：依赖图完全相同，都是当前位置只看前两位置。改变的只是聚合语义——LC-70 对互斥来源做计数相加；LC-746 对两条可行来源做最小化。以后遇到路径 DP，先分清状态表示“方案数、最小代价还是最大收益”。",
        "memory": "楼顶也当一个位置；从哪一阶离开，就加哪一阶 cost；两条最后来源取更便宜的。",
    },

    198: {
        "pattern": "选择 DP：把所有方案按“当前房偷不偷”分成两类，只保留前两个前缀最优",
        "model": "LC-70/746 都从“最后一步来自哪里”划分类别；LC-198 换成“当前房是否被选择”。处理第 i 间房时，任何合法方案只有两类：不偷 i，则答案继承前 i-1 间的最优；偷 i，则 i-1 必须放弃，只能接在前 i-2 间的最优后面。两个类别面对未来时只需保留各自能给出的最大金额，不需要记住具体偷了哪些房。",
        "derivation": """最自然的正确算法是 DFS 枚举每间房“偷 / 不偷”。若偷当前房，就跳过下一间；若不偷，就继续看下一间。所有合法选择集合都会被枚举，因此正确，但最坏会产生指数级搜索树。

重复在哪里？从不同选择路径走到同一个位置时，后面面对的是完全相同的一段房屋。例如前面如何到达“现在开始考虑第 i 间”并不重要；在只关心最大金额时，历史细节可以压缩成一个最优值。

换成前缀视角更直观：设 dp[i] 是处理完前 i 间房的最大金额。加入当前房 money 后，完整方案按是否选择它分成：
- 不偷当前：仍是 dp[i-1]；
- 偷当前：前一间不能偷，所以是 dp[i-2] + money。

同一前缀下，金额较小的方案面对完全相同的未来，再也不可能反超金额较大的方案，因此只需保留 max。这就是候选支配带来的状态压缩。

又因为下一步只读取 dp[i-1]/dp[i-2]，最终只保留 previous1/previous2，得到 O(n) 时间、O(1) 额外空间。""",
        "visual": """nums = [2,7,9,3,1]

走到金额 9 这间房时，面前只有两个互斥选择：

不偷 9：
[2,7] 的最优 = 7
                 \
                  max -> 当前前缀最优
                 /
偷 9：
必须跳过相邻的 7，
接 [2] 的最优 2 + 9 = 11

所以当前变成 11。

注意我们没有保存“11 是偷了哪些房得到的”。
对于后续房屋，真正有用的只有：
前一前缀最大值 previous1
前两前缀最大值 previous2

更差的同前缀方案面对相同未来永远不可能反超，可以永久丢弃。""",
        "core": "对当前房只比较两类完整方案：不偷它就继承前一前缀最优；偷它就接前两前缀最优加当前金额，取较大者。",
        "formula": """把处理到当前房之前的两个前缀最优记为：
previous1 = 前 i-1 间房可取得的最大金额
previous2 = 前 i-2 间房可取得的最大金额

当前房金额 money：
skip = previous1
take = previous2 + money
current = max(skip, take)

滚动后：
previous2 = old previous1
previous1 = current

初始空前缀：
previous2 = previous1 = 0。""",
        "steps": [
            "从空前缀开始令 previous2=0、previous1=0；0 表示当前没有任何房时最大收益为 0",
            "从左到右读取当前 money，先算 skip=previous1 与 take=previous2+money",
            "令 current=max(skip,take)；这一步已经把所有合法方案压缩成当前前缀唯一需要保留的最优值",
            "必须在 current 算完后再执行 previous2=previous1、previous1=current，避免覆盖 take 仍需的旧 dp[i-2]",
            "处理完所有房后 previous1 就是整个线性街道的最优收益",
        ],
        "proof": "任意针对当前前缀的合法方案，要么不含最后一间房，要么包含它，两类互斥且覆盖全部方案。不含最后房的最优值就是 previous1；包含最后房时相邻前一房必不能选，所以此前最多取 previous2，再加当前 money。两类各自都取到了最优，再取 max 就得到完整前缀最优。由于以后只关心这个前缀能提供的最大金额，更差方案面对相同未来不会重新变优，因此可以安全丢弃。",
        "pitfalls": "不要把 previous1 理解成“上一间房偷到的钱”，它表示整个前一前缀的最优总额。take 必须使用 previous2，而不是 previous1+money，否则会同时偷相邻房。滚动更新顺序不能提前覆盖 previous1。题目金额非负，但初始化为 0 也自然允许空前缀。",
        "transfer": "LC-198 把 LC-70 的“最后来源分类”升级成一般的“决策分类”：先枚举当前决策的互斥类别，再只保留每类未来真正需要的摘要状态。LC-213 完全复用这个线性 DP，不重新发明 recurrence；它只负责先消除首尾相邻造成的环。",
        "memory": "当前房：不偷就继承前一最优；偷就接前两最优。面对同一未来，只留金额最大的前缀。",
    },

    213: {
        "pattern": "环形约束拆分：先用首尾互斥把环断开，再原样复用 LC-198",
        "model": "LC-213 的局部规则与 LC-198 完全相同，唯一新增约束是第 0 间和第 n-1 间也相邻。不要为这个环重新设计一套 DP：任何合法答案都不可能同时选择首尾，因此至少会排除其中一个。只要分别求“不允许最后一间”和“不允许第一间”两个线性问题，环就被断开；两个子问题内部直接调用 LC-198 的滚动 DP。",
        "derivation": """最自然的正确算法仍然可以 DFS 枚举每间房偷/不偷，并额外记住第一间是否被偷；走到最后一间时若第一间已偷，就禁止再偷最后一间。它能覆盖所有合法方案，但仍是指数级枚举，而且把一个只有首尾额外冲突的问题处理得过于复杂。

如果尝试直接把 LC-198 的线性 recurrence 套到整个数组，又会出错：线性 DP 只知道相邻 i-1/i 的冲突，它不会记住第 0 间是否已选，因此可能同时选首尾。

关键结构只有一个：首尾不能同时被选。于是所有合法方案一定属于下面至少一类：
A. 不选择最后一间 -> 只考虑 [0, n-2]；
B. 不选择第一间 -> 只考虑 [1, n-1]。

这两个区间内部已经没有环，都是完整的 LC-198。即使某个方案首尾都不选，它会同时出现在 A/B 中也没关系，因为我们只取最大值，不是在计数方案。

优化/建模链：指数枚举环上选择 -> 抓住唯一跨边界冲突“首尾互斥” -> 用两个覆盖全部合法方案的线性区间断环 -> 复用 LC-198 O(n)/O(1)。""",
        "visual": """环形房屋：

       [0] -- [1] -- [2] -- ... -- [n-2] -- [n-1]
        |                                      |
        +--------------------------------------+
                    首尾也相邻

只需要剪断这条额外的首尾边。

情况 A：保证不偷最后一间
[0] -- [1] -- ... -- [n-2]      [n-1] X
=> 线性区间 [0,n-2]

情况 B：保证不偷第一间
[0] X      [1] -- ... -- [n-1]
=> 线性区间 [1,n-1]

任何合法方案都至少落在一个情况里：
- 若偷首，则必不偷尾 -> A；
- 若不偷首 -> B。

所以答案只需 max(robRange(A), robRange(B))。""",
        "core": "环上唯一新增冲突是首尾不能同时选；分别禁止尾、禁止首，把问题拆成两个线性 LC-198，再取两者最大值。",
        "formula": """n == 1：answer = nums[0]

n >= 2：
A = robLinear(nums, 0, n-2)
B = robLinear(nums, 1, n-1)
answer = max(A, B)

其中 robLinear(left,right) 完全复用 LC-198：
current = max(previous1, previous2 + nums[i])。

覆盖不变量：每个合法环形方案至少属于 A 或 B；A/B 内部都不存在首尾额外相邻关系。""",
        "steps": [
            "先处理 n==1；此时不存在可供拆分的两个有效区间，唯一房屋直接返回",
            "定义 robRange(left,right)，内部不要改变 LC-198：previous2/previous1 从 0 开始滚动",
            "计算区间 [0,n-2]，它代表明确禁止最后一间房后的全部合法方案",
            "计算区间 [1,n-1]，它代表明确禁止第一间房后的全部合法方案",
            "返回两条线性最优值的 max；无需判断哪个区间实际偷了首/尾，因为覆盖关系已经保证答案存在其中",
        ],
        "proof": "任何环形合法方案不能同时包含房 0 和房 n-1。若它包含房 0，则必不包含房 n-1，因此完整落在区间 [0,n-2] 的 LC-198 搜索空间中；若它不包含房 0，则完整落在 [1,n-1] 中。故两个线性子问题的并集覆盖全部合法方案。反过来，每个子问题都删除了首尾冲突中的一个端点，内部只剩普通相邻约束，所以 LC-198 给出的方案在原环中也合法。取两者最大值恰为全局最优。",
        "pitfalls": "n==1 必须特判，否则 [0,n-2] / [1,n-1] 会形成无效区间。robRange 使用闭区间，调用端点必须保持一致。两个 case 不是互斥集合：首尾都不偷的方案会被计算两次，但我们取最大值而不是计数，所以不会影响正确性。不要为了环形问题复制一套新的复杂状态机；这里复用 LC-198 更清楚。",
        "transfer": "这是非常通用的“少量边界状态枚举 -> 断环 -> 复用线性模型”。环形染色、环形相邻选择、循环序列 DP 都可以先固定第一个位置的少量状态，使首尾依赖消失，再调用已经会做的线性 DP。下一步学习更复杂 DP 时，也应优先寻找能否先把新约束拆掉，再复用旧状态定义。",
        "memory": "环只多了首尾这一条冲突：禁尾做一次 LC-198，禁首再做一次，取最大。",
    },
}
