"""High-touch pedagogy overrides for Week 2 / Day 13.

Day 13 introduces backtracking as "DFS over a decision tree + recoverable state".
The explanations deliberately distinguish search-space construction, duplicate
avoidance, and pruning instead of presenting backtracking as a memorized template.
"""

PEDAGOGY_WEEK2_DAY13 = {
    46: {
        "pattern": "回溯母模型：DFS 决策树 + 做选择 / 撤销选择",
        "model": "把排列看成一棵按位置逐层展开的决策树：第 d 层决定排列的第 d 个位置，path 是从根走到当前节点的选择历史，used 是仍可选择元素的状态。回溯并不是另一种神秘算法，而是 DFS 在离开子树时把共享状态恢复到进入该子树之前。",
        "visual": """先画出 nums=[1,2,3] 的搜索树，而不是先背 `used + path`：

                         []
                 /        |        \\
               [1]       [2]       [3]
              /   \\      / \\       / \\
          [1,2] [1,3] ...         ...
            |      |
        [1,2,3] [1,3,2]

树的第 d 层只回答一个问题：
“排列第 d 个位置放哪个还没用过的元素？”

走到 [1,2] 时：
path = [1,2]
used = {1:true, 2:true, 3:false}

选择 3 后得到一个叶子 [1,2,3]。
但从这个叶子返回到 [1,2]、再返回到 [1] 时，刚才做的选择不能残留：

选择 2：push 2, used[2]=true
    ↓
递归探索所有以 [1,2] 开头的排列
    ↓
返回：pop 2, used[2]=false

恢复后才能继续尝试 [1,3]。

所以 `push -> recurse -> pop` 不是口诀；它是在 DFS 离开一条边时恢复父节点对应的搜索状态。""",
        "core": "每层给下一个位置选择一个未使用元素；递归回来必须撤销这次选择，让兄弟分支看到完全相同的父状态。",
        "formula": """递归状态：
path.size() = 已经确定的位置数
used[i]     = nums[i] 是否已经出现在 path 中

进入 search 时的不变量：
1. path 中元素两两不同；
2. used 与 path 精确对应；
3. 未使用元素恰好是当前层允许的候选。

若 path.size() == n：
当前路径已经选择 n 个不同元素
=> 它就是一个完整排列。

对候选 i 的状态转移：
used[i] = true
path.push_back(nums[i])
search(...)
path.pop_back()
used[i] = false

后两步把状态恢复到选择 i 之前。""",
        "steps": [
            "answer/path 清空，used 初始化为全 false；根节点代表还没有确定任何位置",
            "进入一层后遍历所有下标 i，已经 used 的元素不能再次用于当前排列，直接跳过",
            "选择 nums[i]：先标记 used[i]=true，再 push 到 path，使状态变成子节点对应的排列前缀",
            "递归搜索这个前缀之下的全部排列；当 path 长度达到 n 时把完整路径复制进 answer",
            "递归返回后 pop path 并恢复 used[i]=false，严格恢复父节点状态，再尝试下一个兄弟候选",
        ],
        "proof": "不会漏：任意一个目标排列 p[0..n-1] 都对应唯一一条选择序列——第 0 层选 p[0]，第 1 层选 p[1]，依此类推；这些元素此前都未使用，所以这条路径一定存在。不会重：题目保证 nums 元素互异，而一条根到叶路径由每一层选中的下标唯一确定，两条不同搜索路径至少在某一层选择不同元素，因此不可能得到同一个排列。撤销操作保证探索一个子树不会污染兄弟子树。",
        "pitfalls": "`used` 表示的是当前路径状态，不是全局访问标记，所以递归返回后必须恢复。`path.pop_back()` 与 `used[i]=false` 都属于撤销动作；漏任意一个都会丢分支或产生非法重复使用。题目保证 nums 中元素互异，因此无需同层去重；若元素可重复，则仅靠 used 不能消除值相同导致的重复排列，需要排序后增加同层去重条件。结果本身有 n! 个排列，每个复制长度 n，因此 O(n·n!) 的输出成本无法被算法消除。",
        "transfer": "把 Day 10 的树 DFS 迁移过来：以前树节点和边由输入给定，现在“搜索树”由选择动态生成；递归调用栈仍然表示当前路径。Day 13 后三题只是在改变搜索树：LC-78 不要求填满所有位置，所以每个节点都可成为答案；LC-39 用 start 把“顺序不同但组合相同”的分支折叠掉；LC-22 则用前缀合法性直接不生成不可能的孩子。",
        "memory": "先问当前层决定什么；做选择进入子树，回来必须恢复父状态。",
    },

    78: {
        "pattern": "子集搜索：用 start 固定唯一选择顺序，每个搜索节点都是答案",
        "model": "子集本质上是每个元素“选或不选”的 2^n 个决策。当前实现把二叉选/不选树改写成更紧凑的 start-index 搜索：path 表示已经选中的下标递增序列，start 表示下一次只能从哪里往后选。因为子集不关心排列顺序，这个单调下标约束为每个子集规定了唯一构造顺序。",
        "visual": """最自然的想法是：每个元素只有“选 / 不选”两种决定。
nums=[1,2,3]：

                         []
                   /            \\
                不选1           选1
                 ...             [1]
                               /     \\
                            不选2    选2
                              ...    [1,2]

这会得到 2^3 个叶子，完全正确。

当前代码换一种等价画法：一个 path 形成后，直接枚举“下一个还想加入谁”：

                 []          <- 空集也是答案
              /   |   \\
            [1]  [2]  [3]    <- 都是答案
           /  \\    |
       [1,2] [1,3] [2,3]     <- 都是答案
          |
      [1,2,3]                 <- 也是答案

关键区别来自“子集不关心顺序”。
如果已经选择下标 1 的元素 2，后面只允许选下标 >1 的元素：

start = i + 1

于是 {1,2} 只会沿 [1] -> [1,2] 生成，
不会再从 [2] 回头生成 [2,1]。

这里每个搜索节点本身就已经是一个合法子集，因此进入 search 就先记录 path，而不是像 LC-46 那样只在叶子记录。""",
        "core": "给每个子集规定“按原下标递增选择”的唯一写法；当前 path 一形成就是一个合法子集，然后只从 start 之后继续扩展。",
        "formula": """状态：search(nums, start)

path 中对应的原下标严格递增。
下一元素只能选择：
i ∈ [start, n)
选择后递归：
search(nums, i + 1)

因此同一个下标不会重复使用，也不会回头形成不同顺序的同一子集。

答案数量：
每个元素独立有“选 / 不选”两种状态
=> 子集总数 = 2^n。

当前递归树把这些 2^n 个子集分布在不同深度的节点上，所以：
进入每个 search 时都执行 answer.push_back(path)。""",
        "steps": [
            "从 path=[]、start=0 开始；进入 search 后立即记录当前 path，所以空集首先被收集",
            "从 i=start 到 n-1 枚举下一个加入子集的元素；push nums[i] 后，path 仍按原下标递增",
            "递归传 i+1，明确当前元素以后不能再选，且更小下标永远不会回头进入这个分支",
            "子树探索完成后 pop 当前元素，恢复父 path，再枚举下一个可能的子集分支",
        ],
        "proof": "任意子集都能把自己的元素按原数组下标升序写成唯一序列 i1<i2<...<ik；搜索从 start=0 可以依次选择 i1、i2...，所以不会漏。反过来，搜索路径的下标严格递增，因此一个元素不会重复出现；同一个下标集合也不可能用另一种顺序再次生成，所以不会重。空集对应根节点，非空子集对应唯一的非根搜索节点，因此在每层入口记录正好覆盖全部 2^n 个答案。",
        "pitfalls": "最容易把 LC-46 的“只在叶子记录”照搬过来，这会漏掉长度小于 n 的子集；本题每个 path 都合法，所以必须在进入每层时记录。递归必须传 i+1 而不是 start+1，也不能传 i（会重复使用同一个元素）。题目元素互异；若允许重复值，则还需要排序并在同一层跳过相同值。O(n·2^n) 中的 n 来自把每个 path 复制进答案，输出规模决定这部分成本无法省掉。",
        "transfer": "和 LC-46 对照最重要：排列关心顺序，所以每层都能从所有“未使用元素”中选；子集不关心顺序，所以用 start 强制下标递增，直接消除 [1,2]/[2,1] 这类重复表示。LC-39 会继续复用 start，但把 i+1 改成 i，从而表达“当前候选可以重复使用”。",
        "memory": "子集不看顺序，就给它规定唯一顺序：只往右选；每走到一个节点就先收答案。",
    },

    39: {
        "pattern": "组合回溯：先规定唯一构造顺序，再用正数与排序剪掉无效后缀",
        "model": "最直接的搜索可以在每一步任选任意 candidate，但同一个组合会以不同排列反复出现，例如 2+2+3、2+3+2、3+2+2。先排序并用 start 规定只能按非降顺序选择，就把这些等价路径压成一条；再利用候选全为正数且已排序，当 candidates[i]>remaining 时，当前以及右侧所有候选都不可能继续，整段搜索可以停止。",
        "derivation": """先写一个真正能工作的直接搜索：
remaining=target；每一层都可以从所有 candidates 中任选一个正数继续减，remaining==0 就得到一个答案。

它能找到答案，但有两个明显浪费：

1. 同一个组合被不同选择顺序重复生成：
   2 -> 2 -> 3
   2 -> 3 -> 2
   3 -> 2 -> 2
   三条路径其实都是组合 {2,2,3}。

2. 当 remaining 已经很小时，还会继续尝试明显过大的候选。

先解决第 1 类浪费：组合不关心顺序，所以人为规定唯一规范——选择下标只能不减。
选择 candidates[i] 后，下层仍从 i 开始：
- 可以再次选 i，保留“无限次使用”；
- 不能再选 i 左边的候选，消除顺序排列造成的重复。

再解决第 2 类浪费：先把 candidates 排序。
若 candidates[i] > remaining，因为右侧只会更大，所以整个 [i,n) 都不可能成功，可以直接 break。

最后才给这种“选择 -> 递归 -> 撤销，并在途中剪掉不可能分支”的机制命名为回溯 / backtracking。""",
        "visual": """例：candidates=[2,3,6,7], target=7。

搜索状态看成：path + remaining + start。

[] rem=7 start=0
 |
 +-- 选2 -> [2] rem=5 start=0
 |             |
 |             +-- 选2 -> [2,2] rem=3
 |             |             |
 |             |             +-- 选2 -> rem=1
 |             |             |          下一候选 2>1，整段停止
 |             |             +-- 选3 -> [2,2,3] rem=0 ✓
 |             |
 |             +-- 选3 -> [2,3] rem=2
 |                         下一候选 3>2，停止
 |
 +-- 选3 -> [3] rem=4 start=1
 |             后面只能继续 3/6/7，不能回头选 2
 |
 +-- 选7 -> [7] rem=0 ✓

为什么 [2,3,2] 不会再出现？
因为选到 3 后 start 已经是 1，后续不能回到下标 0 的 2。
于是同一个多重集合只有非降序这一条表示。""",
        "core": "组合不关心顺序，就用 start 强制候选下标不减；当前候选可复用所以递归仍传 i，排序后候选一旦超过 remaining 就停止整段。",
        "formula": """状态：search(candidates, start, remaining)

path 的候选下标单调不减。

成功条件：
remaining == 0  => 记录 path

本层候选：
i ∈ [start, n)

选择 candidates[i] 后：
remaining' = remaining - candidates[i]
start'     = i        // 不是 i+1：允许再次使用当前候选

因为 candidates 已升序且全部为正：
candidates[i] > remaining
=> 对任意 j>=i，candidates[j] >= candidates[i] > remaining
=> 当前及右侧候选全部不可能成功，直接 break。""",
        "steps": [
            "先排序 candidates；排序既建立组合的统一非降序表示，也让“过大后 break”成为合法剪枝",
            "从 start 开始枚举候选 i；若 candidates[i]>remaining，利用排序证明后续更大候选也都失败，停止本层",
            "把 candidates[i] push 进 path，并递归 search(candidates, i, remaining-candidates[i])；仍传 i 表示该值可以重复使用",
            "remaining==0 时复制当前 path 作为一个完整组合并返回；递归回来后 pop，恢复父状态",
        ],
        "proof": "不会漏：任意合法组合都可以按 candidates 排序后的下标非降序排列；搜索允许从 start 选择当前或更右候选，并且选择 i 后仍可继续选择 i，因此这条规范路径一定存在。不会重：每条生成路径的候选下标都不减，同一个多重集合只有这一种非降序排列，所以不会产生不同顺序的重复答案。剪枝也安全：候选均为正且排序后，当前值已经大于 remaining 时，右边更大的值只会让剩余量更小，不可能恰好归零。",
        "pitfalls": "允许重复使用当前候选，所以递归必须传 i；若误传 i+1，就变成“每个元素最多一次”的另一类组合题。`break` 依赖排序，若不排序只能 continue/继续检查其他候选。当前实现会原地 sort(candidates)，这是可观察副作用；LeetCode 接口允许，但工程调用方若要求保留输入顺序应排序副本。题目保证候选互异且为正；若有重复候选值需要额外同层去重，若允许非正数则 remaining 严格下降和当前剪枝都不再成立。",
        "transfer": "直接从 LC-78 迁移：两题都用 start 规定组合的唯一顺序。LC-78 选过 i 后传 i+1，因为元素只能用一次；LC-39 传 i，因为当前值可以无限复用。以后看到组合题，先问三个问题：顺序算不算不同答案？一个候选能用几次？输入是否有重复值？这三个答案基本决定 start 的推进与去重规则。",
        "memory": "组合先规定唯一顺序；可复用传 i，只用一次传 i+1；排序后大于 remaining 就整段停。",
    },

    22: {
        "pattern": "约束生成：把合法前缀写进搜索树，只生成仍可能成功的节点",
        "model": "最直接的方法是枚举长度 2n 的所有左右括号字符串，再逐个用 LC-20 的栈规则验证；浪费在大量字符串很早就已经不可能合法，却仍被生成到结尾。把 LC-20 的合法性前移成搜索前缀不变量：close<=open<=n，就能在非法前缀出现的第一刻停止扩展，只遍历可能通向合法答案的状态。",
        "derivation": """先从能写出来的直接算法开始：长度固定为 2n，每一位都枚举 '(' 或 ')'。

总候选数 = 2^(2n) = 4^n。
生成完整字符串后，再用 LC-20 的括号匹配规则判断是否合法。

问题在于很多候选在很短的前缀阶段就已经“判死刑”。例如：

())...
  ^
扫描到这里 close=2、open=1。
右括号已经比左括号多；未来再追加任何字符，都不可能修复“某个右括号出现时没有可匹配左括号”这个历史事实。

还有另一类必死前缀：已经放了 n 个左括号后还继续放 '('，最终根本没有足够位置放 n 个右括号。

所以不要“先生成坏字符串，再过滤”；把验证条件搬到生成过程中：
- open < n 才允许继续放 '('；
- close < open 才允许继续放 ')'。

这类优化删掉的是“已经违反前缀约束、未来永远无法恢复”的搜索分支。最后再把这种边构造边剪枝的 DFS 叫做回溯。""",
        "visual": """n=3 时，不要把搜索树画成完整的二叉树；非法孩子根本不生成：

                     ""  (open=0, close=0)
                      |
                     "(" (1,0)
                  /          \\
              "(("(2,0)    "()"(1,1)
              /   \\             |
          "((("  "(()"          "()("
           |        ...            ...

根节点为什么没有 ')' 分支？
因为 close<open 不成立：一开始没有任何左括号可供它闭合。

对任意前缀都维护：
0 <= close <= open <= n

可以把 open-close 看成“当前还有多少个已打开但未闭合的括号”。
它绝不能为负；open 也绝不能超过总额度 n。

当长度达到 2n 时，既然 open<=n、close<=open，又已经放满 2n 个字符，就只能是 open=close=n，于是当前 path 自动是合法答案，不需要再调用 LC-20 验证。""",
        "core": "把 LC-20 的括号合法性从“完整字符串验收”前移成搜索前缀约束；只给仍可能完成的前缀生成孩子。",
        "formula": """状态：
open  = path 中已使用 '(' 数
close = path 中已使用 ')' 数

所有被搜索的前缀始终满足：
0 <= close <= open <= n

添加 '(' 的条件：
open < n

添加 ')' 的条件：
close < open

终点：
path.size() == 2n

此时长度给出：
open + close = 2n
又有 close <= open <= n
=> open = close = n
=> path 必为完整合法括号串。""",
        "steps": [
            "从空 path、open=0、close=0 开始；当前状态天然满足合法前缀不变量",
            "若 open<n，push '(' 并递归 open+1；回来后 pop，恢复当前前缀",
            "若 close<open，push ')' 并递归 close+1；这个条件保证任何时刻右括号都不会超过已出现的左括号",
            "当 path 长度达到 2n 时直接记录答案；此时不变量已经保证左右括号各 n 个且嵌套合法",
        ],
        "proof": "不会生成非法答案：每个前缀都满足 close<=open，所以从未出现无法匹配的右括号；open<=n 且最终长度为 2n，又迫使终点 open=close=n。不会漏：任意合法括号串的每个前缀都必满足 close<=open，并且总左括号数不超过 n，所以按照它真实的下一个字符走时，对应分支条件始终允许，最终一定能到达该答案。不会重：一个具体字符串的字符序列唯一，对应搜索树中唯一一条根到叶路径。",
        "pitfalls": "右括号条件必须是 `close < open`，不是 `close < n`；后者会生成 `)(` 一类无法修复的非法前缀。push 后递归、递归后 pop 必须成对，否则兄弟分支会继承错误字符串。这里无需在终点再调用 LC-20 校验，因为合法性已经作为不变量贯穿整个生成过程。题目 n>=1；若扩展到 n=0，这套逻辑也会自然返回空字符串这一种组合。",
        "transfer": "这是 LC-20 的反向使用：LC-20 给定完整字符串后判断“这个前缀是否已经非法”；LC-22 在生成阶段只允许不会让前缀非法的动作。以后遇到构造 IP、表达式、棋盘或受限序列时，优先问：哪些局部前缀一旦违规，未来无论怎么补都救不回来？把这些条件放进递归入口就是剪枝。",
        "memory": "不要先造坏答案再过滤；能开才开，手里有未闭合左括号才关。",
    },
}
