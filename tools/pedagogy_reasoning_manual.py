"""Manual reasoning-first bridges for problems where a generic fallback is too shallow.

These entries deliberately describe the problem in ordinary language before
naming a reusable algorithm. They override the mechanically generated bridge
without replacing reviewed formula/proof/code layers.
"""


def guide(pattern: str, path: str) -> str:
    return (
        f"前面的推理已经得到可复用机制；现在才给它一个检索名称：{pattern}。"
        f"通用机制见 docs/algorithms/{path}。回到本题时，只需要把其中的状态、边界和更新事件"
        "映射到当前题意，不要反过来用模板猜题。"
    )


MANUAL_REASONING_OVERRIDES = {
    1: {
        "visual": """先把题目当成最普通的配对问题：

nums = [2, 7, 11, 15], target = 9

固定 2：
2 + 7  = 9   <- 找到
2 + 11 = 13
2 + 15 = 17

如果 2 没找到，再固定 7，继续和后面的数配对。

这里最重要的观察还不是任何数据结构，而是：当当前数 x 已经确定时，另一个数也随之被唯一确定为 target-x。""",
        "general_solution": """最自然的正确实现是两层循环：枚举第一个下标 i，再枚举所有 j>i，检查 nums[i]+nums[j] 是否等于 target。它直接覆盖所有不同下标组成的二元组，因此不会漏答案，时间 O(n^2)，额外空间 O(1)。""",
        "limitations": """两层循环的浪费发生在“找第二个数”：固定 x 后，我们明明已经知道只需要 target-x，却仍然把剩余候选一个个比较。不同 i 会一遍遍做这种线性查找。""",
        "generalization": """当当前状态能直接算出一个唯一需要的历史 key 时，问题可以从“遍历历史寻找 key”改写成“提前给历史建立 key -> 信息 的索引”。这样优化掉的是重复查找，而不是配对逻辑本身。""",
        "algorithm_background": guide("哈希索引：边扫描边查询补数", "hash-indexing.md"),
    },
    14: {
        "visual": """把所有字符串左对齐写成多行：

flower
flow
flight
^^^^

公共前缀只能从第 0 列开始连续向右。某一列只要有一个字符串结束，或出现不同字符，公共前缀就在这一列之前结束。""",
        "general_solution": """最自然的做法就是逐列检查：先看第 0 个字符是否所有字符串相同，再看第 1 个……第一次出现越界或不同就停止。最多检查到最短字符串长度，每个被检查位置只比较一次。""",
        "limitations": """这个一般解法已经直接利用了“前缀必须从开头连续一致”的题意，渐进复杂度上没有需要消除的重复工作。真正的限制只是边界：任意字符串为空时答案立即为空；某列失败后后面的列完全没有继续检查的意义。""",
        "generalization": """这是一类“共同起点 + 首次失败决定边界”的扫描问题。只要目标对象必须从同一起点连续满足条件，就可以同步推进，第一次失败即封口。""",
        "algorithm_background": "这题不需要额外的高级数据结构；前面的逐列同步扫描已经是最直接的核心算法。专项文档体系在这里的意义是识别‘无需升级’，而不是强行套一个模板。",
    },
    19: {
        "visual": """链表：

dummy -> 1 -> 2 -> 3 -> 4 -> 5 -> null
                         ^
                    若 n=2，要删除 4

最朴素的想法是先知道链表总长度，再从头走到“待删节点的前一个节点”。""",
        "general_solution": """先完整遍历一次得到长度 len；目标是正数第 len-n+1 个节点，因此第二次从 dummy 出发走 len-n 步，停在目标前驱，再改一次 next 完成删除。这是清晰、正确的两趟 O(n) 解法。""",
        "limitations": """两趟做法的问题不是复杂度阶数，而是第一次遍历只为了得到‘尾部还有多远’。如果希望一趟完成，就需要在扫描过程中直接维持这个相对距离；另外删除头节点时没有真实前驱，需要统一边界。""",
        "generalization": """若两个位置之间保持固定间距 n+1，当较快位置刚越过链表尾部时，较慢位置就自然落在倒数第 n 个节点的前驱。虚拟前驱 dummy 再把‘删除头’统一成普通删除。""",
        "algorithm_background": guide("链表固定间距双指针 + dummy", "linked-list-pointer.md"),
    },
    20: {
        "visual": """把括号理解成一层层尚未关闭的盒子：

读 '(' ：打开圆括号盒子
读 '[' ：在里面又打开方括号盒子
读 ']' ：必须先关掉最近打开的 '['
读 ')' ：随后才能关掉 '('

([)] 为什么错？因为读到 ')' 时，最近尚未关闭的是 '['，不能绕过它去关更外层。""",
        "general_solution": """从左到右扫描。遇到左括号就把它记到“尚未关闭的括号序列”末尾；遇到右括号时，只检查最近那个尚未关闭的左括号，类型一致就删除它，否则立即失败。扫描结束还要求没有残留未关闭括号。""",
        "limitations": """如果每遇到右括号都去所有历史左括号里搜索同类型，会破坏嵌套顺序；题目要求的不是‘数量能配上’，而是‘最近开启的结构必须最先关闭’。因此历史中真正需要访问的永远只有最后一个未完成状态。""",
        "generalization": """当结构具有‘后开启先结束’的嵌套关系时，只需要保存尚未完成的上下文，并始终从最近一个开始结算。这是 LIFO，而不是普通的集合匹配。""",
        "algorithm_background": guide("栈保存未完成的嵌套上下文", "stack-queue.md"),
    },
    21: {
        "visual": """两条链都已升序：

A: 1 -> 3 -> 5
B: 2 -> 4 -> 6

当前未合并部分的最小值只可能是两个链头之一：
1 vs 2 -> 先接 1
3 vs 2 -> 再接 2

不需要去两条链的内部寻找更小值。""",
        "general_solution": """维护结果链表尾部。只要两条链都非空，就比较当前头节点，把较小者接到结果末尾并推进它所属链表；一条耗尽后，另一条剩余后缀已经有序，可以整体接上。""",
        "limitations": """这个逻辑本身已经是线性最优的；主要工程问题是第一次接节点时结果头尚不存在，以及相等值如何处理。若为头节点单独写分支，会让同一连接逻辑出现两套。""",
        "generalization": """两个输入各自有序意味着：每一步全局最小候选只可能出现在各自当前头部。虚拟头节点可以把‘第一次连接’和后续连接统一成同一种 tail->next 操作。""",
        "algorithm_background": guide("有序链表归并 + dummy", "linked-list-pointer.md"),
    },
    46: {
        "visual": """对 nums=[1,2,3]，把答案看成一棵“逐个填位置”的选择树：

第 1 位：选 1 / 2 / 3
          |
第 2 位：从剩余两个里选
          |
第 3 位：只能选最后一个

一条从根走到叶子的路径，就是一个完整排列。""",
        "general_solution": """从空序列开始，每一层枚举一个还没使用的元素放到当前位置，然后继续填写下一位；长度达到 n 时记录答案。返回上一层后，要把刚才的选择撤销，才能尝试同层的另一个候选。""",
        "limitations": """排列本身有 n! 个，指数/阶乘级搜索无法被算法消掉；能优化的是状态管理和重复分支。若每层复制完整剩余集合会产生额外开销；若撤销状态不完整，会污染兄弟分支。""",
        "generalization": """枚举所有方案时，可以把过程统一成‘当前路径 + 当前可选集合’。做选择进入子问题，返回后恢复原状态；这套选择树模型比背某一题的递归函数更通用。""",
        "algorithm_background": guide("回溯：选择、递归、撤销", "backtracking.md"),
    },
    53: {
        "visual": """对每个右端点 r，所有以 r 结尾的子数组共享同一个新元素 nums[r]：

... [A 起点 -------- r-1] + nums[r]
...     [B 起点 ---- r-1] + nums[r]
...         [C 起点 r-1] + nums[r]

如果在 r-1 时，A 的和已经比 B 大，那么两者以后都追加完全相同的 nums[r]、nums[r+1]……，B 永远不可能反超 A。""",
        "general_solution": """最自然的做法是枚举每个起点，再向右扩展并维护当前区间和，用它更新全局最大值。这样每个连续子数组都会被检查一次，时间 O(n^2)。""",
        "limitations": """固定同一个终点时，我们保留了许多彼此被支配的起点。两个候选面对未来会追加完全相同的后缀；当前和更小的那个以后也始终更小，因此没有继续保存的价值。""",
        "generalization": """对每个位置，只需保留‘必须以这里结尾的最佳区间’这一份状态。到下一位置时只有两个合理选择：把新数接到此前最佳后面，或从新数自己重新开始。""",
        "algorithm_background": guide("一维动态规划 / Kadane 的候选压缩", "dynamic-programming.md"),
    },
    78: {
        "visual": """对 [1,2,3]，每个元素只有两个语义：选或不选。

                 []
             /        \
          不选1       选1
           / \         / \
        ...  ...     ...  ...

也可以规定只按下标递增地‘再加入一个元素’，这样每个节点当前 path 本身就是一个子集。""",
        "general_solution": """从空集合开始，记录当前选择；然后只从当前下标之后选择下一个元素，加入后继续扩展。每个中间 path 都是合法答案，因此进入一层时就应记录，而不必等到叶子。""",
        "limitations": """答案本身就有 2^n 个，不能把搜索规模降成多项式。真正要避免的是同一集合被不同顺序重复构造，例如 [1,2] 与 [2,1]；另外只在叶子记录会漏掉较短子集。""",
        "generalization": """对‘顺序不重要’的集合型枚举，给元素规定唯一的递增选择顺序，就能让每个集合只对应一条构造路径。""",
        "algorithm_background": guide("回溯：递增下标消除集合的顺序重复", "backtracking.md"),
    },
    79: {
        "visual": """单词必须沿网格相邻格连续走：

A B C E
S F C S
A D E E

寻找 ABCCED 时，一条候选路径像一支正在移动的笔：进入格子后，这个格子在当前路径里暂时不能再用；如果后面走不通，要退回来尝试另一条路。""",
        "general_solution": """把每个可能匹配 word[0] 的格子当作起点，沿上下左右尝试下一个字符。进入一个格子时记录‘当前路径已使用’，匹配下一个字符；若失败，返回上一格并恢复该标记，再尝试其它方向。""",
        "limitations": """不同路径会共享大量格子，搜索规模最坏仍是指数级；但 visited 不能做成全局永久标记，因为同一格可以属于另一条独立候选路径，只是不能在同一条路径中重复使用。""",
        "generalization": """当约束只对‘当前构造路径’生效时，状态必须随递归进入和退出而变化：进入时做选择/标记，退出时撤销。这与连通分量搜索的一次性 visited 不同。""",
        "algorithm_background": guide("网格回溯：路径局部 visited", "backtracking.md"),
    },
    94: {
        "visual": """中序遍历的定义先于实现：

      2
     / \
    1   3

先完整访问左子树 -> 再访问根 2 -> 再访问右子树，结果 [1,2,3]。

站在节点 2 时，访问根之前必须先把‘以后还要回来处理 2’这件事记住。""",
        "general_solution": """最自然的写法是递归：对当前节点先处理左子树，左边完成后记录当前节点，再处理右子树。这个顺序直接来自中序遍历定义。""",
        "limitations": """递归会把‘处理完左边以后回到哪个祖先’保存在语言调用栈中。若题目要求手写迭代，就必须显式保存这些暂停现场；这不是新的遍历逻辑，只是把隐式状态拿出来。""",
        "generalization": """任何递归树遍历都可以从‘函数暂停时必须记住什么’反推出显式状态结构。中序需要保存沿左链尚未访问根的祖先。""",
        "algorithm_background": guide("树遍历：递归现场与显式栈", "tree-traversal.md"),
    },
    102: {
        "visual": """把树按距离根的层次画开：

        3          <- 第 0 层
       / \
      9  20        <- 第 1 层
         / \
        15  7      <- 第 2 层

目标不是普通访问所有节点，而是把同一层节点放进同一个结果数组。""",
        "general_solution": """从根开始维护‘当前还没处理的一层节点’。先记下这一层有多少个节点，把它们全部取出、记录值，并把各自孩子加入下一批；当前批处理完后再进入下一层。""",
        "limitations": """如果只是普通深度优先地访问节点，却不额外携带 depth，就无法知道何时一层结束。这里必须让待处理状态天然按距离分层，或显式记录每个节点深度。""",
        "generalization": """当问题关心‘离起点多少步/多少层’时，应该维护 frontier：当前层全部处理完，下一批新发现状态才构成下一层。""",
        "algorithm_background": guide("按层 frontier 的队列遍历", "tree-traversal.md"),
    },
    104: {
        "visual": """最大深度可以从根向下理解，也可以从叶子向上汇总：

        root
       /    \
   left    right

root 的答案只取决于两个孩子各自能向下走多深：较深的一边再加上 root 自己这一层。""",
        "general_solution": """定义 D(node)=以 node 为根的子树最大深度。空节点深度为 0；非空节点先分别得到左右子树深度，再返回 1+max(left,right)。每个节点只需计算一次。""",
        "limitations": """这个递归关系已经是 O(n) 的自然最优解，没有需要额外消除的重复子问题。真正需要先固定的是‘深度按节点数还是边数计’以及空树基例，否则公式里的 +1 很容易产生口径错误。""",
        "generalization": """树题经常可以把整棵树的问题压成‘每棵子树向父节点返回一个摘要’。先定义返回值的物理意义，再由孩子摘要组合当前答案。""",
        "algorithm_background": guide("后序式树形汇总", "tree-traversal.md"),
    },
    133: {
        "visual": """克隆图不是只复制值，而是复制‘对象 + 连线’：

原图： A ---- B
       |      |
       +-- C -+

克隆： A' --- B'
       |      |
       +-- C'-+

如果原图中的同一个 B 被多条边指向，克隆图中这些边也必须都指向同一个 B'。""",
        "general_solution": """从给定起点沿边遍历可达节点。第一次遇到原节点 x 时创建唯一克隆 x'；随后复制 x 的每条邻接边，把它连接到对应邻居的克隆。若邻居尚未创建，就先创建并安排后续处理。""",
        "limitations": """图可能有环，也可能多条路径到达同一节点。若每次遇见节点都 new 一个副本，会把一个对象复制成多个；若不记录已处理状态，在环上还会无限重复。""",
        "generalization": """真正需要保存的是对象身份映射 original -> clone。它同时解决‘是否已经见过’和‘其它边应该连接到哪个唯一副本’两个问题。""",
        "algorithm_background": guide("图遍历 + 对象身份哈希映射", "graph-search-toposort.md"),
    },
    144: {
        "visual": """前序的规则就是：

        1
       / \
      2   3

先访问根 1，再完整访问左子树 2，最后右子树 3。

如果不用递归，访问 1 后必须记住‘2 应先处理、3 稍后处理’。""",
        "general_solution": """最自然的实现是递归：记录当前节点，然后递归左子树，再递归右子树。它与前序定义一一对应。""",
        "limitations": """递归把尚未处理的右子树和返回位置隐藏在调用栈里。改成迭代时，需要显式保存未来待处理节点；因为后进先出，想先处理左边，就必须先保存右边、再保存左边。""",
        "generalization": """迭代树遍历的关键不是记压栈顺序，而是先写目标访问顺序，再根据‘后保存的先处理’反推待办状态如何放入容器。""",
        "algorithm_background": guide("树的前序遍历与显式递归现场", "tree-traversal.md"),
    },
    145: {
        "visual": """后序的规则是：

        1
       / \
      2   3

必须先完成左子树 2，再完成右子树 3，最后才能处理根 1。

所以根节点要等两个孩子都结束以后才‘结算’。""",
        "general_solution": """最自然的实现是递归：先递归 left，再递归 right，最后记录当前节点。这个顺序特别适合‘父节点依赖孩子结果’的树形问题。""",
        "limitations": """迭代版比前序更容易出错，因为一个节点第一次遇到时还不能立刻输出，必须区分‘第一次到达’和‘孩子都已处理完’。也可以利用根-右-左的顺序再整体反转。""",
        "generalization": """后序本质是完成事件：子问题先结束，父问题再结算。以后树高、直径、最大路径和等题都复用这种‘孩子返回摘要 -> 父节点组合’的时机。""",
        "algorithm_background": guide("树的后序遍历 / 子问题完成顺序", "tree-traversal.md"),
    },
    199: {
        "visual": """从树的右侧看，每一层只能看到一个节点：

        1          -> 1
       / \
      2   3        -> 3
       \   \
        5   4      -> 4

所以问题可以改写成：对每个 depth，只保留这一层最右边的节点。""",
        "general_solution": """按层处理节点时，把当前层所有节点依次扫描，最后一个节点就是这一层的右视图；或者从右侧优先向下走，某个深度第一次到达的节点就是答案。""",
        "limitations": """普通遍历只保证‘所有节点都会访问’，并不能自动说明某个节点是否是这一层最右边。必须额外保留层边界或 depth，才能把二维树结构压成‘每层一个代表’。""",
        "generalization": """许多树题并不需要保存整层信息，只需要每层一个摘要：最右、最左、最大值、平均值等。先明确层级，再决定每层保留什么。""",
        "algorithm_background": guide("树的层 frontier / 带 depth 遍历", "tree-traversal.md"),
    },
    200: {
        "visual": """把陆地格看成互相相连的地块：

1 1 0 0
1 0 0 1
0 0 1 1

从左上角任意一个 1 出发，沿上下左右能走到的所有 1 都属于同一座岛；把整块走完以后，下一次在全局扫描中再遇到未处理的 1，才代表新岛。""",
        "general_solution": """逐格扫描网格。遇到一个尚未处理的陆地格，岛屿数加一，然后从这里沿四个方向把整个连通陆地区域都标记为已处理。回到全局扫描后，属于同一岛的其它格不会再次触发计数。""",
        "limitations": """若只在全局看到 1 就计数，会把同一座岛的每个格都当成新岛；若扩展邻居时不及时标记，环形路径还会重复进入同一格。关键不是搜索顺序，而是‘一次起点必须消费完整连通分量’。""",
        "generalization": """计数连通块的统一结构是：全局枚举尚未归属的起点；每发现一个起点，就用一次局部遍历把整个连通分量标完。起点次数就是分量数。""",
        "algorithm_background": guide("网格连通分量的 DFS/BFS", "graph-search-toposort.md"),
    },
    206: {
        "visual": """反转过程中把链表分成两段：

null <- 1 <- 2    3 -> 4 -> null
            ^     ^
           prev  current

左边已经反转完成；右边仍保持原方向。处理 current 时，必须先记住它原来的 next，再把箭头反过来。""",
        "general_solution": """从头到尾逐个处理节点。对 current：先保存 next=current->next；再令 current->next 指向已经反转好的前缀头 prev；最后让 prev=current、current=next 继续。循环结束时 prev 就是新头。""",
        "limitations": """这已经是 O(n) 时间、O(1) 额外空间的直接解法，难点不在进一步优化，而在破坏性更新的顺序：若先改 current->next 却没保存原 next，未处理后缀会立刻丢失。""",
        "generalization": """原地指针变换应先画清‘已处理区 / 当前对象 / 未处理区’，并在断开旧连接之前保存通往未处理部分的唯一入口。这个不变量可直接迁移到局部反转和链表重排。""",
        "algorithm_background": guide("链表原地反转的三指针不变量", "linked-list-pointer.md"),
    },
    207: {
        "visual": """课程依赖可以画成箭头：

A -> C
B -> C
C -> D

C 只有等 A、B 都完成后才能开始；D 又必须等 C。某门课当前还有多少条未完成的入边，就是它还有多少前置条件没满足。""",
        "general_solution": """一个直观做法是反复扫描所有尚未完成课程，寻找‘所有前置课都已完成’的课程并把它们完成；若某一轮一个都找不到但仍有课程剩余，就说明依赖互相卡住。""",
        "limitations": """反复全表扫描会一遍遍检查同一批尚未满足的前置条件，最坏产生大量重复工作。其实每完成一门课，只会影响它直接指向的后续课程；无需重新检查无关节点。""",
        "generalization": """把每门课尚未满足的前置数压成一个计数。计数降到 0 的瞬间，课程就从‘不可处理’变成‘可处理’，再只通知它的直接后继。若最终仍有节点从未变成 0，则存在循环依赖。""",
        "algorithm_background": guide("入度驱动的拓扑排序", "graph-search-toposort.md"),
    },
    210: {
        "visual": """和课程表判定一样，箭头表示先修关系：

A -> C -> D
B -> C

区别是这次不仅要判断能否全部完成，还要把每次‘已经没有未完成前置’的课程按处理顺序记录下来。""",
        "general_solution": """反复寻找当前所有前置条件都已完成的课程，选一门加入答案，再把它视作已完成，直到没有课程可选。如果最终记录了 n 门课，这个记录顺序就是合法安排；否则无解。""",
        "limitations": """若每选一门课都重新扫描所有课程和前置关系，会大量重复判断。真正变化的只有这门课的直接后继：它们各自少了一个未完成前置。""",
        "generalization": """维护每个节点的未满足前置计数；计数变 0 就进入可处理集合。把出队/处理顺序直接写入 answer，便同时完成‘判环’和‘构造一个拓扑序’。""",
        "algorithm_background": guide("拓扑排序并输出处理顺序", "graph-search-toposort.md"),
    },
    226: {
        "visual": """翻转不是交换节点值，而是对每个节点交换左右子树：

      4                 4
     / \               / \
    2   7     ->       7   2
   / \ / \           / \ / \
  1  3 6  9         9  6 3  1

根完成交换后，左右两棵子树内部还要做同样的事情。""",
        "general_solution": """对任意非空节点，交换它的 left/right，然后分别对交换后的两个孩子继续做同样操作；空节点直接结束。每个节点只需访问一次。""",
        "limitations": """这个递归定义已经与题目结构完全一致，没有需要额外消除的重复工作。主要风险是只交换根的一层、忘记继续处理子树，或错误地复制新树而没有意识到题目允许原地修改。""",
        "generalization": """当一棵树的变换对每个节点都遵循同一个局部规则，而且子树之间互不依赖时，可以把整题直接定义成‘处理当前节点 + 对两个子树重复同一操作’。""",
        "algorithm_background": guide("树的递归结构变换", "tree-traversal.md"),
    },
    236: {
        "visual": """先从最容易验证的思路看：

root -> ... -> p 的路径
root -> ... -> q 的路径

两条路径从 root 开始有一段共同前缀；共同前缀最后一个节点，就是最近公共祖先。

例如：
root -> 5 -> 6
root -> 5 -> 2 -> 4
共同前缀最后是 5。""",
        "general_solution": """可以分别找到 root 到 p、root 到 q 的两条路径，再从头比较，最后一个相同节点就是答案。这个方法直观且正确，但需要保存路径/父指针信息。""",
        "limitations": """两条路径法会显式保存从根到目标的历史。其实站在某个节点时，只需要知道‘p/q 是否出现在左子树、右子树或当前节点’，无需保留完整路径；这些信息可以由子树返回值直接汇总。""",
        "generalization": """若当前节点本身是目标，就把自己向上报告；否则分别询问左右子树。左右都报告非空，说明两个目标第一次在这里汇合；只有一边非空，就把那边继续向上传。""",
        "algorithm_background": guide("后序汇总定位最近公共祖先", "tree-traversal.md"),
    },
    695: {
        "visual": """与岛屿数量不同，这次每发现一块新陆地，不只要确认它属于哪座岛，还要数完整座岛有多少格：

1 1 0
1 1 0   -> 这一块面积 4
0 0 1   -> 另一块面积 1

每个连通块只应统计一次。""",
        "general_solution": """全局扫描未处理陆地。每发现一个新起点，就沿四邻把整个连通块走完，并在访问一个新陆地格时把当前面积加一；这次局部遍历结束后用面积更新最大值。""",
        "limitations": """若同一陆地格能在局部遍历中被重复进入，面积会重复计数；若不在全局层面区分已处理格，同一座岛还会被多次启动。标记时机必须保证每格只计一次。""",
        "generalization": """‘岛屿数量’与‘最大岛面积’共享同一个连通分量框架；区别只是每次吃掉完整分量后，是把答案 +1，还是返回该分量的节点数做 max。""",
        "algorithm_background": guide("网格连通分量遍历 + 分量聚合", "graph-search-toposort.md"),
    },
    1109: {
        "visual": """一条预订 [L,R] += seats 的含义是：从 L 开始，多了一份持续有效的 seats；在 R 之后，这份影响才结束。

位置:    1   2   3   4   5
预订:       +10 ---------
变化:       +10         -10

区间内部没有新事件，只是在延续同一份状态。""",
        "general_solution": """最直接的正确实现是对每条 booking=[first,last,seats]，循环 i=first..last，把 answer[i] += seats。它完全照题意更新，若预订区间很长且条数很多，最坏会接近 O(bookings * n)。""",
        "limitations": """同一条预订在整个连续区间里做的是完全相同的 +seats；逐点写入只是重复表达‘这份状态仍然有效’。真正发生变化的只有开始位置和结束位置之后两个边界。""",
        "generalization": """把连续区间操作改写成边界事件：L 记录 +seats 表示状态开始，R+1 记录 -seats 表示状态结束。随后从左到右累计所有仍有效的事件，就能恢复每个位置真实值。""",
        "algorithm_background": guide("差分数组：记录边界变化，再用累计状态恢复", "prefix-sum-difference.md"),
    },
}
