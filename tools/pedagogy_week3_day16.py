"""High-touch pedagogy overrides for Week 3 / Day 16.

Day 16 turns the graph-traversal foundation from Day 15 into shortest-distance
and reverse-reachability models.  The common theme is to stop repeating one
search per start/target and instead choose the direction and source set so one
BFS wave answers many states at once.
"""

PEDAGOGY_WEEK3_DAY16 = {
    127: {
        "pattern": "隐式无权图最短路：按规则现场生成邻居，而不是先显式两两建图",
        "model": "把每个合法单词看成图节点；两个单词只差一个字符时存在一条无权边。Day 15 已经建立 BFS frontier/visited 模型，本题的新点不是 BFS，而是图太密集地隐藏在字符串变换规则里：无需先比较所有单词对，只要从当前单词逐位尝试 26 个字符，就能现场生成可能邻居。",
        "derivation": """一个完全正确的直接方案，是先显式建图：对 wordList 中每一对单词逐字符比较，若恰好一位不同就连边，再从 beginWord 做 BFS。它容易证明正确，但 N 个单词会产生 O(N^2) 对比较，每次比较还要看 O(L) 个字符，建图本身就可能达到 O(N^2·L)。

真正重复的工作是：为了知道当前 word 的邻居，提前把它与所有其他单词都比较了一遍。其实邻居必须只改变一个位置，因此候选空间可以由规则直接生成：固定位置 i，把该字符改成 a..z，最多只产生 25L 个真正可能相邻的字符串，再用哈希集合 O(1) 平均查询它是否在字典中。

于是“先枚举所有节点对找边”变成“BFS 走到一个节点时现场生成它可能存在的边”。图仍然完全相同，只是不再物化无用的 O(N^2) 候选边。

优化类型：重复两两比较 -> 利用局部变换规则生成隐式邻居；随后复用无权图 BFS 求最少边数。""",
        "visual": """例：begin=hit，end=cog。

不要先画完整图，只看 BFS 当前节点如何生成邻居：

hit
 |- 改第0位：ait, bit, ...
 |- 改第1位：hat, hbt, ... hot ...
 `- 改第2位：hia, hib, ...

只有出现在 unused 字典中的候选才是真实边，例如：

hit -> hot -> dot -> dog -> cog
        \\-> lot -> log --/

队列按层展开：
距离1: hit
距离2: hot
距离3: dot, lot
距离4: dog, log
距离5: cog

这里 distance 表示“序列中包含多少个单词”，所以起点不是 0，而是 1。""",
        "core": "把字符串变换规则当成隐式边生成器；BFS 每弹出一个单词，只枚举一位替换得到的真实候选，首次到达 endWord 时就是最短转换序列。",
        "formula": """节点：合法单词。
边：两个单词长度相同且恰好一个字符不同。
每条边代价 = 1。

BFS 状态：
(word, distance)
distance = 从 beginWord 到 word 的序列单词数。

邻居生成：
对每个位置 i：
  对 c in ['a','z'] 且 c != original：
    candidate = word，仅第 i 位改成 c
    candidate ∈ unused => 存在一条尚未访问的真实边

入队：
(candidate, distance + 1)
并立即从 unused 删除。

“入队即删除”是不变量的一部分：一个单词只允许第一次、也就是最短距离时进入队列。""",
        "steps": [
            "把 wordList 放入 unordered_set unused；若 endWord 根本不在字典中，按题意直接返回 0",
            "从 unused 删除 beginWord，并以 (beginWord,1) 入队；1 表示序列已经包含起点这个单词",
            "弹出当前 word 后，对每个字符位置保存 original，再依次尝试 26 个小写英文字母",
            "候选若能在 unused 中 find 到，就以 distance+1 入队，并立刻 erase 该集合元素作为 visited 标记",
            "一个位置的所有替换完成后恢复 original，继续下一个位置；首次弹出 endWord 时返回 distance",
            "队列耗尽仍未到终点则返回 0",
        ],
        "proof": "现场生成邻居不会漏边：任何与 word 恰差一个字符的合法单词，都对应某个唯一位置 i 和替换字符 c，因此一定会在 25L 个候选中出现。BFS 在无权图中按边数非降序发现节点；一个单词第一次入队时已经获得最短距离，所以立即从 unused 删除不会丢失更短路径，只会阻止等长或更长路径重复搜索它。endWord 第一次被处理时的 distance 因而是最短序列长度。",
        "pitfalls": "返回的是序列中的单词数，不是变换次数，所以 beginWord 初始 distance=1。beginWord 不要求存在于 wordList，但 endWord 必须存在。修改 word[i] 后必须在完成该位置枚举时恢复 original，否则下一位置会在已经被修改的字符串上继续生成错误候选。纯查询使用 find，删除使用 erase(it)，均兼容 C++17。",
        "transfer": "直接复用 Day 15 的 BFS/visited，只把“邻接表”换成“邻居生成函数”。以后遇到锁组合、基因变换、拼图、最少操作数等问题，若每一步只做局部合法修改且代价相同，应优先问：状态是不是节点？操作是不是隐式边？是否根本不需要把整张图显式建出来？",
        "memory": "图不用先建出来：当前状态现场生成一跳候选；无权最短路仍然交给 BFS。",
    },

    417: {
        "pattern": "反向多源可达性：把“每个格子能否到终点”改成“终点能反向到哪些格子”",
        "model": "Day 15 的岛屿题从未知陆地出发找整个连通块；本题若也从每个格子分别向两个海洋搜索，会大量重复走同一片区域。更好的视角是把水流边全部反向：原来只能从高处流向不高于自己的格子，反过来就从海岸只向不低于当前高度的格子爬。分别从两片海洋边界做一次多源 BFS，交集就是同时能流向两洋的格子。",
        "derivation": """最直接的正确做法，是对每一个格子 (r,c) 分别执行搜索：沿着 nextHeight<=currentHeight 的真实水流方向，看能否碰到太平洋边界、又能否碰到大西洋边界。单次搜索最坏 O(mn)，一共有 mn 个起点，因此最坏可能达到 O((mn)^2)，而且相邻起点会一遍遍探索相同区域。

问题问的是“很多起点能否到少数终点集合”。把边方向反过来后，关系完全等价：
A 正向能到 Ocean
<=> Ocean 在反向图中能到 A。

于是无需从 mn 个起点重复问同一个问题。把太平洋全部边界格同时作为源做一次反向洪泛，得到 pacific；再对大西洋做一次，得到 atlantic。每个格子在每次洪泛中最多访问一次。

优化类型：每个起点重复可达性搜索 -> 反转边 + 从终点集合做多源搜索；复杂度压到 O(mn)。""",
        "visual": """正向水流规则：

高 5  --->  低 3
只允许往不更高的位置流。

把问题反过来看：

Ocean ---> 岸边 ---> 内陆
             3   ---> 5

反向搜索只能“爬坡”或走平地：
nextHeight >= currentHeight

太平洋源集合：上边界 + 左边界
P P P P
P . . .
P . . .

大西洋源集合：下边界 + 右边界
. . . A
. . . A
A A A A

分别反向洪泛后：
Pseen ∩ Aseen
就是原方向下同时能流向两边海洋的格子。""",
        "core": "不要从每个格子向海洋重复找路；把水流边反过来，从两个海洋边界各做一次多源洪泛，最后取两份可达集合的交集。",
        "formula": """原方向允许：
height[next] <= height[current]

反向边允许：
height[next] >= height[current]

定义：
pacific[r][c] = 太平洋边界在反向图中能否到达 (r,c)
atlantic[r][c] = 大西洋边界在反向图中能否到达 (r,c)

由反向可达等价：
(r,c) 正向能到太平洋 <=> pacific[r][c] = true
(r,c) 正向能到大西洋 <=> atlantic[r][c] = true

最终条件：
pacific[r][c] && atlantic[r][c]。""",
        "steps": [
            "准备两张独立 seen 表 pacific/atlantic；同一个格子对两个海洋的可达性是两份不同状态",
            "把上边界与左边界收集为太平洋多源起点，把下边界与右边界收集为大西洋多源起点",
            "flood 初始化时对起点先检查 seen，避免四个角因为属于两条边而在同一次洪泛中重复入队",
            "BFS 每次只向未访问且 heights[nr][nc]>=heights[r][c] 的邻格前进，这正是原水流边的逆边",
            "两次 flood 完成后扫描整个矩阵，只收集 pacific 与 atlantic 同时为 true 的坐标",
        ],
        "proof": "若某格 x 正向能沿合法水流边到达海洋，则把该路径完全倒过来，每一步都满足反向的“不下降”条件，所以海岸源在反向搜索中一定能到 x。反之，任何海岸到 x 的反向路径倒过来后都成为 x 到海岸的合法水流路径。因此两份 seen 分别精确描述到两个海洋的正向可达集合，取交集既不会漏也不会多。",
        "pitfalls": "相等高度在原题允许流动，所以反向条件必须是 >=，不能写 >。两个海洋必须有独立 seen。角落会同时出现在同一海洋的两条边源集合中，入队前检查 seen 即可去重。当前实现使用 structured binding、lambda、move，均属于 C++17 或更早；没有依赖 C++20 API。",
        "transfer": "它和 LC-542 的共同思想是“很多位置都在问离/能否到某类目标”，于是从目标集合反向统一搜索。更一般地，在有向图中如果问题是大量节点到固定终点集合的可达性，可以考虑反图；在无权图中若目标集合是多个点，再叠加多源 BFS。",
        "memory": "起点太多、终点很少时先问能否反边：从终点一起往回找。",
    },

    542: {
        "pattern": "多源 BFS 最近距离：把所有目标同时放进第 0 层，让最近的波前自然获胜",
        "model": "Day 15 的 BFS 已说明首次到达给出无权图最短距离。本题对每个 1 都想知道“最近哪个 0”，如果每个 1 自己启动 BFS 会重复搜索。把问题反过来：所有 0 都是距离 0 的源，同时进入同一个队列；距离 1、2、3 的波纹向外扩散，每个格子第一次被某个波纹碰到时，就是到最近 0 的距离。",
        "derivation": """最自然的正确做法，是对每个值为 1 的格子单独做 BFS，直到找到第一个 0。单个 BFS 最坏访问整个矩阵 O(mn)，如果几乎所有格子都是 1，就会把同一片网格重复搜索很多次，最坏接近 O((mn)^2)。

观察所有这些独立 BFS 的目标完全相同：都在寻找“0 集合”。距离又是对称的——A 到 B 的四邻步数和 B 到 A 相同。因此无需让每个 1 去寻找 0，可以让所有 0 同时向外传播。

把所有 0 先放到距离 0 层，相当于增加一个虚拟超级源，用 0 代价连到所有真实 0；普通 BFS 每向外一层距离加 1。不同源的波前相遇时，无需比较第二次，因为首次到达已经是全体源中的最短距离。

优化类型：每个查询点重复最短路 -> 目标集合变成多源起点，一次 BFS 同时回答所有格子。""",
        "visual": """mat：
0 0 0
0 1 0
1 1 1

先把所有 0 同时放进第 0 层：
0 0 0
0 ? 0
? ? ?

它们一起向外扩散：

距离0: 所有原始 0
距离1: 中心和底部两侧
距离2: 底部中间

结果：
0 0 0
0 1 0
1 2 1

不是“某个 0 跑遍全图”，而是所有 0 的波前同时扩张；谁先碰到一个格子，谁就是它最近的 0。""",
        "core": "所有 0 同时作为距离 0 的 BFS 源；未访问格第一次被任意源的波前到达时，当前层数就是它到最近 0 的距离。",
        "formula": """distance[r][c]：到任意 0 的最短四邻步数。

初始化：
mat[r][c] == 0 => distance[r][c] = 0，入队
mat[r][c] == 1 => distance[r][c] = -1，表示尚未确定

状态转移：
若 neighbor 尚未访问：
distance[neighbor] = distance[current] + 1
neighbor 入队

BFS 队列保证 current 的 distance 非降序，因此 neighbor 第一次赋值就是：
min over all zero sources (distance(source, neighbor))。""",
        "steps": [
            "创建与 mat 同尺寸的 distance，统一初始化为 -1；扫描矩阵，把所有 0 的 distance 设为 0 并全部入队",
            "从队首弹出当前格，枚举上下左右四个相邻位置",
            "越界或 distance 已经不是 -1 的格子跳过；后者表示已经被更早或同层的最近波前确定",
            "对首次到达的邻格赋 distance[current]+1，并立即入队，保证它只处理一次",
            "队列耗尽后返回 distance；题目保证至少存在一个 0，所以所有格子最终都会得到距离",
        ],
        "proof": "把所有 0 看成同时出发的源。BFS 第 k 层恰好包含与最近源距离为 k 的尚未处理节点：第 0 层显然成立；若 current 的最短距离为 k，则任何首次由它发现的邻居存在一条长度 k+1 的路径。若该邻居存在更短路径，它应当已被更早层的某个源发现，与“尚未访问”矛盾。因此第一次赋值就是到所有 0 中最近一个的距离。",
        "pitfalls": "所有 0 必须在 BFS 开始前同时入队，不能逐个 0 分别跑完整 BFS。`-1` 同时承担未访问标记和“距离尚未确定”状态，避免另开 visited。题目是四方向步数，不允许对角线。当前代码仅使用 C++17 或更早特性。",
        "transfer": "与 LC-994 完全同源：LC-542 的层数表示“离最近 0 多远”，LC-994 的层数表示“最早第几分钟被腐烂”。以后看到“每个点到最近医院/门/出口/障碍的距离”，优先把所有目标点一起作为第 0 层。",
        "memory": "最近哪个源？不要每个点自己找；让所有源同时从距离 0 向外扩散。",
    },

    994: {
        "pattern": "多源 BFS 时间传播：同一层代表同一分钟，状态改变时立即入下一层",
        "model": "本题不是新的搜索模板，而是 LC-542 多源 BFS 的时间版本：所有初始腐烂橘子都是时间 0 的源；第一层感染发生在第 1 分钟，下一层是第 2 分钟。额外维护 fresh 数量，就能区分“所有目标都已被波前覆盖”和“仍有隔离区域永远到不了”。",
        "derivation": """最贴近题意的直接模拟，是每一分钟都重新扫描整张网格：找到当前腐烂橘子附近的新鲜橘子，先记录本分钟将腐烂的位置，扫描结束后统一更新，再进入下一分钟。它正确，但每分钟都要 O(mn) 全表扫描；传播最长又可能持续 O(mn) 分钟，因此最坏 O((mn)^2)。

浪费在于：绝大多数格子每一分钟都被重新检查，即使它与当前传播边界毫无关系。真正能在下一分钟制造新变化的，只有“这一分钟刚刚处于腐烂前沿”的那些橘子。

队列正好保存这个活动前沿。把所有初始腐烂橘子同时作为第 0 层；固定当前 layer 大小时，这一层产生的新腐烂橘子只入队，不在本层继续传播，下一轮再处理，于是队列层边界天然实现了“同步一分钟”。

优化类型：每分钟全网格重复扫描 -> 只维护会产生新事件的传播前沿；多源 BFS 把总时间降为 O(mn)。""",
        "visual": """初始：
2 1 1
1 1 0
0 1 1

时间0 frontier：所有初始 2
[左上角]

第1分钟：只处理时间0 frontier
2 2 1
2 1 0
0 1 1
新腐烂节点进入 next frontier

第2分钟再处理上一分钟新加入的节点。

关键层边界：
layer = rotten.size()
只处理这 layer 个旧节点。
处理过程中 push 的新节点属于“下一分钟”，不能马上在同一分钟继续感染。

fresh 则是一个剩余工作计数器：
每感染一个 1：--fresh
fresh==0 时说明所有新鲜橘子都已被某一层波前覆盖。""",
        "core": "队列只保存当前传播前沿；冻结 layer 后处理这一分钟已有的腐烂橘子，新腐烂节点只进入下一层，因此 BFS 层数与真实分钟一一对应。",
        "formula": """初始：
所有 grid==2 的格子入队，时间为 0
fresh = grid==1 的格子总数

每轮：
layer = queue.size()
minutes++
只弹出 layer 个节点

若邻格为新鲜橘子：
grid[nr][nc] = 2
fresh--
push(neighbor)

不变量：
一轮开始时，队列前 layer 个节点恰好是在 minutes-1 时刻已经腐烂、能在本分钟向外传播的 frontier。

结束：
fresh == 0 => minutes
否则队列耗尽但 fresh>0 => 存在不可达新鲜橘子，返回 -1。""",
        "steps": [
            "先完整扫描一次网格：所有初始 2 入队，同时统计 fresh；这一步建立时间 0 的全部多源前沿",
            "若 fresh 一开始就是 0，while 条件不会进入，最终自然返回 0",
            "每分钟开始冻结 layer=rotten.size()，再 minutes++；只处理这 layer 个节点，隔离本分钟与下一分钟",
            "枚举四邻，只对值仍为 1 的格子执行 1->2、fresh--、入队；改成 2 就是入队即 visited，防止多个腐烂邻居重复感染同一格",
            "队列为空或 fresh 清零时结束；fresh==0 返回 minutes，否则返回 -1",
        ],
        "proof": "所有初始腐烂点在时间 0 同时作为 BFS 源。由无权多源 BFS 性质，一个新鲜格第一次被传播前沿到达的层数就是它到最近初始腐烂点的最短四邻距离，也正是它最早可能腐烂的分钟。冻结 layer 保证新加入节点必须等到下一轮才能继续传播，与题目的同步分钟过程一致。若最终仍有 fresh，则这些格子不在任何初始腐烂源的可达区域中，因此不可能腐烂。",
        "pitfalls": "不能在处理当前节点时让刚入队的节点立刻继续传播，否则会把多分钟压成一分钟；所以必须冻结 layer。感染时立即把 1 改成 2，避免同一新鲜橘子被多个邻居重复入队和重复 fresh--。没有新鲜橘子应返回 0。这里没有使用任何 C++20/C++23 API。",
        "transfer": "它和 LC-542 是同一个多源 BFS：前者把层数解释为最早感染时间，后者解释为最近目标距离。再往外推广，火灾、病毒、信号扩散、多个入口的最短到达时间都只是在改变“源是什么、哪些格子可传播、层数代表什么”。",
        "memory": "每分钟只处理当前 frontier；所有源时间 0 同时入队，新节点留到下一层。",
    },
}
