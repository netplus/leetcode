"""Optional optimization-derivation backfills for already reviewed problems.

The main pedagogy modules explain the final algorithm with the seven-layer
structure.  Some optimized solutions also need an explicit bridge from a real
direct algorithm to the optimized mechanism, as required by AGENTS.md.

Keep only that bridge here.  Problems whose primary solution is already the
natural direct algorithm should not receive a forced derivation block.
"""

DERIVATION_OVERRIDES = {
    560: """最自然的正确做法，是枚举每个 left，再向右扩展 right 并维护区间和；这样确实能检查所有连续子数组，但会产生 O(n^2) 个区间。

换成“固定右端点”来看：扫描到当前位置时，当前前缀和 prefix 已经确定。若某个历史位置之后到当前的区间和为 k，那么那个历史前缀也被唯一确定：oldPrefix = prefix-k。

因此暴力真正重复做的不是“计算 k”，而是在大量历史起点里反复寻找这个已经确定的 oldPrefix。把历史前缀建立成 prefix -> frequency 的索引后，就把每次重新扫描历史改成一次哈希查询。

优化类型：重复查找 -> 历史状态索引。算法名最后才是“前缀和 + 频次哈希表”。""",

    167: """最直接的做法仍然是枚举所有下标对 (i,j)，检查 numbers[i]+numbers[j] 是否等于 target，时间 O(n^2)。

题目额外给出的“数组有序”意味着一次比较不只排除当前这一对。当两端和太小时，left 已经与当前最大的 right 相加都不够，那么 left 与中间任何更小元素相加都不可能成功；这一整批候选可以同时删除。和太大时对 right 完全对称。

优化类型：存在单调性 -> 一次判断排除整批候选。于是两层枚举被压缩成两根只向内移动的指针，总扫描 O(n)。""",

    15: """最自然的 3Sum 是三层循环枚举 i、j、k，时间 O(n^3)。真正的突破不是直接记住“三数之和要排序双指针”，而是先固定一个数 nums[i]：剩余问题立即变成“在后面的元素里寻找两数之和等于 -nums[i]”。

如果剩余区间无序，这个二数问题仍要反复搜索；排序之后，就可以完整复用 LC-167 的单调淘汰：和小排除 left，和大排除 right。这样每个固定 i 只需线性扫描一次。

优化类型：问题降维 + 排序制造单调性。复杂度从 O(n^3) 降为排序 O(n log n) 加 n 次 O(n) 双指针，即 O(n^2)。""",

    11: """最直接的做法是枚举所有左右边界 (left,right)，计算每个容器面积，时间 O(n^2)。

优化的关键是看清一次比较能永久淘汰谁。若左板更短，那么当前面积已经使用了“保留这块左板时能够得到的最大宽度”；以后只移动右边界，宽度一定更小，而有效高度仍不可能超过这块左板，所以这些候选全部被当前状态支配。

因此真正可尝试的方向只有“换掉短板”。每轮都能永久删除一个端点，O(n^2) 的端点组合枚举被压缩成 O(n) 次安全淘汰。

优化类型：候选被支配 -> 双指针贪心淘汰。""",

    209: """最自然的正确算法，是从每个 left 出发重新向右累加，找到第一个 sum>=target 的 right；最坏会为不同 left 重复扫描大量相同元素，达到 O(n^2)。

本题所有 nums[i] 都是正数，这让窗口拥有可利用的单调性：right 右移只会让和变大，left 右移只会让和变小。于是当某个 right 已经让窗口合法时，寻找更短答案只需继续推进 left；窗口变非法后，这个 left 永远不需要回到过去，只需继续推进 right。

相邻候选窗口高度重叠，旧窗口的 sum 可以通过“右边加一个、左边减一个”继续沿用，不必为每个 left 重新求和。

优化类型：状态可延续 + 正数带来的单调性 -> 滑动窗口。""",

    3: """最直接的做法可以枚举每个起点，并向右扩展直到遇到重复字符；不同起点之间会反复检查大量相同的字符串片段，最坏 O(n^2)。

扫描到新字符 c 时，旧窗口本来已经无重复，因此唯一新增的冲突只可能来自 c 自己。若记住 c 最近一次出现的位置，就无需从 left 开始一个字符一个字符重新检查；可以直接把 left 跳到那个冲突位置之后。

这里复用的是“最近冲突位置”这一小段历史状态。由于 left 只向右移动，被排除的前缀永远不会重新成为合法窗口的一部分。

优化类型：重复检查 -> 记录最近冲突位置 + 单调左边界。""",

    438: """最直接的做法，是对 s 中每个长度 |p| 的子串重新统计 26 个字符频次，再与 p 比较。相邻窗口只有两个字符不同，却把中间 |p|-1 个字符反复统计；按窗口长度重新计数会造成大量重复工作。

滑动一格时真正发生的变化只有两个事件：右边进入一个字符、左边离开一个字符。因此只要维护一张可增量更新的 need 账本，就可以把上一窗口的状态直接延续到下一窗口，而不是从零重建频次。

missing 再把“检查 26 个字符是否全部满足”压缩成一个总欠账数，使每次窗口移动只做 O(1) 状态更新。

优化类型：重复计算 -> 相邻窗口增量维护 + 摘要状态。""",

    76: """最自然的做法，是枚举每个 left，再向右寻找第一个覆盖 t 的窗口；换一个 left 后又从头统计字符需求，很多相邻候选会反复扫描和计数同一段字符。

真正可复用的是当前窗口的字符账本：right 加入一个字符只修改一项 need，left 移出一个字符也只恢复一项 need。更关键的是，left 没必要回退——当窗口已经覆盖 t 时，只需向右收缩 left 寻找当前 right 下的最短合法窗口；一旦刚刚失去覆盖，再继续推进 right。

因此算法并不是“套一个滑动窗口模板”，而是把大量重叠窗口的重复统计压缩为两个单调边界上的增量事件。

优化类型：状态可延续 + 边界单调移动 -> 可变长度滑动窗口。""",

    303: """如果 sumRange 只调用一次，最自然也最合适的做法就是直接遍历 [left,right] 求和，时间 O(n)，完全没有必要使用前缀和。

题目的关键是同一个不可变数组会被查询很多次。不同查询会一次又一次把相同的前缀元素重新相加；这些累计结果与具体查询无关，可以只预处理一次。

于是把“每次查询现场重复求和”改成“构造时保存每个边界的累计值”。查询 [left,right] 时，两份已保存累计值相减即可消掉共同前缀。

优化类型：重复计算 -> 预处理换查询时间。构造 O(n)，单次查询从 O(n) 降为 O(1)。""",

    304: """二维情况下，最直接的 sumRegion 会遍历查询矩形里的每个单元格；矩形面积越大，单次查询越慢，而且多次查询会反复累加大量重叠区域。

矩阵本身在构造后不会修改，因此可以把“从左上原点到每个二维边界的矩形和”预处理一次。以后任何目标矩形都不再逐格扫描，而是用四个已经保存的左上矩形做容斥。

这和 LC-303 完全同源：一维用两个边界累计值消掉公共前缀，二维用四个累计矩形消掉公共区域。

优化类型：重复计算 -> 二维预处理 + 容斥查询。预处理 O(mn)，单次查询 O(1)。""",

    238: """最直接的做法，是对每个 i 再遍历一遍数组，把所有 j!=i 的值相乘；这样每个答案都重新计算大量相同乘积，总时间 O(n^2)。

对固定 i，真正需要的信息天然只有两块：i 左边所有元素的乘积，以及 i 右边所有元素的乘积。相邻位置之间，这两块累计值可以持续复用：从左扫描时 prefix 只需乘上刚经过的一个元素；从右扫描时 suffix 同理。

于是把“每个 i 重新乘 n 个元素”改成“两次线性扫描维护可延续累计状态”。最终 answer[i]=leftProduct*rightProduct。

优化类型：重复计算 -> 前缀/后缀累计状态复用。""",

    28: """最自然的字符串匹配，是让 needle 从 haystack 的每个可能起点重新比较；一旦后面失配，就把文本起点右移一格并从 needle[0] 重来。最坏情况下，大段已经比较相等的字符会被反复比较，达到 O(nm)。

真正可以复用的是“已经匹配成功的那一段”本身。若它的某个后缀同时也是 needle 的前缀，那么失配后无需让文本指针回退；这段后缀已经被证明和模式前缀相同，只要让模式指针跳到对应长度继续比较即可。

lps 就是在预处理 needle 时，把每个状态失配后还能保留的最长边界提前算好。

优化类型：重复比较 -> 复用已确认的前后缀状态。最终文本指针不回退，复杂度 O(n+m)。""",

    179: """一个完全直接且正确的思路，是枚举所有排列、把每种排列拼成字符串后取最大；它说明了目标函数是什么，但排列数是 n!，无法实际使用。

观察任意相邻的两个元素 a、b：在其他前缀和后缀固定时，这两项只存在 ab 与 ba 两种局部顺序。如果 ab < ba，那么交换这两个相邻元素一定让整个结果变大；因此最优排列中不允许存在这种可改进的相邻逆序。

于是全局排列搜索可以压缩成一个两两比较规则：a 应排在 b 前面，当且仅当 a+b > b+a。排序负责系统地消除所有这类局部逆序。

优化类型：从全局排列目标推出局部交换准则 -> 自定义排序。""",

    239: """最自然的做法是每滑动一个长度 k 的窗口，就重新扫描窗口里的 k 个数求最大值，时间 O(nk)。相邻两个窗口共享 k-1 个元素，这些最大值候选被一遍遍重复比较。

真正没必要保存的是“已经被新元素完全支配”的旧候选：若旧元素更小或相等，而且下标更早，那么只要新元素还在窗口里，旧元素就不可能成为最大值；等新元素离开时，旧元素反而更早已经过期。

因此窗口不再保存所有元素，只保存仍有资格成为未来最大值的候选，并额外从队首删除过期下标。

优化类型：重复扫描 + 候选被支配 -> 单调队列。每个下标只入队一次、出队一次，总复杂度 O(n)。""",

    41: """一个很自然的 O(n) 解法，是先把所有正整数放进 unordered_set，再从 1 开始查询第一个不存在的值；它已经满足时间目标，但需要 O(n) 额外空间。

题目进一步要求 O(1) 额外空间。关键观察是：长度为 n 的数组，答案只可能在 1..n+1；而值 x∈[1,n] 恰好可以映射到唯一槽位 x-1。既然数组本身就有 n 个槽位，就无需再额外建立哈希桶。

把每个合法值原地送到自己的槽位后，数组本身就变成了“值是否出现”的索引表。

优化类型：额外哈希表 -> 利用值域与下标的一一映射，把输入数组原地当作哈希表。""",

    128: """最直接且容易想到的做法是排序后扫描连续段，时间 O(n log n)。题目要求 O(n)，所以排序这一步必须被拿掉。

用哈希集合可以把“x+1 是否存在”降成平均 O(1) 查询，但如果从每个 x 都向后走一遍连续链，像 1,2,3,...,n 这样的输入仍会重复扫描同一条链，退化到 O(n^2)。

关键是只允许链头启动扫描：当 x-1 不存在时，x 才是某条连续段唯一的起点；链内部节点全部跳过。于是每条连续段只会被完整走一次。

优化类型：排序 -> 哈希查询；再消除重复链扫描 -> 只从唯一链头启动。""",

    160: """最自然的线性解法之一，是先把链表 A 的所有节点地址放进 unordered_set，再扫描 B，遇到第一个已存在地址就是交点；时间 O(m+n)，但需要 O(m) 额外空间。

若要 O(1) 空间，真正需要消除的是两条独有前缀的长度差。让 pA 依次走 A 再走 B，让 pB 依次走 B 再走 A，两者都会走完全相同的总路程 m+n；只是把各自的“长出来的那一段”放到了不同阶段。

因此无需存 visited，也无需先显式计算两条链长度，换头本身就在重新排列路径以完成对齐。

优化类型：辅助集合/显式长度对齐 -> 两条等长虚拟路径自动抵消长度差。""",

    141: """最自然的判环方法，是边走边把访问过的节点地址放进 unordered_set；某个节点第二次出现就说明有环。这个方法直观且 O(n)，但需要 O(n) 额外空间。

链表的 next 是确定性的：从任意节点出发，下一状态只有一个。若存在环，进入环后状态会周期性重复；因此可以不保存全部历史，而用两个不同速度的观察者检测“是否进入周期”。

fast 每轮比 slow 多走一个节点。在有限环上，这个相对位移按 +1 mod L 演化，最终必回到 0；无环时 fast 则会先到 nullptr。

优化类型：保存全部 visited 状态 -> 用相对速度在确定性状态机中 O(1) 空间检测周期。""",

    234: """最自然的做法是把链表值复制到 vector，然后像数组一样左右双指针比较；时间 O(n)，但需要 O(n) 额外空间。

若希望 O(1) 额外空间，问题只剩一个障碍：单链表没有“从尾部向前”的指针。与其保存所有节点，不如改变后半段的方向：先用快慢指针找到中点，再原地反转后半段，就把“左边向右、右边向左”转换成“两边都沿 next 向右”。

比较完成后再反转一次恢复结构。

优化类型：复制数组获得反向访问能力 -> 原地改变后半段方向，复用 LC-206 反转。""",

    105: """递归结构本身很自然：preorder 当前第一个未消费值确定根，然后在当前 inorder 区间里找到这个根的位置，再切成左右子树。

如果每层递归都在线性扫描 inorder 寻找 rootValue，这个算法仍然正确，但极端链状树会产生 n+(n-1)+...+1 次查找，最坏 O(n^2)。

题目保证节点值唯一，因此 rootValue 在 inorder 中的位置可以提前建立成 value -> index。每个递归层的“重新线性找根”就变成一次 O(1) 查询，而每个节点只消费一次。

优化类型：递归中的重复查找 -> 预建 inorder 索引。整体从最坏 O(n^2) 降到 O(n)。""",

    543: """一个直接思路是枚举每个节点作为路径最高点，再分别计算它左、右子树的高度，用 leftHeight+rightHeight 更新直径。逻辑正确，但若每个节点都重新递归求高度，同一棵子树会被多个祖先重复计算，最坏 O(n^2)。

后序遍历本来就会先完成孩子再回到父亲，所以可以让每棵子树只计算一次高度，并在返回父节点前顺手上报这个高度；当前节点拿到左右高度时同时完成一次直径候选更新。

于是“求高度”和“枚举最高点”被融合到同一趟 DFS。

优化类型：重复子问题 -> 后序返回可复用状态。每个节点只处理一次，O(n)。""",

    155: """如果只使用普通栈，push/pop/top 都是 O(1)，但 getMin 只能临时扫描整个栈找最小值，单次 O(n)。若 getMin 被频繁调用，就会反复扫描同一批历史元素。

最小值其实可以在 push 时增量维护：新状态的最小值只取决于“上一层最小值”和“新压入的 val”。所以每层除了保存 value，再保存进入这一层后的 minimum 摘要。

pop 时也无需重新计算——弹掉当前层后，下面一层保存的摘要天然就是恢复后的正确最小值。

优化类型：查询时重复扫描 -> 更新时提前维护可回滚摘要，使四种操作全部 O(1)。""",

    739: """最直接的做法，是对每一天 i 向右逐日寻找第一个更高温度；最坏每个位置都要扫描很远，总时间 O(n^2)。

换成从左到右看，真正需要保存的只有“还没有找到答案的历史日期”。当前温度到来时，如果它比某些历史日期更高，那么这些日期的答案就在这一刻第一次被确定，可以立即结算并永久删除。

为了快速知道哪些未决日期能被当前温度结算，让栈中温度保持单调不增；当前温度只需从栈顶连续处理更冷的日期。

优化类型：每个位置向未来重复搜索 -> 保存未决状态，由未来事件一次结算。每个下标只入栈/出栈一次，O(n)。""",

    84: """最直接的做法是把每根柱子当作最低高度，分别向左、向右扩展，直到遇到更矮柱；每根柱都可能重复扫描大片相同区域，最坏 O(n^2)。

对某根柱 mid 来说，真正需要的只有两个边界：左侧第一个更矮位置和右侧第一个更矮位置。一旦扫描过程中第一次遇到右侧更矮柱，mid 的最大可扩展范围就永久确定，不需要以后再查。

维护单调不降栈后，当前更矮柱就是被弹元素的 rightLess；弹栈后的新栈顶又恰好暴露出 leftLess，于是一次事件同时得到两个边界并完成面积结算。

优化类型：重复扩边界 -> 单调栈保存未封口高度，在边界事件到来时一次结算。""",
}
