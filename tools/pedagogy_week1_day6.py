"""High-touch pedagogy overrides for Week 1 Day 6."""

PEDAGOGY_WEEK1_DAY6 = {
    252: {
        "visual": """先不要急着比较所有会议对。把会议按开始时间排成一条时间线：

原来： [5,8)  [0,4)  [3,6)
排序： [0,4)  [3,6)  [5,8)
           ^      ^

排序之后，最先可能发生的冲突一定出现在相邻会议之间。

为什么？看当前会议 [3,6)：它前面的会议开始得都更早。如果紧挨着它的前一场都已经在 3 之前结束，那么更早的会议也不可能越过它；反过来，只要 previous.end > current.start，相邻两场已经直接冲突。

因此排序做了一件很重要的事：把“任意两场会议是否冲突”的全局问题，压缩成“相邻两场是否冲突”的局部检查。""",
        "core": "先按开始时间排序，让所有潜在冲突沿时间线排好；随后只检查相邻会议是否满足 previous.end <= current.start。",
        "formula": """排序后：
start[0] <= start[1] <= ... <= start[n-1]

半开区间 [start,end) 下，相邻会议 i-1 与 i 不冲突当且仅当：
end[i-1] <= start[i]

只要存在：
start[i] < end[i-1]
就可以立即判定有冲突。""",
        "steps": [
            "按会议开始时间排序；vector 的字典序排序同时把结束时间作为次关键字，但这里不影响结论",
            "从第二场会议开始，只比较当前 start 与前一场 end",
            "若 current.start < previous.end，立即返回 false",
            "全部相邻对都通过，则返回 true",
        ],
        "proof": "若所有相邻会议都满足 end[i-1] <= start[i]，那么第 i 场开始时第 i-1 场已经结束；由于更早会议又依次不晚于它们后面的会议结束，整条时间线不存在交叠。若存在任何冲突，把参与冲突且开始时间更晚的那场记为 i；沿排序后的前驱向前看，至少会出现一个尚未在 start[i] 前结束的相邻边界，因此冲突不会被漏掉。",
        "pitfalls": "本仓库把会议按半开区间 [start,end) 理解，所以一场在 10 结束、下一场在 10 开始不冲突，判断必须是 current.start < previous.end，而不是 <=。空数组或只有一场会议自然返回 true；排序会修改输入顺序。",
        "transfer": "这是 Day 6 的区间母模型：排序把全局关系变成局部关系。LC-56 不再只问“有没有冲突”，而是遇到重叠时把相邻区间合成一个更大的当前区间；LC-57 则进一步利用输入本来就有序，连排序步骤都可以省掉。",
        "memory": "先排时间线，再看相邻边界；start < previous.end 就撞车。",
    },

    56: {
        "visual": """把排序后的区间想成从左向右扫一条数轴，并维护一个正在生长的“当前并集”。

intervals = [[1,3],[2,6],[8,10],[9,12]]

先拿 [1,3]：
[1-----3]

看到 [2,6]，2 <= 3，碰到了当前并集：
[1-----3]
   [2---------6]
=> [1-----------6]

再看到 [8,10]，8 > 6，中间出现真正的空隙：
[1-----------6]   [8----10]
                  ^
此时 [1,6] 可以“封口”了；因为后面的区间起点只会更大，不可能再回来碰到它。

所以扫描过程中只有两种事件：
1. start <= currentEnd：继续把当前并集向右撑大；
2. start > currentEnd：旧并集封口，开启新的一段。""",
        "core": "排序后只维护最后一个已合并区间；新区间能碰到它就扩右端，碰不到就说明出现永久空隙，开启新的合并段。",
        "formula": """answer.back() = [L,R]，当前区间 = [s,e]。

若 s <= R：两区间重叠或端点相接
  [L,R] U [s,e] = [L, max(R,e)]

若 s > R：出现空隙
  [L,R] 已经最终确定
  新段从 [s,e] 开始

关键不变量：
answer 中除最后一段外都已经封口；最后一段是所有已扫描且仍连通区间的并集。""",
        "steps": [
            "按区间起点排序，使后续 start 单调不减",
            "answer 为空时，把当前区间直接作为第一段",
            "若 interval.start > answer.back().end，说明存在空隙，把当前区间作为新段加入 answer",
            "否则两段相交，把 answer.back().end 更新为 max(oldEnd, interval.end)",
            "扫描结束后，answer 中每一段都是一个已经封口的连通并集",
        ],
        "proof": "排序后，一旦某个新区间的 start 已经大于当前合并段 R，之后所有区间的 start 只会更大，所以没有任何未来区间能跨过这个空隙重新连接旧段，旧段可以安全封口；若 s <= R，则新区间与当前并集共享至少一个点，取 max(R,e) 恰好得到两者并集。每个原区间只被归入当前连通块一次，因此既不漏覆盖，也不会重复输出。",
        "pitfalls": "题目区间是闭区间，端点相等也算重叠，所以只有 start > lastEnd 才能开新段；不能写成 >=。排序会修改 intervals。不要只和原数组的前一个区间比较后直接输出，因为前面若已经合并，真正应该比较的是不断扩张后的 answer.back()。",
        "transfer": "直接复用 LC-252 的“排序让关系局部化”：LC-252 遇到重叠就失败，LC-56 遇到重叠则把状态 [L,R] 扩大。下一题 LC-57 输入已经排序且互不重叠，本质上是在这条扫描线上只对 newInterval 附近做一次局部 LC-56。",
        "memory": "能接上就扩右端；出现空隙就封口开新段。",
    },

    57: {
        "visual": """这题可以看成 LC-56 的“已整理时间线 + 插入一个新区间”。

intervals:   [1,2]   [3,5]   [6,7]   [8,10]   [12,16]
new:                    [4-----------8]

因为原区间已经有序且互不重叠，整条时间线天然分成三段：

左段：end < new.start
[1,2]
完全在新区间左边，原样复制。

中段：start <= new.end
        [3,5] [6,7] [8,10]
它们会被新区间连续吸收：
new = [min(start), max(end)]
最终长成 [3,10]。

右段：start > new.end
                              [12,16]
完全在合并结果右边，原样复制。

关键是 newInterval 在中段不是固定的，它像雪球一样边走边扩大；扩大后的 new.end 可能继续吸收后面的区间。""",
        "core": "利用原数组已经有序且互不重叠，把扫描分成左边直接复制、中间让 newInterval 连续吸收、右边直接复制三段。",
        "formula": """当前新区间 new = [L,R]，原区间 cur = [s,e]。

左段：e < L
  cur 完全在 new 左边 -> 直接输出

中段：s <= R
  cur 与 new 相交 ->
  L = min(L,s)
  R = max(R,e)

右段：s > R
  后续也都在 new 右边 -> 先输出 new，再原样复制剩余区间

闭区间相交条件：
不是 (e < L 或 s > R)。""",
        "steps": [
            "从头复制所有 intervals[i].end < newInterval.start 的左段",
            "随后只要 intervals[i].start <= newInterval.end，就把该区间吸收到 newInterval：同时更新左右端点",
            "中段结束时，把已经长成最终大小的 newInterval 放入 answer",
            "把剩余所有区间原样复制到 answer，形成右段",
        ],
        "proof": "原 intervals 已按起点排序且彼此不重叠，因此完全位于 new 左侧的区间必然连续出现在最前面；进入第一个可能相交区间后，只要 start <= 当前不断扩大的 new.end，就属于同一个连通块，合并不会漏掉被扩张右端新触及的区间；一旦某区间 start > new.end，后续 start 更大，全部永久位于右侧。三段互斥且覆盖全部输入，所以不会漏也不会重。",
        "pitfalls": "这是闭区间，end == new.start 或 start == new.end 都算重叠；中段判断必须使用不断更新后的 newInterval.end，而不是最初的 end。当前实现会直接修改传入的 newInterval；LeetCode 允许这样做，但如果调用方需要保留原值，应改用局部副本。intervals 为空时仍要输出 newInterval。",
        "transfer": "知识迁移顺序是 LC-252 -> LC-56 -> LC-57：先学会排序后检测局部关系，再维护一个会增长的合并段，最后利用“输入已经排好”把全量 merge 特化成一次插入附近的局部 merge。这个三阶段扫描也常见于向有序事件流、版本区间或内存区段插入新范围。",
        "memory": "左边复制 -> 中间滚雪球吸收 -> 放下雪球 -> 右边复制。",
    },

    179: {
        "visual": """这题不是问“哪个数字更大”，而是问“谁放前面能让最终拼接串更大”。

例如 3 和 30：

3 在前：  \"3\" + \"30\" = \"330\"
30 在前： \"30\" + \"3\" = \"303\"

330 > 303，所以排序时必须让 3 排在 30 前面。

再看 9 和 34：
9 在前：  \"934\"
34 在前： \"349\"
=> 9 在前。

因此比较两个元素 a、b 时，真正参与比较的不是 a 和 b 本身，而是两个可能的局部拼接：

a+b  VS  b+a

哪一种更大，就采用哪一种相对顺序。排序完成后，所有相邻对都处于“更优拼接顺序”，顺次连接就是全局最大结果。""",
        "core": "从最终拼接目标反推排序规则：对任意 a、b，若 a+b > b+a，就让 a 排在 b 前面。",
        "formula": """定义排序关系：
a ≺ b  当且仅当  a+b > b+a

这里 a、b 都是十进制字符串。

例如：
a=\"3\", b=\"30\"
\"330\" > \"303\"
=> 3 应排在 30 前。

排序后结果：
answer = part[0] + part[1] + ... + part[n-1]

若 part[0] == \"0\"，因为 0 已经是排序后的最大首项，所以全部元素都只能是 0，答案规范化为 \"0\"。""",
        "steps": [
            "把每个整数转成字符串，避免最终结果溢出整数范围",
            "排序时对两个字符串 a、b 比较 a+b 与 b+a，而不是比较数值大小或普通字典序",
            "若排序后的第一个字符串是 0，直接返回单个字符串 0",
            "否则按排序后的顺序依次拼接所有字符串",
        ],
        "proof": "考虑最终序列中任意相邻的 a、b。其他前缀和后缀保持不变时，这两个元素只有 ab 与 ba 两种局部排列；若 ab < ba，却仍让 a 在 b 前，那么交换它们会让整个结果字典序严格变大，因此原排列不可能最优。按 ab > ba 排序消除了所有这种可改进的相邻逆序；该比较关系具有排序所需的一致性，所以最终序列达到最大拼接结果。",
        "pitfalls": "不能直接按整数降序，也不能按字符串字典序降序，3 与 30 就是反例。比较器必须使用严格 >，相等时返回 false，满足 strict weak ordering。结果可能远超整数类型。全零输入若直接拼接会得到 000，应规范成 0。",
        "transfer": "这题训练的是“目标函数决定比较器”，而不是记住 a+b>b+a。若目标改成最小拼接数，只需把比较方向反过来；类似思想也出现在按字符串组合效果、自定义任务优先级排序的问题中。与前三题共同点是：都先通过排序重新组织问题，但前三题利用的是坐标单调性，这题利用的是交换后对目标值的直接影响。",
        "memory": "别问谁本身大；问 ab 和 ba 哪个大。",
    },
}
