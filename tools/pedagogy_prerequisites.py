"""Prerequisite concept definitions for high-touch learning explanations.

These blocks implement the AGENTS.md rule that a term whose exact meaning is a
premise of the algorithm should be defined before optimization derivation and
before the main visual explanation.  Keep entries focused on concepts that
change comparisons, invariants, counting units, or uniqueness semantics.
"""

PREREQUISITE_OVERRIDES = {
    3: """子串（substring）必须由原字符串中的一段连续字符组成；不能跳过中间字符。

例如 s=\"abcde\"：
- \"bcd\" 是子串；
- \"ace\" 不是子串，它只能算子序列（subsequence），因为中间跳过了字符。

因此本题维护的窗口 s[left..right] 必须始终对应原字符串中的一段连续区间。""",

    5: """子串（substring）是原字符串中的连续片段，不允许跳过字符。

例如 s=\"abcba\"：\"bcb\" 是子串；\"aba\" 若依靠跳过中间字符才能取得，就不是该位置上的子串。

所以“最长回文子串”要求回文字符在原串中连续出现，这也是中心扩展可以用左右指针连续向外走的前提。""",

    31: """字典序（lexicographical order）像查字典一样从左到右比较两个序列：
1. 找到第一个不同位置，谁在这个位置上的值更大，谁的字典序更大；
2. 如果公共部分全部相同，也就是一个序列是另一个的完整前缀，则较短序列更小。

例如：
[1,3] > [1,2,3]，因为第二位首次不同，3>2；
[1,2] < [1,2,3]，因为前两位完全相同，而较短序列先结束。

LC-31 的所有排列长度相同，因此实际求解时只会用到“第一个不同位置决定大小”这一部分。所谓 next permutation，就是所有排列按这种顺序排列后紧挨当前排列之后的那个。""",

    128: """这里的“连续序列”指的是数值连续，而不是数组位置连续：相邻数值必须相差 1，但它们在原数组中可以分散在任意位置。

例如 nums=[100,4,200,1,3,2] 中，1、2、3、4 在原数组里并不连续，却构成长度 4 的连续序列。

这与 subarray 不同：subarray 必须占据原数组中的连续下标。正因为本题不关心原下标，才可以把所有值丢进哈希集合后只研究数值邻接关系。""",

    179: """C++ 字符串的 `>` 按字典序比较。对于长度相同、且都只含十进制数字的字符串，字典序大小与对应整数大小一致。

本题比较的 `a+b` 与 `b+a` 总长度一定相同，因此可以直接用字符串字典序决定哪个拼接结果数值更大。

例如 a=\"3\", b=\"30\"：
\"330\" > \"303\"，所以 3 应排在 30 前。""",

    252: """半开区间 [start,end) 包含 start，但不包含 end。

例如 [1,3) 表示从 1 开始、到 3 之前结束；[1,3) 与 [3,5) 不重叠，因为前一段在 3 这个时刻已经结束。

因此会议恰好在上一场结束时开始是允许的：
previous.end == current.start 仍然不冲突。
这直接决定代码使用 `current.start < previous.end` 判断冲突，而不是 `<=`。""",

    56: """本题区间是闭区间 [L,R]：左右端点都属于区间。

因此 [1,3] 与 [3,5] 共享端点 3，必须视为重叠并合并为 [1,5]。

这和 LC-252 的半开会议区间不同。闭区间下只有 `next.start > current.end` 才出现真正空隙；若 `next.start == current.end`，两段仍然相交。

还要排除一个很自然但错误的简化：排序后只把当前区间和“原数组的前一个区间”比较。
例如 [[1,10],[2,3],[4,5]]：前两段先合成 [1,10]。处理 [4,5] 时，如果只看原前驱 [2,3]，会因为 4>3 错误地认为出现空隙；但 [4,5] 明明仍被已经扩张到 10 的当前并集覆盖。
因此扫描时真正有意义的前驱不是原数组的 previous，而是不断扩张后的 `answer.back()` / 当前合并段。""",

    57: """本题沿用 LC-56 的闭区间语义：[L,R] 同时包含 L 和 R。

例如 [1,3] 与 [3,5] 在点 3 相交，所以必须合并；只有一段的 end 严格小于另一段的 start，才说明两段完全分离。

因此三阶段扫描中的边界分别是：
左侧：end < new.start
重叠：start <= new.end
右侧：start > new.end。""",

    283: """这里的“稳定（stable）”指：被保留下来的元素必须保持它们在原数组中的相对顺序。

例如 [1,0,3,2,0] 移零后，非零部分必须仍是 [1,3,2]；[3,1,2] 虽然也把 0 放到了末尾，但破坏了稳定性，因此不合法。

所以 write 指针不能随意从后面拿元素填洞；它只能按 read 从左到右发现非零元素的顺序依次写入。""",

    438: """异位词（anagram）要求两个字符串包含完全相同的字符及其出现次数，字符顺序可以不同。

例如 \"abc\" 与 \"cba\" 是异位词；\"abb\" 不是 \"abc\" 的异位词，因为字符频次不同。

因此一个窗口要成为 p 的异位词，必须同时满足：窗口长度等于 |p|，并且每个字符的频次与 p 完全一致。""",

    104: """“深度/高度”的计数约定在不同资料中可能按节点数，也可能按边数，所以必须先固定本题口径。

LeetCode LC-104 把最大深度定义为：从根到最远叶子这条路径上的节点数。
空树深度 = 0；只有根节点的树深度 = 1。

因此当前递归状态 D(node) 也按节点数计：`D(node)=1+max(D(left),D(right))`。""",

    543: """直径（diameter）是树中任意两个节点之间最长简单路径的长度；LC-543 明确按“边数”计，不按节点数计。

例如路径 4 -> 2 -> 1 -> 3 一共有 4 个节点，但长度是 3 条边。

这里复用 LC-104 的 child height：leftHeight 表示从左孩子向下的节点数。站在当前 node 看，这恰好等于“从 node 经过左孩子向下能走的边数”；右侧同理，所以经过当前节点的直径候选直接是 `leftHeight + rightHeight`，不要再 +1。""",

    98: """BST 的约束是“整棵子树”的全局约束，不是只要求每个父节点和它的两个直接孩子局部有序。

最容易产生的错误简化是递归检查：
`node->left->val < node->val < node->right->val`。
看下面这棵树：

        5
       / \\
      1   7
         / \\
        4   8

如果只检查直接父子：
1 < 5 < 7   -> 通过
4 < 7 < 8   -> 通过
看起来每个局部节点都合法。

但节点 4 位于根 5 的整个右子树中，所以它还必须满足 4 > 5；这一条祖先约束被局部父子比较完全漏掉了。
因此 BST 判断必须让深层节点继续携带祖先留下的约束。上下界递归把这些约束压成 `(lower, upper)`；中序遍历严格递增则是另一种等价的全局验证方式。""",

    46: """排列（permutation）是把给定元素全部排成一个有顺序的序列；顺序不同就是不同排列。

例如对 [1,2]：
[1,2] 与 [2,1] 是两个不同排列。

本题 nums 中元素互异，并且每个排列必须使用每个元素恰好一次。因此搜索树的第 d 层，就是在决定排列的第 d 个位置放哪个“尚未使用”的元素。""",

    78: """子集（subset）只关心“选了哪些元素”，不关心这些元素的排列顺序。

例如 {1,2} 与 {2,1} 是同一个子集，不应输出两次。空集 {} 和全集 nums 本身也都是合法子集。

这正是 `start` 存在的原因：人为规定只按原下标递增选择，为每个子集保留唯一构造顺序。""",

    39: """组合（combination）与排列的关键区别是：组合不关心选择顺序。

例如 target=7 时，[2,2,3]、[2,3,2]、[3,2,2] 表示同一个组合，只能输出一次。

本题还额外允许同一个 candidate 被无限次重复使用，所以“顺序不重要”与“元素可重复使用”是两个独立规则。`start` 负责消除不同顺序的重复表示，而递归继续传 i（不是 i+1）负责允许再次选择当前值。""",

    200: """连通（connected）表示两个陆地格之间存在一条路径，路径每一步都只能走到上下左右相邻的陆地格；对角线不算连接。

连通分量（connected component）是一个“最大”的互相可达节点集合：集合里的任意格都能沿合法边走到彼此，而且不能再加入集合外的新陆地而仍保持连通。

因此一座岛恰好对应一个四邻接连通分量；从其中任意一个格启动 BFS，都应该把整座岛一次访问完。""",

    133: """深拷贝（deep copy）要求复制后的图拥有全新的节点对象，同时保持原图的节点值和邻接关系；克隆节点不能与原图共享同一个 Node 对象。

“对象身份”也必须保持一一对应：若原图中的同一个节点 B 被 A 和 C 同时引用，克隆图里 A' 和 C' 也必须都指向同一个 B'，而不能各自 new 出 B1'、B2'。

因此本题必须维护 original Node* -> clone Node* 的唯一映射。""",

    236: """LC-236 里的 p、q 是两个确定的节点对象（`TreeNode*`），不是两个“节点值”。题目本身还保证所有 Node.val 互不相同，但算法语义仍然应该按节点身份理解。

两个确定节点可以有多个公共祖先。例如：

        3
       / \\
      5   1
     / \\
    6   2
       / \\
      7   4

若 p 是节点 6，q 是节点 4：
p 到根的路径：6 -> 5 -> 3
q 到根的路径：4 -> 2 -> 5 -> 3

它们的公共祖先有 5 和 3，并不只有一个；但最近公共祖先（Lowest Common Ancestor）只有 5，因为它是这些公共祖先中深度最大的那个。

为什么在“树”里 LCA 一定唯一？因为每个节点到根只有唯一一条路径，两条祖先路径的公共部分一定形成一条连续祖先链；这条链中最靠下、深度最大的节点只有一个。

这也解释了代码为什么比较 `root == p` / `root == q`，而不是只比较 `root->val`。即使把题目推广到允许重复 val，只要 p、q 仍指向两个确定节点，对象身份仍然没有歧义；用 val 比较反而可能把另一个同值节点误认为目标。

记住：公共祖先可以有多个；Lowest Common Ancestor 在树中唯一。""",

    207: """对有向图中的边 u -> v：
- v 的入度（indegree）是指向 v 的边数量；
- 拓扑序（topological order）是一种线性排列，使每条边 u -> v 中的 u 都出现在 v 之前。

例如 2 -> 1 -> 0 的一个拓扑序就是 [2,1,0]。

只有无环有向图（DAG）才存在拓扑序。课程表里边表示“先修 -> 后修”，所以入度 0 的课程表示当前已经没有尚未完成的前置依赖，可以作为下一门课。

不要把“检查环”简化成只寻找一对互相依赖的课程。三门课就能形成更长的环：
0 -> 1 -> 2 -> 0。
这里不存在任何一对同时具有 a->b 和 b->a，但整体仍然没有合法拓扑序。
所以必须验证整个依赖图是否能被拓扑消除完，而不是只做局部成对检查。""",

    139: """字符串分词是一个“整条路径是否能走到结尾”的全局可达性问题，不能仅凭当前最漂亮的局部匹配做贪心决定。

反例：s=\"cars\"，wordDict={\"car\",\"ca\",\"rs\"}。
如果每一步都贪心选择当前最长前缀，首先会拿 \"car\"，剩下 \"s\"，于是失败；但真正存在合法切分：\"ca\" + \"rs\"。

因此“当前最长词”“当前最短词”都没有天然的安全贪心性质。DP / memo 要保留的是切分位置的可达性，让不同前缀选择可以在同一个位置重新汇合。""",

    45: """“每一步跳到当前能到达的最远下标”看起来很贪心，但它不是 LC-45 的正确贪心。

反例：nums=[2,3,1,1,4]。
从下标 0 直接跳到最远的下标 2，之后只能 2->3->4，共 3 跳；
但先跳到下标 1，再由 1 直接到 4，只需要 2 跳。

正确做法不是在当前层里提前选择某一个落点，而是把当前一次跳跃能覆盖的整个区间当作 BFS 一层：扫描这一层所有位置，统一计算下一层最远能扩到哪里，只有走到层边界时才把 jumps 加一。""",

    253: """LC-253 求的是任意时刻的最大并发会议数，和 LC-252 的“是否存在任意冲突”不是同一个局部问题。

反例（半开区间）：[0,10)、[1,2)、[3,4)。
按开始时间排序后，后两场 [1,2) 与 [3,4) 相邻且不重叠；但长会议 [0,10) 在两场期间都仍然活跃，所以系统仍需要 2 个会议室。

因此不能像 LC-252 那样只比较当前会议和排序后的直接前驱。必须维护所有“尚未结束”的会议；小根堆保存这些活动会议的结束时间，堆大小才对应当前并发数。""",

    875: """不能把最低速度简化成 `ceil(sum(piles)/h)` 这样的平均吞吐，因为 Koko 每小时只能选择一堆；一堆提前吃完后，本小时剩余能力不能转移到另一堆。

反例：piles=[1,1,100], h=4。
总香蕉数 102，平均下界是 ceil(102/4)=26；但速度 26 需要的时间是 1+1+ceil(100/26)=6 小时，明显不够。
真正的最小速度是 50：1+1+ceil(100/50)=4。

所以可行性必须逐堆计算 `ceil(pile/k)` 再求和；总量平均只能给一个很松的下界，不能直接作为答案。""",

    621: """任务调度器里不能只记“一个最高频任务”的频次，还要记有多少种任务并列最高频。

反例：tasks=AAABBB, n=2。
最高频 f=3。如果错误地只按一种最高频任务写骨架 `(f-1)*(n+1)+1`，会得到 7。
但 A 和 B 都出现 3 次，最后一列需要同时容纳 A、B，合法最短安排例如：
A B idle A B idle A B
长度是 8。

因此骨架尾部不是固定 `+1`，而是 `+maxCount`：
`(f-1)*(n+1)+maxCount`。""",

    685: """有根有向树可以先用结构条件定义：
1. 恰好一个根节点，根的入度为 0；
2. 其它每个节点入度都恰好为 1，也就是只有一个父节点；
3. 从根可以沿有向边到达所有节点；
4. 不存在有向环。

边 parent -> child 表示父子方向。原题保证输入是在一棵这样的树上额外加入一条边，因此额外边造成的核心异常只会集中在“某节点出现两个父亲”或“形成环”这两类结构破坏上。""",
}
