"""High-touch pedagogy overrides for Week 2 / Day 14.

Day 14 is a consolidation day.  It deliberately reuses previously learned
models instead of introducing four unrelated templates:
- LC-124 upgrades LC-543's tree-DP "close two arms / return one arm" model;
- LC-143 composes midpoint + LC-206 reverse + merge, echoing LC-234;
- LC-148 reuses LC-21 merge inside bottom-up merge sort;
- LC-199 reuses LC-102's BFS layer-boundary invariant.
"""

PEDAGOGY_WEEK2_DAY14 = {
    124: {
        "pattern": "树形 DP：当前节点闭合双臂，向父只返回单臂最大贡献",
        "model": "直接复用 Day 11 / LC-543 的双角色递归：子树向父只汇报一条可继续延伸的单臂；当前节点却可以同时接上左右两条正贡献，形成一条在这里闭合的完整路径。LC-124 只是把 LC-543 的“高度”换成“路径和”，再增加负贡献应被丢弃这一层判断。",
        "derivation": """一个直接但代价较高的思路，是把每个节点都当成路径最高点，再分别重新计算它左、右子树能提供的最大向下路径和。这样当然能覆盖答案，但祖先和子节点会反复计算同一棵子树的最佳向下贡献，最坏可到 O(n^2)。

Day 11 的 LC-543 已经告诉我们怎样消掉这类重复子问题：后序遍历时让每棵子树只计算一次“向父还能延伸多少”，并把结果直接上报。当前节点收到左右两个上报值后，顺手计算一次“经过我闭合的完整候选”。

本题比 LC-543 多出的关键只有负数：如果某侧向下贡献 < 0，把它接进路径只会让总和更差，因此对当前节点而言，这一侧等价于贡献 0，可以永久丢弃。

优化类型：重复子树计算 -> 后序状态复用；负贡献被支配 -> 截断为 0。最后才把这套机制称为树形 DP。""",
        "visual": """看经典局部结构：

          20
         /  \\
       15    7

左右子树向 20 汇报：
leftGain  = 15
rightGain = 7

站在 20 自己这里，可以把两边都接起来：
15 -> 20 -> 7
candidate = 15 + 20 + 7 = 42

但 20 若继续向父节点汇报，就不能把 15 和 7 两边同时带上去；否则路径会在 20 分叉。
所以只能返回：
20 + max(15, 7) = 35

再看负贡献：

          5
         /
       -8

把 -8 接进路径只会让 5 变差，所以对 5 来说：
leftGain = max(0, -8) = 0

这不是“忽略一个节点”，而是说：当前完整路径可以选择不进入那条负收益分支。""",
        "core": "每棵子树只向父节点汇报一条最大单臂贡献；当前节点把左右正贡献都接上更新全局答案，再只选较好的一侧继续向上。",
        "formula": """令 gain(node) 表示：
必须从 node 出发，并且向下最多选择一侧时，能提供给父节点的最大路径和。

left  = max(0, gain(node->left))
right = max(0, gain(node->right))

以当前节点为最高点的完整路径候选：
candidate = node->val + left + right
best = max(best, candidate)

向父节点返回：
gain(node) = node->val + max(left, right)

两个角色必须分开：
- 更新全局答案时，左右两臂可以同时使用；
- 向父返回时，只能选一臂，否则路径会分叉。

best 必须初始化为 -∞（实现用 INT_MIN），不能初始化为 0，因为整棵树可能全是负数。""",
        "steps": [
            "后序进入 gain(node)：空节点返回 0，让父节点可以自然把不存在的分支视为无贡献",
            "递归得到左右子树的单臂贡献，并分别与 0 取最大值；负贡献不值得接入当前路径",
            "用 node->val + left + right 更新全局 best，表示把当前节点作为最高汇合点时的最佳完整路径",
            "向父节点返回 node->val + max(left,right)，只允许一侧继续延伸，保持结果仍是一条简单路径",
            "根节点递归结束后返回全局 best；它可能来自任意子树，并不要求经过 root",
        ],
        "proof": "任意一条非空简单路径都有唯一的最高汇合节点 x。若路径经过 x 的左右两侧，那么在 x 看来，它的两部分分别不会优于 left/right 所代表的最佳向下贡献；若某侧贡献为负，舍弃它只会更优。因此处理 x 时的 candidate 一定覆盖以 x 为最高点的最佳路径。遍历所有节点，就覆盖了全局最优路径。向父只返回一侧，是因为父节点若继续接这条路径，经过当前节点后只能进入一个孩子，否则会形成分叉而不再是简单路径。",
        "pitfalls": "最常见错误是把 best 初始化为 0，这会让单节点负数树错误返回 0；题目要求路径至少包含一个节点，所以必须让真实负节点有机会成为答案。另一个错误是向父返回 node+left+right——那是当前节点的完整候选，不是可继续延伸的单臂。`max(0, gain(child))` 的含义是“可以不走这条负分支”，不是说负节点不存在。",
        "transfer": "这是 LC-543 的几乎一比一升级：LC-543 的 child state 是高度，LC-124 的 child state 是最大向下路径和；LC-543 不需要处理负高度，LC-124 需要把负贡献截断为 0。以后遇到树上最长/最大路径问题，优先问两个问题：当前节点能不能把左右两侧闭合？向父又允许带几条分支？",
        "memory": "当前节点可以双臂闭合；向父只能单臂上报；负贡献不接。",
    },

    143: {
        "pattern": "链表模型组合：找中点 + 反转后半段 + 拉链式交替合并",
        "model": "目标顺序 L0,Ln,L1,Ln-1... 的困难点只有一个：单链表能顺着 next 取头部，却不能从尾部向前取。解决办法不是发明新的尾指针结构，而是复用 LC-234 的同一组合：快慢指针切成两半，把后半段用 LC-206 原地反转，于是原来的尾、次尾就变成第二条链的头、次头；最后像拉拉链一样交替连接两条链。",
        "derivation": """最自然的正确做法，是先把所有 ListNode* 放进 vector。这样既能 O(1) 访问头部，也能 O(1) 访问尾部，再用左右下标按 L0,Ln,L1,Ln-1... 重新连 next。时间 O(n)，但需要 O(n) 额外空间。

若要求 O(1) 额外空间，真正要消掉的是“为了从尾往前读而保存整张节点索引表”。单链表本身只能向后走，所以把问题变形：不要让指针去尾部倒着读，而是把后半段整个反过来，让尾部顺序主动变成可向前读取。

于是已有工具可以直接拼起来：
1. 快慢指针找到中点并断链；
2. 复用 LC-206 反转第二段；
3. 两条链都能沿 next 正向读取，再做交替合并。

优化类型：完整节点索引 -> 结构变换让“倒序访问”变成“顺序访问”，空间从 O(n) 降为 O(1)。""",
        "visual": """例：
1 -> 2 -> 3 -> 4 -> 5

目标：
1 -> 5 -> 2 -> 4 -> 3

第一步切开：
first : 1 -> 2 -> 3 -> null
second: 4 -> 5 -> null

第二步反转 second：
first : 1 -> 2 -> 3
second: 5 -> 4

现在目标顺序已经变成“两条正向链交替取节点”：

first :  1 ----> 2 ----> 3
          \\      \
second:   5 ----> 4

交织：
1 -> 5 -> 2 -> 4 -> 3

关键不是记三段代码，而是看清：
“尾部倒着取”被反转操作变成了“第二条链从头顺着取”。""",
        "core": "先把后半段反转，让原链尾部变成一条可顺向读取的链；然后两条链各取一个节点交替连接，就自然得到头、尾、次头、次尾的顺序。",
        "formula": """切分后保持：
first  = L0 -> L1 -> ...
second = ... -> Ln-1 -> Ln

反转 second 后：
second = Ln -> Ln-1 -> ...

合并阶段每轮前的不变量：
- first 指向下一枚“前半段节点”；
- second 指向下一枚“原尾部节点”；
- 已连接前缀已经严格满足 L0,Ln,L1,Ln-1... 的目标顺序。

每轮执行：
nextFirst  = first->next
nextSecond = second->next
first->next = second
second->next = nextFirst
然后两指针推进到保存好的 next。""",
        "steps": [
            "用 slow=head、fast=head->next 的快慢指针定位前半段尾节点 slow，使奇数长度时前半段自然多一个节点",
            "令 second=slow->next，并立即执行 slow->next=nullptr 把两段断开；这是后续安全交织的结构边界",
            "完全复用 LC-206：逐节点保存 next、反转 next，得到从原尾部开始的 reversed 第二段",
            "first=head、second=reversed；每轮先保存两边 next，再连接 first->second->nextFirst，最后推进到下一对节点",
            "当 second 用完时停止；前半段长度不少于后半段，剩余的单个中间节点已经自然位于结果末尾",
        ],
        "proof": "切分后，第一段按 L0,L1,L2... 排列；第二段反转后按 Ln,Ln-1,Ln-2... 排列。交替取两段头节点，因此第 k 轮恰好追加一对 Lk 与 Ln-k，直接生成题目要求的顺序。两段先断开，且每个节点只从自己的链中被取一次，所以不会重复节点，也不会形成旧连接残留造成的环。",
        "pitfalls": "必须先断链 `slow->next=nullptr`，否则后半段反转和交织时旧连接仍存在，容易形成环。合并时必须先保存 nextFirst/nextSecond，再改 next，否则会丢失尚未处理的后缀。这里 fast 从 head->next 出发，是为了让偶数长度时两半等长、奇数长度时前半段多一个；不同初始化也能写对，但对应的切分边界必须一起调整。",
        "transfer": "这是 LC-234 的直接兄弟题：两题前两步都完全一样——找中点 + 反转后半段。LC-234 第三步是逐项比较，LC-143 第三步则是交替合并。它也复用了 LC-206 的反转和 LC-21 的“维护结果尾部/连接剩余链”思维。以后看到链表需要交替访问前后两端，优先想能否先把后一半反过来。",
        "memory": "拿不到尾部前驱，就把后半段翻过来；两条正向链再拉链合并。",
    },

    148: {
        "pattern": "自底向上归并排序：把 LC-21 的有序链合并扩展成长度 1、2、4... 的有序段",
        "model": "链表不适合依赖随机访问的排序，但非常适合归并：切断一段只需改一个 next，合并两个有序段又正好复用 LC-21。当前实现选择自底向上归并，不用递归栈：先把每个单节点视为长度 1 的有序段，两两合并成长度 2，再合成 4、8...，直到一整条链有序。",
        "derivation": """最容易想到的办法，是把所有节点值拷进 vector，调用 sort，再写回链表；时间 O(n log n)，但需要 O(n) 额外空间，而且没有利用链表本身“改指针即可重排节点”的特点。

若直接在链表上做插入排序，可以 O(1) 空间，但最坏 O(n^2)，达不到题目的时间目标。

要同时满足 O(n log n) 时间和常数级额外指针空间，就需要一种只做顺序访问、又能稳定把问题规模翻倍解决的排序。LC-21 已经提供核心积木：两个有序链表可以 O(a+b) 合并。

于是把每个单节点当作已排序 run：
长度 1 的相邻 run 两两 merge -> 长度 2；
长度 2 再两两 merge -> 长度 4；
不断翻倍，经过 O(log n) 轮后整链有序。

选择自底向上而非递归归并，是为了不额外消耗 O(log n) 递归栈。

优化类型：O(n) 中间数组或 O(n^2) 原地插排 -> 顺序切段 + 复用有序归并；自底向上进一步去掉递归栈。""",
        "visual": """例：4 -> 2 -> 1 -> 3

runSize = 1：每个节点天然有序
[4] [2] [1] [3]
  \\ /       \\ /
 [2,4]      [1,3]

runSize = 2：两个长度 2 的 run 已有序
[2,4] + [1,3]
       \\ /
    [1,2,3,4]

每一轮只做两件事：
1. split：从当前链中切出 left / right 两个长度至多 runSize 的段；
2. merge：复用 LC-21，把两段按升序接成一个更长的有序段。

runSize：1 -> 2 -> 4 -> 8 ...
不是魔法数字，而是在表达“不变量中的有序段长度每轮翻倍”。""",
        "core": "把单节点视作有序段，反复把相邻两个长度 runSize 的有序链归并成更长有序链；runSize 每轮翻倍，直到覆盖整条链。",
        "formula": """第 k 轮开始时：
runSize = 2^k
整条链可被看成若干个“长度至多 runSize、且内部已有序”的连续段。

每次：
left  = split(current, runSize)
right = split(left 后的剩余链, runSize)
rest  = 第二次 split 后的未处理部分

merge(left,right)
=> 得到长度至多 2*runSize 的有序段。

一轮结束后：
所有 run 的有序长度从 runSize 扩大到 2*runSize。

轮数 O(log n)，每轮所有节点总共被扫描/归并 O(n)：
总时间 O(n log n)。""",
        "steps": [
            "先线性统计链表长度 length；dummy 指向 head，统一处理每轮第一个合并段的接回操作",
            "从 runSize=1 开始，每轮令 current=dummy.next、sortedTail=&dummy；当前轮只合并固定长度的相邻 run",
            "对 current 调 split 得到 left/right/rest 三段：split 走到段尾后必须把 `head->next=nullptr`，真正断开边界",
            "调用 mergeRuns(left,right)；其核心就是 LC-21 的双表头比较，并同时返回 mergedHead 与 mergedTail",
            "用 sortedTail->next 接回 mergedHead，再令 sortedTail=mergedTail；current 指向 rest，继续处理本轮下一对 run",
            "一整轮结束后 runSize*=2；当 runSize>=length 时，整条链已经成为单个有序 run",
        ],
        "proof": "归纳证明阶段不变量：runSize=1 时每个单节点显然有序。假设某轮开始时每个长度至多 runSize 的连续段内部有序，那么 mergeRuns 对相邻两段执行标准有序归并，得到的长度至多 2*runSize 且仍有序；整轮覆盖全部节点后，下轮不变量成立。runSize 最终覆盖 length 时，只剩一个有序段，即整条链有序。每轮节点只参与常数次顺序扫描，且轮数为 O(log n)。",
        "pitfalls": "`split` 的关键不是“找到第二段头”，而是必须同时把第一段尾的 next 置空；不真正断链，merge 会跨过 run 边界。`mergeRuns` 返回尾节点是为了 O(1) 接上下一段；若每次接回后重新从 mergedHead 扫到尾，会引入额外重复工作。题头写 O(1) 空间时，自顶向下递归归并严格说会占 O(log n) 调用栈，因此当前实现选择自底向上。",
        "transfer": "核心积木就是 Day 8 / LC-21：以前只合并两条完整有序链，现在把同一操作嵌进多轮 run 合并。这个“先把小块视为已解，再两两归并成更大块”的结构也直接对应数组归并排序、外部排序和大文件多路归并。",
        "memory": "LC-21 是积木；runSize=1,2,4... 每轮把两个已排序小段拼成一个更大的已排序段。",
    },

    199: {
        "pattern": "层序遍历的层内聚合：每层只取最右一个节点",
        "model": "这题几乎不需要新算法。直接复用 Day 10 / LC-102 的 BFS 层边界：一轮开始时 queue 恰好保存当前层全部节点，先冻结 width，再处理这 width 个节点。因为当前实现按左孩子、右孩子入队，所以这一层最后被处理的节点就是最右节点；LC-199 只是把 LC-102 的“收集整层”改成“只收集层末一个值”。",
        "visual": """例如：

        1
       / \\
      2   3
       \\   \\
        5   4

从右侧看到：1, 3, 4。

BFS 分层：
level 0 queue: [1]
                 ^ 最后一个也是 1

level 1 queue: [2, 3]
                    ^ 该层最后一个 = 3

level 2 queue: [5, 4]
                    ^ 该层最后一个 = 4

所以真正需要维护的不是“右侧视角的几何射线”，而是 LC-102 已经建立的层边界：

width = queue.size()

处理第 i 个节点时：
若 i == width-1
=> 它就是当前层最后一个节点
=> 记录答案。""",
        "core": "完全复用层序遍历：冻结当前层 width，按从左到右处理这一层，只在 i==width-1 时记录节点值。",
        "formula": """每轮开始的不变量：
queue 中恰好是同一 depth 的全部节点，并按从左到右排列。

width = queue.size()

for i in [0, width):
    pop 当前层第 i 个节点
    if i == width-1:
        answer.push_back(node->val)

因为孩子按 left 再 right 入队，下一轮 queue 仍保持下一层从左到右顺序。

注意 width 必须在处理这一层之前冻结；循环中 queue.size() 会因为加入孩子而变化。""",
        "steps": [
            "空树直接返回空 answer；否则根节点入队，建立第 0 层 frontier",
            "每轮开始先保存 width=pending.size()，把它冻结为当前层节点数量",
            "循环 width 次从队首取节点；只有 i==width-1 时把 node->val 加入答案",
            "仍按 left、right 顺序把非空孩子加入队尾，让下一轮队列保持从左到右的层序顺序",
            "当前层恰好处理 width 个节点后进入下一轮，直到队列为空",
        ],
        "proof": "LC-102 已保证冻结 width 后，一轮恰好处理同一深度的全部节点；当前实现又按从左到右的顺序处理这一层，所以最后一个节点就是该深度最右节点。每层只记录一次，因此答案与树的深度一一对应，不会漏层也不会重复。",
        "pitfalls": "不要在 for 条件里动态使用 `pending.size()` 作为当前层长度，因为处理中会持续加入下一层孩子；必须先冻结 width。若改成右孩子先入队，则“最后一个”不再代表最右节点，需要相应改成记录第一个。空树要返回空数组。",
        "transfer": "它是 LC-102 的最小改造：BFS 框架、frontier、width 不变量全部不变，只改变“每层如何聚合”。同样可以得到每层最大值、平均值、最左视图、锯齿层序等；新题先问的应是“遍历结构是否已学过，真正变化的只是层内统计规则吗？”",
        "memory": "LC-102 原样复用；一层只取最后一个。",
    },
}
