"""High-touch pedagogy overrides for Week 2 / Day 10."""

PEDAGOGY_WEEK2_DAY10 = {
    144: {
        "pattern": "前序遍历：根先处理，栈只负责保存之后要走的分支",
        "visual": """先把递归的动作画出来，而不是先背“右压栈、左压栈”：

        1
       / \\
      2   3
     / \\
    4   5

前序 = 根 -> 左 -> 右
所以从 1 出发时：
1 已经立刻输出；接下来要先走 2，但 3 不能丢。

显式栈就是“以后再回来走的分支”：
输出 1 后，栈里要让 2 位于顶部。
stack(top -> bottom): 2, 3

由于栈后进先出，代码必须先 push(3)，再 push(2)。""",
        "core": "前序遍历的本质是“节点一到手就处理”；栈保存尚未进入的子树，并用逆序压栈保证下一次先进入左子树。",
        "formula": """递归前序的时间顺序：
visit(root)
preorder(root->left)
preorder(root->right)

迭代版维护不变量：
栈顶 = 按前序顺序下一个应该进入的节点。

因此弹出 node 后：
1. visit(node)
2. push(node->right)
3. push(node->left)

右先入栈只是因为它应该更晚被处理。""",
        "model": "把栈看成“未来待进入的子树清单”，而不是把前序遍历记成一套固定模板。",
        "steps": [
            "空树直接返回；否则把 root 作为第一个待进入节点压栈",
            "每次弹出栈顶 node，因为它就是当前前序中的下一个节点，所以立即记录 node->val",
            "若右孩子存在先压右孩子，若左孩子存在再压左孩子，使左孩子位于栈顶",
            "重复直到栈为空，此时所有节点都已经恰好进入一次",
        ],
        "memory": "前序是“拿到节点就处理”；想先走谁，就让谁最后入栈。",
        "proof": "每个非空节点只会由它唯一的父节点压栈一次（根单独压一次），因此不会重复。弹出一个节点时，右子树虽先入栈，却被左子树压在下面；左子树内部也遵守同样规则，所以整棵左子树一定先于右子树完成。于是根、左、右的相对次序在每棵子树上都成立，也不会漏掉任何非空孩子。",
        "pitfalls": "最常见错误是把左右孩子的压栈顺序写成 left 再 right，这会得到根-右-左。还要区分“压栈时机”和“访问时机”：本题是在 pop 后立即记录值。空节点不必压栈，可减少状态分支。",
        "transfer": "Day 10 的 LC-94/145 只是在改变“什么时候处理根”。前序是进入节点时处理；中序要等左子树回来；后序要等左右都回来。后续树题应优先问：递归函数在进入、左右子树之间、还是退出节点时做决策？",
    },

    94: {
        "pattern": "中序遍历：栈保存“左子树回来后还要处理的祖先”",
        "visual": """同一棵树：

        1
       / \\
      2   3
     / \\
    4   5

中序 = 左 -> 根 -> 右。
面对 1 时不能马上输出，因为 1 的左边还有整棵子树。
所以不断向左走，同时把“回来以后要处理的根”压栈：

current: 1 -> 2 -> 4 -> null
stack : [1, 2, 4]

走到 null 说明最左侧已经到底：
pop 4 -> 输出 4 -> 转向 4.right
若右边仍为空，再 pop 2 -> 输出 2 -> 转向 2.right(5)

栈里的节点不是“普通待访问节点”，而是“左子树处理完后等待恢复的递归现场”。""",
        "core": "一路向左时把祖先现场压栈；走不动时弹出最近祖先处理根，再转入它的右子树。",
        "formula": """递归中序：
inorder(root->left)
visit(root)
inorder(root->right)

迭代不变量：
ancestors 中的每个节点都满足：
- 自己还没 visit；
- 正在或即将处理它的左子树。

当 current == nullptr：
栈顶节点的左子树已经完整结束，
因此此刻恰好是 visit(root) 的时间。""",
        "model": "把显式栈理解为递归调用栈：沿左链压栈就是不断调用 left；弹栈就是一次 left 调用返回。",
        "steps": [
            "current 从 root 开始；只要 current 非空，就压入 ancestors 并继续 current=current->left",
            "走到空节点后，弹出最近祖先；它的左子树已经完成，因此此时记录它的值",
            "令 current 指向该节点的右孩子，相当于开始递归处理右子树",
            "只要 current 非空或 ancestors 非空就继续，直到没有当前子树也没有待恢复祖先",
        ],
        "memory": "左走时存现场，走到底后“弹一个、访问根、转右边”。",
        "proof": "一个节点第一次被 current 指到时只压栈一次；只有当它的整条左侧递归已经走到 null 后才会被弹出，因此左一定先于根。弹出后 current 转向右孩子，右子树又按同一规则完整处理，因此根一定先于右。每个节点压一次、弹一次，既不会遗漏也不会重复。",
        "pitfalls": "外层条件必须是 current || !stack.empty()；否则 current 变成 null 时会提前结束，来不及恢复祖先。若在压栈时记录值，就已经变成前序。不要把栈简单理解成“待访问节点”，中序里它更准确地表示未完成的递归现场。",
        "transfer": "LC-144 的栈保存“未来子树”，LC-94 的栈保存“暂停的祖先现场”。这个区别会直接迁移到 BST：中序会产生严格递增序列，因此 Day 11 的 LC-98 验证 BST 可以复用本题的访问顺序。",
    },

    145: {
        "pattern": "后序遍历：先构造根-右-左，再整体反转成左-右-根",
        "visual": """后序想要：左 -> 右 -> 根。
单栈直接模拟时，节点必须等左右子树都结束后才能访问，状态比前序复杂。
本实现换一个角度：先做一个容易生成的镜像前序。

目标：      左  右  根
整体反转：  根  右  左

所以先生成 根 -> 右 -> 左，再 reverse。

例如：
        1
       / \\
      2   3

先得到 [1,3,2]
reverse -> [2,3,1]
正好是后序。""",
        "core": "把难直接生成的“左-右-根”翻转成容易用栈生成的“根-右-左”，最后一次 reverse 恢复目标顺序。",
        "formula": """postorder = Left + Right + Root
reverse(postorder)
          = Root + reverse(Right) + reverse(Left)

对每棵子树都按“根、右、左”记录，
最终把整个序列 reverse，
就得到“左、右、根”。

为了弹出时先得到右孩子：
push(left)
push(right)
因为 right 后入先出。""",
        "model": "把后序转化成一个已学的前序变体，再利用序列反转完成顺序转换。",
        "steps": [
            "空树返回空结果；否则 root 入栈",
            "每次弹出 node 并立即记录，当前正在生成的是根-右-左序列",
            "先压左孩子、再压右孩子，使右孩子下一次先弹出",
            "所有节点记录完后 reverse(order)，得到真正的左-右-根后序",
        ],
        "memory": "后序不好直接做，就先做“根右左”，最后反转。",
        "proof": "第一次扫描中，每个节点仍只入栈和出栈一次，不会漏重。对任意父节点，生成序列中它在两个子树之前，且右子树整体在左子树之前；整体反转后，相对顺序同步翻转为左子树整体、右子树整体、父节点。因此每个局部子树都满足后序，整棵树也满足。",
        "pitfalls": "这里的压栈顺序与 LC-144 正好相反：为了生成根-右-左，要先 push(left) 再 push(right)。reverse 的是结果数组，不是树。该方案需要 O(n) 结果空间本来就不可避免；如果题目要求流式输出，才需要 visited/lastVisited 的真正单栈后序。",
        "transfer": "这是典型的“目标顺序难直接维护 -> 构造一个容易的逆序 -> 最后统一变换”。和 Day 7 LC-31 的思路一样，都不是机械模拟目标，而是寻找更容易维护的等价表示。",
    },

    102: {
        "pattern": "层序遍历：BFS 队列 + 冻结当前层宽度",
        "visual": """把树按距离根的层次画开：

level 0:        3
               |
level 1:     9    20
                  / \\
level 2:        15   7

队列始终从左到右保存“接下来要处理的节点”。
进入某一轮时：
queue = [9, 20]
width = 2   <- 先冻结

处理 9、20 的过程中会把 15、7 放到队尾：
queue = [15, 7]

关键是本轮只循环原来的 width=2 次；新孩子属于下一层，不能混进本层。""",
        "core": "每轮开始先冻结 queue.size()，这个数字就是当前层节点数；只弹这批节点，新入队的孩子天然组成下一层。",
        "formula": """一轮开始的不变量：
queue = 当前层全部节点（从左到右）
width = queue.size()

循环恰好 width 次后：
- 当前层全部出队；
- 每个当前层节点按 left、right 顺序加入孩子；
- queue 恰好变成下一层全部节点。

这就是 BFS 的“距离分层”性质。""",
        "model": "把 queue.size() 当成当前 BFS frontier 的边界快照；新产生的 frontier 留到下一轮。",
        "steps": [
            "空树返回空结果；非空时只把 root 放入队列",
            "每轮开始读取 width=pending.size()，创建这一层的结果数组",
            "恰好循环 width 次：弹出节点、记录值，并按左后右顺序把非空孩子加入队尾",
            "这一批处理完成后保存 level；此时队列只剩下一层，继续下一轮",
        ],
        "memory": "先拍一张当前队列长度的快照，再处理这一批；新来的孩子下一轮再说。",
        "proof": "根显然构成第 0 层。假设某轮开始队列恰好是第 d 层，循环只消费这固定的 width 个节点，因此不会误吃刚加入的下一层节点；每个第 d+1 层节点都有唯一父节点，并在处理父节点时恰好入队一次，所以循环结束后队列恰好且仅包含第 d+1 层。归纳即可覆盖所有层且无重复。",
        "pitfalls": "不能写成 for (i=0; i<pending.size(); ++i)，因为 size 在循环中会随着孩子入队变化，层边界会被破坏。左右孩子入队顺序决定同层输出顺序。空树不能把 nullptr 入队后再统一处理，否则会增加无意义分支。",
        "transfer": "这一题建立的 frontier 模型会直接迁移到 Week 3 图 BFS：腐烂橘子、01 矩阵、多源 BFS 都是在一层层推进“距离”。树只是一个天然无环、无需 visited 的特殊图。",
    },

    104: {
        "pattern": "树形递归：先定义子树返回值，再由左右答案合成当前节点",
        "visual": """不要先想“怎么遍历整棵树”，先问一个节点向父亲应该汇报什么。

        1
       / \\
      2   3
     /
    4

站在节点 2：
左子树(4) 报告深度 1
右空子树报告深度 0
所以 2 向上报告 1 + max(1,0) = 2

站在节点 1：
左子树报告 2
右子树(3)报告 1
所以根报告 1 + max(2,1) = 3

递归不是“神奇地往下钻”，而是每棵子树完成同一份契约后把结果交给父节点。""",
        "core": "定义 maxDepth(node) 为“以 node 为根的子树深度”；父节点只需取左右子树较大值再加自己这一层。",
        "formula": """状态定义：
D(node) = 以 node 为根，到最远叶子的节点数

边界：
D(nullptr) = 0

递推：
D(node) = 1 + max(D(node->left), D(node->right))

不变量：每次函数返回时，这棵子树的最大深度已经完整算好，父节点无需知道子树内部细节。""",
        "model": "树形递归的第一原则：先写清“这个函数给父节点返回什么”，再写如何由孩子返回值合成自己。",
        "steps": [
            "遇到空节点返回 0，这是最小子问题，也是递归终点",
            "递归取得左子树最大深度和右子树最大深度",
            "当前节点必须占一层，因此返回 1 + max(leftDepth, rightDepth)",
            "根节点拿到的返回值就是整棵树的最大深度",
        ],
        "memory": "孩子报深度，父亲选更深的一边再 +1。",
        "proof": "任意从 node 到叶子的路径第一步只能进入左子树或右子树，因此最长路径必等于两边最长路径中较大的那条再加当前节点。空树深度为 0，叶节点因此得到 1。这个递推枚举了路径唯一可能的两个方向，没有其它路径可漏，也不会把左右两条路径同时相加造成重复。",
        "pitfalls": "本题深度按“节点数”计，所以空树 0、叶子 1。不要误写成 left+right+1，那计算的是经过当前节点拼接两边的路径规模，会导向 Day 11 的直径问题。极端链状树递归深度可达 n；若环境栈限制严格可以改用 LC-102 的 BFS 层数。",
        "transfer": "这是 Day 11/14 树形 DP 的基础语言：LC-543 直径会复用左右深度，但把 left+right 作为经过当前节点的候选答案；LC-124 最大路径和也会区分“向父节点返回什么”和“当前节点更新什么全局答案”。",
    },

    105: {
        "pattern": "前序定根 + 中序切左右：用两个遍历序列给递归子树划边界",
        "visual": """用经典例子：
preorder = [3, 9, 20, 15, 7]
inorder  = [9, 3, 15, 20, 7]

前序第一项一定是当前根：3
在中序里找到 3：

inorder : [9] | 3 | [15,20,7]
           左       右

于是根的左右节点集合已经确定。
前序又保证“根后面先完整出现左子树，再出现右子树”：
preorder: 3 | [9] | [20,15,7]

对子树重复同一个动作：
当前前序首元素定根，中序位置切左右。

哈希表 value -> inorder index 只是为了 O(1) 找切口；真正决定结构的是两个遍历顺序提供的互补信息。""",
        "core": "前序告诉我们“下一个根是谁”，中序告诉我们“这个根把当前节点集合切成哪两棵子树”；递归不断重复这两个信息。",
        "formula": """当前递归负责 inorder[left..right]。
若 left > right：空子树。

rootValue = preorder[preorderIndex++]
middle = inorderIndex[rootValue]

左子树节点集合：inorder[left .. middle-1]
右子树节点集合：inorder[middle+1 .. right]

因为 preorder 顺序是 Root, Left, Right：
消费完 root 后必须先递归 build(left)，再 build(right)。

节点值唯一 => middle 唯一 => 每次切分唯一。""",
        "model": "把递归参数理解成“当前子树在 inorder 中拥有的合法领地”，把 preorderIndex 理解成“下一个尚未认领的根”。",
        "steps": [
            "预处理 inorder 的 value->index 哈希表，把每次寻找根位置从 O(n) 降为 O(1)",
            "build(left,right) 若区间为空直接返回 nullptr；否则从 preorderIndex 取当前根并推进游标",
            "用根值在 inorder 中的位置 middle 把当前合法区间切成左区间和右区间",
            "严格先递归构建左子树，再递归构建右子树，因为 preorder 在根之后就是先左后右",
            "顶层调用覆盖整个 inorder 区间，递归完成后返回整棵树根节点",
        ],
        "memory": "前序负责点名根，中序负责划地盘；根后先建左，再建右。",
        "proof": "节点值唯一时，当前根在 inorder 中只有一个位置，因此左右子树的节点集合唯一。preorder 的第一个未消费元素必是当前子树根，而其后紧接着的是完整左子树的前序，再是右子树前序；递归按照 inorder 区间限制消费范围，左子树恰好消费属于左区间的那些根，随后游标自然来到右子树。每个 preorder 元素只消费一次，每个 inorder 节点只作为一次切点，所以不漏不重，结构也唯一。",
        "pitfalls": "必须先建 left 再建 right；若交换顺序，全局 preorderIndex 会被错误子树先消费。inorderIndex 和 preorderIndex 是成员状态，本实现每次 buildTree 前都 clear/reset，避免同一个 Solution 对象重复调用时污染。该 O(n) 方法依赖节点值唯一；有重复值时 value->index 不再唯一。",
        "transfer": "这题把 LC-144/94 的遍历顺序反过来使用：前面是“树 -> 序列”，这里是“两个序列 -> 树”。若给 inorder + postorder，则后序末尾定根，但因为从后往前消费顺序是 Root, Right, Left，所以递归顺序也要反过来先建右子树。",
    },
}
