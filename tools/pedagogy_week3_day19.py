"""High-touch pedagogy overrides for Week 3 / Day 19.

Day 19 expands the one-dimensional DP state from Day 18 into grid and sequence
states.  The central question stays the same: which smaller solved states are
sufficient to summarize every candidate that can still affect the future?
"""

PEDAGOGY_WEEK3_DAY19 = {
    62: {
        "pattern": "二维网格 DP：从重复路径递归到一行滚动状态",
        "model": "把每个格子都看成一个子问题：到达 (row,col) 的所有路径，最后一步只能来自上方或左方。先得到二维递推，再观察每个格子只依赖上一行同列和当前行左侧，因此整张表可以压成一行。关键不是背 dp[col] += dp[col-1]，而是知道更新前后的 dp[col] 分别代表什么。",
        "derivation": """最自然的正确解法是递归：站在 (row,col)，若还没到终点，就分别尝试向右和向下，最后把两边返回的路径数量相加。它会枚举所有合法路径，所以正确。

真正的浪费是重复子问题。比如从多个不同路径都可能走到同一个格子 (r,c)；从这个格子到终点的剩余问题完全相同，却会被递归重新计算很多次。网格越大，重复越严重。

于是把问题反过来看：不再记录“我是怎么走到这里的”，只记录“到达这个格子一共有多少种方法”。设 ways[r][c] 为到达 (r,c) 的路径数，最后一步只可能来自上或左，所以每个格子只算一次：
ways[r][c] = ways[r-1][c] + ways[r][c-1]。
这把指数搜索压成 O(m*n) 个状态。

再看依赖：按行从左到右更新时，ways[r][c] 只需要上一行同列和当前行左列。把上一行保存在一维 dp 后：更新前 dp[c] 就是“上方”，已经更新后的 dp[c-1] 就是“左方”。因此二维表还能压成 O(n) 空间。

优化链：枚举所有路径 -> 相同格子后的剩余问题被重复计算 -> 每格只保存一次答案 -> 只依赖上方/左方 -> 一行滚动 DP。""",
        "visual": """以 3 x 4 网格为例，每个格子写“到这里有多少条路径”：

1  1  1  1
1  2  3  4
1  3  6 10

看中间一个格子：

        上方 old dp[c]
              |
              v
左方 --->   当前格
new dp[c-1]

当前格的每条路径最后一步必然从这两个方向之一进入。

一维压缩时，处理当前行某个 c 之前：
dp[c]   = 上一行 (r-1,c) 的路径数
dp[c-1] = 当前行 (r,c-1) 的路径数

因此直接执行：
dp[c] = dp[c] + dp[c-1]

从左到右的顺序本身就是状态语义的一部分。""",
        "core": "每个格子的路径只按最后一步分成“从上来”和“从左来”两类；按行左到右时，一维 dp 同时保存旧上方和新左方。",
        "formula": """二维状态：
ways[r][c] = 到达格子 (r,c) 的路径数量。

边界：
ways[0][c] = 1
ways[r][0] = 1

内部格：
ways[r][c] = ways[r-1][c] + ways[r][c-1]

压成一维后，在更新 (r,c) 前：
dp[c]   = ways[r-1][c]   // 上方旧值
dp[c-1] = ways[r][c-1]   // 左方新值

所以：
dp[c] += dp[c-1]。

循环不变量：处理完当前行的列 c 后，dp[0..c] 已表示当前行，dp[c+1..] 仍表示上一行。""",
        "steps": [
            "创建长度 n 的 dp，并全部初始化为 1；这等价于先写好第一行，也同时覆盖第一列只有一种走法的边界",
            "从第二行开始逐行处理；每一行都从 col=1 开始，因为 col=0 永远只能一路向下到达",
            "对每个 col，先认清旧 dp[col] 是上方，dp[col-1] 已经是本行左方，再执行 dp[col]+=dp[col-1]",
            "必须从左向右更新，这样左侧状态已经切换到当前行，而右侧仍保留上一行",
            "全部行处理后，dp[n-1] 就是右下角路径数量",
        ],
        "proof": "任意到达内部格 (r,c) 的路径最后一步唯一属于两类：从 (r-1,c) 向下，或从 (r,c-1) 向右。两类互斥且覆盖全部路径，所以数量可以相加。第一行和第一列只有唯一方向，因此边界为 1。按左到右顺序滚动时，两个依赖值在读取时分别仍保持正确的“旧上方”和“新左方”语义；压缩只丢弃未来不会再访问的更早行，不会改变任何状态值。",
        "pitfalls": "一维压缩后最容易忽略的是遍历方向：若从右向左，dp[col-1] 还停留在上一行，就不再代表当前格的左方。m 或 n 为 1 时答案仍为 1，初始化已自然覆盖。官方保证答案 <= 2*10^9，因此当前 int 实现满足题面范围。",
        "transfer": "这是 Day 18 LC-70 的二维版本：LC-70 的状态只从前两个位置来，这里每个网格状态从上和左来。下一题 LC-64 完全复用同一张依赖图，只把“两个来源的方案数相加”换成“两个来源的最小成本取 min 再加当前格”。",
        "memory": "画依赖箭头：旧 dp[col] 是上，新 dp[col-1] 是左；左到右更新才能同时拿到两者。",
    },

    64: {
        "pattern": "网格最短路径 DP：复用 LC-62 的上/左依赖，把计数改成最小代价",
        "model": "LC-64 与 LC-62 的移动规则完全相同，因此状态依赖图也完全相同。区别只有聚合语义：LC-62 把两类路径数量相加；LC-64 对两条来源的最小累计成本取较小值，再加当前格固定成本。理解这一点后，一维滚动的遍历方向可以直接从 LC-62 迁移。",
        "derivation": """最自然的正确做法仍然可以是 DFS：从左上角向右/下枚举所有合法路径，走到右下角时计算路径和并取最小。它当然不会漏答案，但不同路径会反复进入同一个格子；从同一个格子到终点的最优剩余成本是同一个子问题，却被重复求解，路径数量又是组合爆炸的。

把问题改成“到达每个格子的最小累计成本”，每个格子只需解一次。因为最后一步只能来自上或左，所以：
best[r][c] = min(best[r-1][c], best[r][c-1]) + grid[r][c]。
这就是最短路径意义下的最优子结构：到当前格时，如果某条来自上方的前缀已经比 best[r-1][c] 更贵，那么后面再加完全相同的当前格和未来成本，它永远不可能反超，可以永久丢弃。

和 LC-62 一样，当前格只依赖上一行同列与当前行左侧，因此二维 best 表可以压成一行 dp。

优化链：枚举整条路径 -> 同一格子的剩余问题重复出现 -> 每格只保留一个不被更差前缀支配的最小成本 -> 上/左二维 DP -> 一行滚动。""",
        "visual": """grid =
1 3 1
1 5 1
4 2 1

把每格改写成“到这里的最低累计成本”：

1 4 5
2 7 6
6 8 7

例如右中格值为 1：

上方最低成本 = 5
左方最低成本 = 7
当前格成本   = 1

所以当前最低 = min(5,7) + 1 = 6。

压成一维时仍和 LC-62 一样：
更新前 dp[col]   = 上方最低成本
更新后 dp[col-1] = 左方最低成本

只是操作从“+ 两个来源”变成：
min(上, 左) + 当前格。""",
        "core": "移动依赖完全复用 LC-62；到当前格只保留上/左两类来源中更便宜的前缀，再加当前格成本。",
        "formula": """二维状态：
best[r][c] = 从左上角到 (r,c) 的最小路径和。

内部格：
best[r][c] = min(best[r-1][c], best[r][c-1]) + grid[r][c]

第一格：
best[0][0] = grid[0][0]
第一行只能来自左；第一列只能来自上。

当前实现的一维不变量：进入某行时 dp[col] 是上一行对应列的最优值。
处理 col==0：
dp[0] = dp[0] + grid[row][0]

处理 col>0：
dp[col] = min(dp[col], dp[col-1]) + grid[row][col]

其中旧 dp[col] 是上，新 dp[col-1] 是左。""",
        "steps": [
            "创建长度 cols 的 dp，并把除 dp[0] 外的位置设为 INT_MAX；dp[0]=0 让第一格可以统一由虚拟起点进入",
            "逐行扫描 grid，并在每一行从左到右更新，保持和 LC-62 相同的依赖方向",
            "col==0 时只能从上方下来，因此执行 dp[0]+=row[0]；不要读取不存在的左邻居",
            "col>0 时，先比较旧 dp[col]（上）与新 dp[col-1]（左），取较小者后再加当前 row[col]",
            "最后 dp[cols-1] 即右下角最小路径和",
        ],
        "proof": "所有到 (r,c) 的路径最后一步仍只可能来自上或左。固定最后一步来自上时，若此前不用到上方格子的最小成本，而选一个更贵的前缀，之后两条候选都会加相同的当前格成本，所以更贵前缀永远不能变优；左方同理。因此只需保留两个来源各自的最优摘要，再取 min。按左到右更新保证一维 dp 的上/左语义正确，所以空间压缩不影响二维递推。",
        "pitfalls": "不要把第一行/第一列按普通双来源公式处理；当前代码用 col==0 单独处理第一列。INT_MAX 只用于初始化不可达状态，不能在尚为 INT_MAX 时盲目加值；当前 dp[0]=0 且逐行推进，实际更新路径不会对不可达 INT_MAX 做加法。遍历必须左到右。",
        "transfer": "LC-62 与 LC-64 是一对非常重要的模型迁移：状态图不变，只改变聚合运算。看到同样的“只能从上/左进入”，先复用依赖图，再判断目标是计数、最小成本还是最大收益。LC-1143 也会再次出现上、左、左上三个二维依赖。",
        "memory": "先复用 LC-62 的箭头，再把“上+左”换成“min(上,左)+当前成本”。",
    },

    300: {
        "pattern": "LIS：从 O(n²) 结尾 DP 到候选支配压缩，再用二分维护最优结尾",
        "model": "不要直接把 tails 当成神奇模板。先从自然 DP 出发：dp[i] 表示必须以 nums[i] 结尾的 LIS 长度，需要回看所有更小的 nums[j]。真正的优化观察是：对于相同子序列长度，结尾更大的候选面对未来永远不如结尾更小的候选——未来若能接在大结尾后，也一定能接在小结尾后。因此每个长度只保留最小结尾即可；这些最小结尾天然严格递增，于是可以二分定位应该被 x 改善的长度。",
        "derivation": """一个真实而自然的正确算法是 O(n²) DP：

dp[i] = 以 nums[i] 结尾的最长严格递增子序列长度。

对每个 i，扫描所有 j<i；若 nums[j] < nums[i]，就可以把 nums[i] 接到以 j 结尾的序列后：
dp[i] = max(dp[i], dp[j] + 1)。
答案是所有 dp[i] 的最大值。

它为什么慢？对每个新元素 x，我们会重新扫描所有历史位置，只为了回答一个更抽象的问题：“哪些长度的递增子序列可以被 x 延长，或者 x 能否让某个长度拥有更好的结尾？”历史中同长度的大量候选其实没有继续保留的价值。

候选支配是关键。假设已经存在两个长度同为 L 的递增子序列，结尾分别是 5 和 9。未来任何 value>9 都同时能接在 5 后面；而 6、7、8 只能接在 5 后面。也就是说结尾 9 面对任何未来都不可能比结尾 5 更有优势，可以永久丢弃。

于是对每个长度 L，只保留“长度为 L 的递增子序列所能达到的最小结尾”，记在 tails[L-1]。这些最小结尾必然严格递增。新值 x 到来时，只需找到第一个 >=x 的 tails 位置并替换：这表示 x 让这个长度拥有了更小或相同的结尾；若 x 大于所有 tails，则最长长度真正增加 1。

因为 tails 有序，线性寻找替换位置还可以进一步用 lower_bound 降成 O(log n)。最终总时间 O(n log n)。""",
        "visual": """nums = [10,9,2,5,3,7,101,18]

把 tails[k] 理解成：
“长度 k+1 的递增子序列，目前见过的最小可能结尾”。

依次处理：
10  -> [10]
 9  -> [9]       // 长度仍 1，但结尾 9 比 10 更有潜力
 2  -> [2]
 5  -> [2,5]
 3  -> [2,3]     // 长度 2 不变，只把结尾 5 改善成 3
 7  -> [2,3,7]
101 -> [2,3,7,101]
18  -> [2,3,7,18]

最终 tails.size() = 4。

注意最后的 [2,3,7,18] 恰好是一条 LIS，但这只是这个例子碰巧如此；算法只保证每个长度的最优结尾，不保证整个 tails 在处理历史替换后对应同一条真实原始子序列。""",
        "core": "同样长度的递增子序列只需要保留结尾最小的那个；维护每个长度的最小结尾后，新值用 lower_bound 找到它能改善的第一档长度。",
        "formula": """自然 DP：
dp[i] = 1 + max(dp[j])，其中 j<i 且 nums[j] < nums[i]。

压缩后的状态：
tails[L-1] = 当前扫描前缀中，长度为 L 的严格递增子序列的最小可能结尾。

不变量：
tails[0] < tails[1] < ... < tails[k-1]
且 tails.size() = 当前前缀的 LIS 长度。

处理 x：
pos = lower_bound(tails.begin(), tails.end(), x)

若 pos == end：
    x 大于所有最优结尾，追加 x，LIS 长度 +1
否则：
    *pos = x，当前长度不变，但对应长度的最优结尾不变差且通常变小。

严格递增使用第一个 >=x；若用 upper_bound，会错误允许相等值扩展长度。""",
        "steps": [
            "从空 tails 开始，逐个处理 nums 中的 value；tails 不保存所有候选，只保存每种长度的最小结尾",
            "对 value 使用 lower_bound，寻找第一个 >= value 的最优结尾位置；有序性使这个查找为 O(log n)",
            "若找不到该位置，说明 value 大于所有现有结尾，可以接到当前最长序列之后，因此 push_back(value)",
            "若找到位置，只替换该结尾为 value；这不会制造更长长度，只是在相同长度下保留更有利的未来状态",
            "扫描结束后返回 tails.size()；不要把 tails 内容本身当成一定可还原的 LIS",
        ],
        "proof": "对固定长度 L，只保留最小结尾不会丢掉任何未来最优解：若未来 x 能接在某个更大的同长度结尾后，那么 x 也一定能接在更小结尾后；反过来更小结尾还可能接受更多未来值，所以大结尾被永久支配。替换第一个 >=x 的位置时，前一档结尾（若存在）严格小于 x，因此确实存在一个长度为 pos+1、以 x 结尾的递增子序列；而不替换更靠后的长度，保证没有凭空增长长度。只有 x 大于所有 tails 时，才有证据把最长长度增加一。",
        "pitfalls": "严格递增必须用 lower_bound（第一个 >=x），重复元素不能扩展长度。tails[k] 表示最小结尾，不表示第 k 个 LIS 元素，也不保证整个 tails 就是一条原数组中的 LIS；若要还原具体序列，需要额外记录前驱与位置。不要跳过 O(n²) DP 的含义，否则很难理解 tails 到底压掉了什么状态。",
        "transfer": "这题把 Day 18 LC-198 的“候选支配”思想推得更彻底：LC-198 在同一前缀只保留最大收益；LC-300 在同一长度只保留最小结尾。看到“多个候选面对同样未来，其中一个在所有未来情形都不占优”，就应该考虑能否永久淘汰它，再利用压缩后的状态结构寻找单调性。",
        "memory": "先学 O(n²) 的“以 i 结尾”；再问同长度谁更值得留——结尾越小越强；最小结尾有序，所以二分。",
    },

    1143: {
        "pattern": "双序列 DP：两个前缀形成二维网格，用 diagonal 保存被一维覆盖的左上角",
        "model": "把 text1 的前缀长度放在行，text2 的前缀长度放在列。每个状态回答“这两个前缀的 LCS 有多长”。若两个末字符相等，可以把它们配成同一个新的末尾，来源是左上角 +1；若不同，至少有一个末字符不属于某个最优公共子序列，于是比较舍弃 text1 末字符（上）和舍弃 text2 末字符（左）。空间压缩后最难的不是公式，而是同时保存旧上、当前左和旧左上。",
        "derivation": """自然的正确递归是从两个字符串末尾开始：
- 若末字符相等，就把这个匹配计入答案，然后递归处理两个更短前缀；
- 若末字符不同，就分别尝试丢掉 text1 的末字符或 text2 的末字符，取两种结果较大者。

它正确，但不同分支会反复询问相同的前缀对 (i,j)。例如一次先删 text1 字符、下一层再删 text2，与先删 text2、再删 text1，都会走到同一个 (i-1,j-1) 子问题。直接递归因此会产生大量重复子问题。

把每个前缀对只算一次，就得到二维 DP：共有 (m+1)*(n+1) 个状态，时间 O(m*n)。

接着观察转移只依赖上一行 dp[i-1][j]、当前行左边 dp[i][j-1]、上一行左上 dp[i-1][j-1]。所以不需要保存更早的所有行，可以压成一维数组。

但压缩带来一个新的实现问题：从左到右覆盖 dp[j] 后，旧的左上角会消失。因此额外用 diagonal 保存“更新前上一列的旧 dp[j-1]”。每次先把旧 dp[j] 保存为 oldAbove，完成当前格更新后，再令 diagonal=oldAbove，为下一列准备新的左上角。

优化链：指数递归 -> 重复前缀对子问题 -> 二维 O(m*n) DP -> 依赖仅上/左/左上 -> 一维滚动 + diagonal。""",
        "visual": """把两个字符串放成网格坐标：

          text2:  a  c  e
                0  1  2  3
text1:      0   0  0  0  0
 a          1   0  \\  .  .
 b          2   .   X  .  .
 c          3   .   .  .  .
 ...

当前格 X 需要三个方向：

左上 diagonal    上 old dp[j]
        \\          |
         \\         v
          ---->   当前 X
                  ^
                  |
             左 new dp[j-1]

一维数组从左到右更新时：
dp[j]     在覆盖前 = 上方
dp[j-1]   已覆盖后   = 左方
diagonal  额外保存   = 左上方

这三个时间版本不能混。""",
        "core": "把两个前缀作为二维状态；字符相等走左上 +1，不等在上/左中取 max；压成一维后用 diagonal 专门保存即将被覆盖的旧左上角。",
        "formula": """二维状态：
LCS[i][j] = text1 前 i 个字符与 text2 前 j 个字符的最长公共子序列长度。

边界：
LCS[0][j] = 0
LCS[i][0] = 0

若 text1[i-1] == text2[j-1]：
LCS[i][j] = LCS[i-1][j-1] + 1

否则：
LCS[i][j] = max(LCS[i-1][j], LCS[i][j-1])

一维更新前：
dp[j]   = LCS[i-1][j]     // 上
dp[j-1] = LCS[i][j-1]    // 左
diagonal = LCS[i-1][j-1] // 左上

先保存 oldAbove=dp[j]，再写 dp[j]，最后 diagonal=oldAbove。""",
        "steps": [
            "创建 text2.size()+1 个 0，额外的第 0 列表示任何字符串与空前缀的 LCS 长度为 0",
            "逐个扫描 text1 的字符；每进入新一行时把 diagonal 重置为 0，对应这一行最左侧的旧左上边界",
            "对 j 从 1 向右扫描，第一件事先保存 oldAbove=dp[j]，因为覆盖后下一列仍需要它作为旧左上角",
            "若两个当前字符相等，写 dp[j]=diagonal+1；否则写 dp[j]=max(dp[j],dp[j-1])，分别对应上与左",
            "当前格写完后再执行 diagonal=oldAbove，让下一列得到正确的上一行左上状态；最后返回 dp.back()",
        ],
        "proof": "若两个前缀末字符相等，存在一个最优 LCS 可以把这个共同字符作为新增末尾，其余部分来自两个更短前缀的 LCS，因此左上 +1。若末字符不同，它们不可能同时作为同一个匹配末尾；任意公共子序列至少不使用其中一个末字符，所以一定包含在“删 text1 末字符”或“删 text2 末字符”的某个子问题中，取 max 覆盖全部可能。滚动数组只是复用存储位置；oldAbove 与 diagonal 保证覆盖前需要的上/左上值没有丢失。",
        "pitfalls": "最常见错误是 diagonal 更新时机：必须先保存 oldAbove，再计算当前 dp[j]，最后才 diagonal=oldAbove。dp[j-1] 是当前行新值，dp[j] 在更新前是上一行旧值，两者不能混淆。子序列允许删除但保持相对顺序，不要求连续；不要和最长公共子串混为一题。当前实现空间是 O(text2.length)，若严格追求 O(min(m,n)) 可先让较短字符串作为列，但不必为此改变已正确主实现。",
        "transfer": "直接复用 LC-62/64 的二维网格阅读法：先画依赖箭头，再决定遍历顺序。LC-62/64 只有上和左；LCS 多了左上，因此一维压缩必须额外保存 diagonal。之后学习编辑距离时仍然是同一张“两个前缀”网格，只会改变三个方向的转移含义。",
        "memory": "双前缀画网格：相等左上+1，不等上左取大；压一维时先救出 oldAbove，再覆盖，再把它交给 diagonal。",
    },
}
