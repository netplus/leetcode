"""High-touch pedagogy overrides for Week 4 / Day 26.

Day 26 studies greedy boundary compression: once the future only needs a release
frontier, a dominance order, two enclosing maxima, or a frequency skeleton, keep
that smaller state instead of repeatedly rebuilding the whole arrangement.
"""

PEDAGOGY_WEEK4_DAY26 = {
    253: {
        "pattern": "时间线资源复用：按开始时间扫描 + 最早结束时间堆",
        "model": "按开始时间从左到右处理会议。对当前会议来说，已经结束的会议都不再占房间，尚未结束的会议仍各占一间；因此真正需要持续维护的是这些活跃会议的结束时间，而其中最先可能释放资源的是最小结束时间。",
        "derivation": """一个直接正确的做法是先按开始时间排序；处理每场会议时，扫描此前所有会议，重新判断哪些仍与当前会议重叠，再计算当前同时进行的会议数。它不会算错，但每到一个新开始时间都重复检查大量已经处理过的会议，最坏 O(m^2)。\n\n观察一场旧会议一旦满足 end <= currentStart，它不仅对当前会议已经结束，对以后开始得更晚的所有会议也永久结束，再也没有保留价值。反过来，仍活跃的会议只需要记住 end；下一批能被淘汰的一定是 end 最小者。于是把“反复扫描历史会议”改成“最小堆维护仍活跃的结束时间”：不断弹出所有 end <= currentStart，再加入当前 end。优化类型：状态可延续 + 永久失效候选淘汰。""",
        "visual": """会议按 start 排序：\nA: [0,30)\nB: [5,10)\nC: [15,20)\n\n处理 A：heap=[30]                 活跃 1\n处理 B：5 < 30，heap=[10,30]      活跃 2\n处理 C：先弹 10，因为 10 <= 15\n        heap=[30] -> push 20\n        heap=[20,30]               活跃 2\n\n堆里不是“所有历史会议”，而只是此刻仍占房间的会议结束时间。""",
        "core": "按开始时间推进时间线，永久删除已经结束的会议；堆大小就是当前占用的房间数，历史最大堆大小就是最少所需房间数。",
        "formula": """处理当前会议 [start,end) 前：\nwhile heap.top() <= start:\n    pop()\n\npush(end)\nactive = heap.size()\nanswer = max(answer, active)\n\n不变量：push 当前会议之后，heap 中恰好保存所有已经开始但尚未结束的会议的 end。\n半开区间 [start,end) 下，end == start 时两场会议不重叠，所以可以复用房间。""",
        "steps": [
            "按 start 升序排序会议，使后续开始时间只会向右移动",
            "处理当前会议前，反复弹出 end<=currentStart 的堆顶，因为这些会议从现在起永久失效",
            "把当前会议的 end 压入最小堆，表示它开始占用一个房间",
            "用堆大小更新历史最大同时占用数 best，最终返回 best",
        ],
        "proof": "开始时间单调递增，所以一旦某会议 end<=currentStart，它也必然在所有未来会议开始前结束，可以永久删除。删除完后，堆中每个 end 都大于 currentStart，对应一场与当前时刻重叠的已开始会议；加入当前会议后，堆大小正好等于此刻同时占用的房间数。任何时刻同时进行 r 场会议至少需要 r 间房，而按最早释放资源复用恰能用这么多房间，因此全程最大活跃数就是最优答案。",
        "pitfalls": "结束等于下一场开始时可复用，所以条件必须是 <= 而不是 <。要弹出所有已结束会议，不是只弹一个。空输入返回 0。排序会修改 intervals。LC-253 在仓库中使用半开区间语义，解释和实现必须一致。",
        "transfer": "直接复用 Day 25 的“只维护未来仍有资格的 frontier”：LC-23 的堆顶是多个有序来源的下一个值，这里堆顶是多个占用资源里最早释放者。机器调度、连接池容量、并发请求峰值都可用同一时间线模型；若只求峰值，也可改成 start/end 事件扫描。",
        "memory": "时间向右走；结束过的永不回头，堆里只留还占房的会议。",
    },

    406: {
        "pattern": "约束支配顺序：高个先固定，再按 k 插入",
        "model": "一个人 [h,k] 只关心自己前面有多少个身高 >=h 的人；更矮的人无论站在他前面还是后面，都不会改变这个计数。因此先把更高的人全部固定后，再处理当前人时，当前队列中的每个人都会被计入他的 k，直接插到下标 k 即可。",
        "derivation": """一个自然且正确的构造思路是回溯：从尚未安排的人中选一个放到下一位置，维护前方身高计数；若某个部分队列已经违反某人的 k 就剪枝，最终枚举到合法排列。它能覆盖所有可能队列，但最坏仍接近排列枚举，因为大量顺序尝试其实不会影响某些人的约束。\n\n关键观察是约束有单向依赖：身高 h 的人只被 >=h 的人影响，所有更矮者对他的 k 完全不可见。于是先处理“会影响更多人的”高个，把未来更矮的人从当前约束中排除。处理 [h,k] 时，队列里只包含身高 >=h 的人，所以把他插到位置 k，前面恰有 k 个会计数的人；以后插入更矮者不会破坏这个事实。这把全局排列搜索变成一个可逐步固定且无需回退的贪心构造。优化类型：利用约束支配关系消除无意义排列分支。""",
        "visual": """people = [[7,0],[4,4],[7,1],[5,0],[6,1],[5,2]]\n\n先按 h 降序、同 h 按 k 升序：\n[7,0] [7,1] [6,1] [5,0] [5,2] [4,4]\n\n逐个按 k 插入：\n[7,0]\n[7,0] [7,1]\n[7,0] [6,1] [7,1]\n[5,0] [7,0] [6,1] [7,1]\n[5,0] [7,0] [5,2] [6,1] [7,1]\n[5,0] [7,0] [5,2] [6,1] [4,4] [7,1]\n\n插入 5 或 4 时，之后出现的更矮人不会改变已经固定人的 k。""",
        "core": "先安排会被当前人计数的高个；当轮到 [h,k] 时，已有队列全是 >=h 的人，因此插入下标 k 就一次性满足它的约束。",
        "formula": """排序规则：\nheight 降序；height 相同则 k 升序。\n\n处理 person=[h,k] 时的不变量：\nanswer 中所有已处理者 height >= h。\n\n于是插入 answer.begin()+k 后：\n当前 person 前面恰有 k 个 height >= h 的人。\n\n以后插入 height < h 的人，不会进入 person 的计数集合。""",
        "steps": [
            "按身高从高到低排序；同身高的人按 k 从小到大排序",
            "从排序后的第一人开始，依次取出 [h,k]",
            "把当前人插入 answer 的下标 k；此时已有元素都不矮于当前人",
            "处理完所有人后返回 answer；后来的更矮者不会破坏先前已经满足的约束",
        ],
        "proof": "对处理顺序做归纳。插入当前 [h,k] 前，answer 只含身高 >=h 的人，因此插到下标 k 后，前面恰有 k 个满足 >=h 的人，当前人的条件成立。以后只会插入更矮者，或在同高组中因 k 升序而按合法次序插入；更矮者不计入当前人的 k，而新插入动作只会把已有元素整体右移，不改变它们前面“不矮于自己”的人数。于是每一步固定的约束以后都不会失效。",
        "pitfalls": "两个排序关键字都不能反：高个必须先处理；同高时 k 小者必须先插，否则较大的 k 可能在当前 answer 中还没有合法下标，也会破坏同高计数。vector 中间插入是 O(n)，所以当前实现总时间 O(n^2)，这符合题目规模；不要误写成 O(n log n)。",
        "transfer": "这类贪心的核心是先找“谁的决定不会被后续弱对象影响”。与区间调度按结束时间排序类似，排序不是装饰，而是在制造一条决策一旦落下就不需要回退的支配顺序。若数据规模要求 O(n log n)，可把“找第 k 个空位”换成 Fenwick tree / segment tree。",
        "memory": "高个先站好；轮到你时，眼前的人全都算数，所以站到 k 就够了。",
    },

    42: {
        "pattern": "双侧边界压缩：从每格左右扫描到 O(1) 双指针",
        "model": "位置 i 能装多少水，只由它左侧最高柱和右侧最高柱中较矮的一侧决定：水位=min(leftMax,rightMax)。如果已经知道左边界不高于右边界，那么左端当前位置的水位已经被 leftMax 完全确定，无需知道右侧更精确的最高值，可以立即结算并向内移动。",
        "derivation": """最直接的正确算法是对每个位置 i，分别向左扫描最高柱、向右扫描最高柱，再计算 min(leftMax,rightMax)-height[i]。每个位置都重新寻找几乎相同的两侧最大值，时间 O(n^2)。\n\n第一步可复用这些查询：预处理 prefixMax[i] 和 suffixMax[i]，每个位置 O(1) 得到两侧最高柱，总时间 O(n)，但额外空间 O(n)。继续观察这些数组真正提供的信息：处理左端时，只要 leftMax<=rightMax，就已经知道右侧至少存在一根高度为 rightMax 的柱，因此左端水位一定由较小的 leftMax 决定；右侧具体还有多高不重要。于是只需一边向内走一边维护 leftMax/rightMax，把两个 O(n) 边界数组压缩成四个标量。优化类型：重复计算消除 -> 状态可延续 -> 边界状态压缩。""",
        "visual": """height = [0,1,0,2]\n\n对中间的 0：\n左最高 = 1\n右最高 = 2\n水位 = min(1,2)=1，接 1\n\n双指针视角：\nleftMax=1 <= rightMax=2\n          ^ 较低边界已经是瓶颈\n\n右边哪怕不是 2 而是 20，\n这个左位置最多仍只能蓄到高度 1。\n所以它现在就可以结算，不必等待右侧扫描完成。""",
        "core": "始终结算两侧已知最高边界中较低的一侧，因为较低侧已经成为该位置水位的确定瓶颈。",
        "formula": """单点公式：\nwater[i] = max(0, min(maxLeft[i], maxRight[i]) - height[i])\n\n双指针维护：\nleftMax  = 已扫描左侧最高柱\nrightMax = 已扫描右侧最高柱\n\n若 leftMax <= rightMax：\n    更新 leftMax\n    water += leftMax - height[left]\n    left++\n否则对称处理 right。\n\n不变量：被移出 [left,right] 的位置，其最终水量已经被唯一确定并累计。""",
        "steps": [
            "left/right 从两端开始，leftMax/rightMax 记录两侧已经见过的最高柱",
            "若 leftMax<=rightMax，先用 height[left] 更新 leftMax，再累计 leftMax-height[left]，然后 left++",
            "否则对右侧做完全对称的更新、累计和 right--",
            "直到区间处理完；water 即所有位置水量之和",
        ],
        "proof": "当 leftMax<=rightMax 时，右侧已经存在一根高度至少为 rightMax 的真实柱，因此对 left 位置而言，右边界绝不会低于 leftMax；它的最终水位只可能由 leftMax 限制。先把当前柱纳入 leftMax 后，leftMax-height[left] 就是该位置最终水量，可以安全永久结算。另一分支完全对称。每轮恰好移走一个尚未结算的位置，所以所有位置都会被处理一次且不会重复。",
        "pitfalls": "不要把比较条件机械换成 height[left] 与 height[right] 后仍沿用同一套变量证明；当前实现比较的是 leftMax/rightMax。先更新边界再做差，才能保证贡献非负。少于三根柱子自然得到 0。water 用 long long 累加更稳妥，最终按题目返回 int。",
        "transfer": "这条推导比直接背双指针更重要：先把 O(n^2) 重复边界查询变成前后缀缓存，再问缓存中哪些信息可以在线延续、何时一侧答案已被较小边界确定。类似的“双侧上界谁先确定谁”思路会出现在边界约束扫描中；与 LC-11 盛最多水的容器相比，两题都移动较弱边界，但淘汰证明不同。",
        "memory": "水位看两墙短的那面；短墙一旦确定，这一格就可以结算。",
    },

    621: {
        "pattern": "频率骨架：由最拥挤任务给出调度长度下界",
        "model": "频次最高的任务最难安放，因为它的相邻两次出现之间必须塞进至少 n 个时间单位。先把这些最高频任务当成骨架柱子，前 maxFrequency-1 次之间各撑开一个长度 n+1 的槽；其他任务只能去填这些槽或把调度继续拉长。",
        "derivation": """一个直接正确的算法是逐个时间单位模拟：维护每类任务剩余次数和下一次允许执行时间；每一时刻从当前可执行任务里挑剩余次数最多者，否则 idle。这个堆/冷却队列模拟能得到最短调度，但题目只有 26 种、每项耗时固定且冷却规则完全相同，实际保留了远多于答案需要的逐时刻状态。\n\n要问最短总长度，最紧的约束来自最高频任务。设最高频为 f：它的前 f-1 次出现后面都必须留出 n 个位置，所以形成 f-1 个长度至少 n+1 的完整块；若有 c 种任务都达到频率 f，最后一列需要容纳这 c 个最高频任务，于是骨架下界为 (f-1)(n+1)+c。另一方面任何调度都至少要执行 tasks.size() 个真实任务。其他任务若能填进骨架空位，就不会增加长度；若多到填满所有空位，它们本身把长度推到 tasks.size()。因此答案正好是两者最大值。优化类型：从逐时刻状态模拟压缩为决定答案的频率下界。""",
        "visual": """tasks = A A A B B B, n=2\nf=3, c=2\n\n先放最高频骨架：\nA _ _ | A _ _ | A\n\nB 也是最高频，把它放成另一列：\nA B _ | A B _ | A B\n\n长度 = (3-1)*(2+1)+2 = 8\n\n如果还有足够多 C/D/E 去填两个 '_'，\n就不会再有 idle；此时总长度最多由真实任务数主导。""",
        "core": "先用最高频任务算出任何合法调度都绕不过去的冷却骨架，再与真实任务总数取较大值。",
        "formula": """f = maxFrequency\nc = count(frequency == f)\n\nskeleton = (f - 1) * (n + 1) + c\nanswer = max(tasks.size(), skeleton)\n\n含义：\nf-1 个完整间隔块，每块至少 n+1；\n最后一组不需要再补冷却尾巴，只需放下 c 个并列最高频任务。""",
        "steps": [
            "统计 26 类任务频次，求最高频 f",
            "统计有多少种任务频率同样等于 f，记为 c",
            "按 (f-1)*(n+1)+c 计算最高频任务强制撑开的骨架长度",
            "返回 max(tasks.size(), skeleton)：任务足够多时填满 idle，任务不足时骨架中的空闲不可避免",
        ],
        "proof": "下界一：f 次同类任务之间有 f-1 个间隔，每个间隔至少需要 n 个其他时间单位，因此最高频任务必然撑开相应冷却距离；c 个并列最高频任务共同占据最后一组，得到 skeleton 下界。下界二：所有真实任务都必须执行一次，所以长度至少为 tasks.size()。可构造性方面，先按最高频任务形成这些块，再把其他任务分散填入块内空位；若任务数不超过骨架容量，可以不突破 skeleton，若任务更多，则空位全部被填满且剩余任务继续占据时间单位，不再需要额外 idle，于是达到 tasks.size()。所以两个下界的较大值可达。",
        "pitfalls": "冷却 n 表示两个相同任务之间至少隔 n 个时间单位，因此块长度是 n+1。并列最高频必须加 c，不能只加 1。n=0 时公式自然退化为任务数。这个 O(26+|tasks|) 公式依赖：任务单位时长、任意重排、所有类型共享同一个冷却 n；条件变化时应回到堆 + 时间模拟。",
        "transfer": "与 Day 25 的 Top-K/边界维护不同，这里进一步问‘能否不模拟每一步，只保留决定最优值的紧约束’。遇到调度题先区分：如果只求最短长度且约束高度对称，可尝试频率/容量下界；如果要输出具体顺序、任务时长不同或冷却不同，通常需要堆、队列或事件模拟。",
        "memory": "最高频任务先把时间轴撑开；空位能填就填，填满后答案就是任务总数。",
    },
}
