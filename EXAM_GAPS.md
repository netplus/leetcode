# 考试查漏补缺题

本文件记录由真实考试/面试反馈暴露出来、但不计入原 106 道正式训练题统计的补充题。补充题仍放在原有 Week / Day 能力链中，并使用相同的本地 `Makefile` / `tools/judge.sh` 判题方式。

## 2026-09-10 考试反馈

| 考试题型 | 对应训练题 | 状态 | 放置位置 | 主要能力 |
| --- | --- | --- | --- | --- |
| 数字字符串组成合法 IPv4 地址 | LC-93 复原 IP 地址 | 新增 P0 补充题 | Week 2 / Day 13 | 字符串分段、回溯、前缀剪枝 |
| 立定跳远最少步数 | LC-45 跳跃游戏 II | 已有 P0，无需重复补题 | Week 4 / Day 24 | BFS 层边界压缩、贪心最少步数 |
| 零钱凑整的组合数量 | LC-518 零钱兑换 II | 新增 P0 补充题 | Week 3 / Day 20 | 完全背包、组合计数、循环顺序语义 |

## LC-93：复原 IP 地址

目录：`problems/week2-list-tree-bsp/day13-lc93-restore-ip-addresses/`

学习链：

`枚举三个切点 -> 发现非法前缀仍被继续枚举 -> 按下一段长度构造搜索树 -> 合法才递归 -> 剩余长度 / 前导 0 / >255 剪枝 -> push / recurse / pop`

重点不是背回溯模板，而是理解：当一个完整方案由连续若干段组成，并且当前段一旦确定就能立刻判断是否合法时，应尽早停止不可能成功的前缀，而不是先枚举完整切法再统一过滤。

生成 / 校验：

```bash
python3 tools/gen_exam_gaps.py
python3 tools/gen_exam_gaps.py --check
```

## LC-518：零钱兑换 II

目录：`problems/week3-graph-dp/day20-lc518-coin-change-ii/`

学习链：

`按硬币种类枚举使用数量 -> 重复状态 (i, remaining) -> 二维 ways[i][amount] -> 消掉“枚举 k 枚”的内层 -> ways[i][a] = ways[i-1][a] + ways[i][a-coin] -> 一维压缩`

实现时必须从语义理解两个顺序：

- `coin` 在外层：固定硬币种类的处理顺序，因此统计的是组合，不会把 `1+2` 与 `2+1` 重复计算；
- `amount` 正序：`dp[a-coin]` 已经包含当前 coin，因而允许当前面额被重复使用，表达的是完全背包而不是 0/1 背包。

生成 / 校验：

```bash
python3 tools/gen_exam_gap_lc518.py
python3 tools/gen_exam_gap_lc518.py --check
```

## Day 20 背包能力矩阵

| 题目 | 物品使用次数 | 状态聚合 |
| --- | --- | --- |
| LC-416 分割等和子集 | 每个元素一次 | 可达性 |
| LC-494 目标和 | 每个元素一次 | 方案计数 |
| LC-322 零钱兑换 | 每种硬币无限次 | 最少枚数 |
| LC-518 零钱兑换 II | 每种硬币无限次 | 组合计数 |

以后根据考试反馈继续补题时，仍遵守“一题一题独立分析、独立生成、独立验证”的规则；先说明当前题库为什么不足，再补最小且精准的能力缺口。