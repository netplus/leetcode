"""Second-pass optimization derivations for reviewed learning problems.

These entries cover problems where the existing seven-layer explanation is good,
but the latest AGENTS.md asks for a clearer bridge from a natural direct method
to the chosen optimized mechanism.  Keep this separate from the first derivation
batch so the historical backfill remains incremental and easy to review.
"""

DERIVATION_BACKFILL_OVERRIDES = {
    1248: """最自然的做法，是枚举每个子数组并数里面有多少个奇数；即使向右扩展时复用当前 oddCount，仍然要检查 O(n^2) 个连续区间。

真正值得先做的不是立刻发明一个新窗口公式，而是把题目里的“奇数个数”翻译成可累加数值：奇数记 1，偶数记 0。这样一个区间“恰好有 k 个奇数”就等价于这个 0/1 区间“元素和恰好为 k”。

问题到这里已经完整变成 LC-560：固定当前前缀 prefix 后，需要寻找的历史状态唯一是 prefix-k，于是把枚举所有起点改成一次历史频次查询。

优化类型：先做问题等价变换，再复用已学的“重复查找 -> 历史状态索引”。主线优先复用 LC-560，而不是为同一问题重新背一套模板。""",

    283: """一个最容易写对的办法，是新建数组，把所有非零元素按原顺序复制过去，再补足 0；时间 O(n)，但额外空间 O(n)，不满足题目的原地要求。

如果不允许新数组，其实不需要记住全部输出，只需要记住“下一个非零元素应该落到哪里”。扫描到 read 时，前面已经处理过的非零元素顺序不会再变化，因此一个 write 下标就足以表示整个已压缩前缀。

遇到非零时，把它放到 write；遇到 0 时 write 不动。当前实现用 swap，使被覆盖位置自然留下 0，于是既保持稳定顺序，又不需要第二个数组。

优化类型：完整结果缓冲 -> 只保留下一写入位置这一小段状态，即原地稳定压缩。""",

    5: """最直接的正确做法，是枚举所有子串 [left,right]，再逐个检查它是否回文；子串有 O(n^2) 个，每次检查又可能 O(n)，总时间 O(n^3)。

这里重复做的工作是：同一个回文内部的镜像字符，会在许多包含它的子串检查里被反复比较。换一个视角，每个回文其实都有唯一的几何中心；只要固定中心，回文能延伸多远就只需从内向外顺序比较一次。

于是搜索空间从“枚举两个端点 + 再检查内部”改成“枚举 O(n) 个字符中心和 O(n) 个缝隙中心 + 向外扩展”。每个中心的最大回文直接在一次扩展里得到。

优化类型：重复验证内部结构 -> 改变枚举坐标系，从端点枚举转成唯一中心枚举。时间从 O(n^3) 降为 O(n^2)。""",

    151: """最自然也最容易理解的做法，是先按空格解析出所有单词放进 vector<string>，反转单词数组，再用一个空格 join；时间 O(n)，但会额外保存整套单词，空间 O(n)。

若进一步要求原地处理，就要问：单词顺序怎样在不保存所有单词的情况下整体翻转？把整个字符串先 reverse，会一次性把单词块的先后顺序倒过来；副作用只是每个单词内部也反了。

随后用 LC-283 的 read/write 稳定压缩去掉多余空格，并在每个单词写完后局部 reverse 恢复字符顺序。这样“保存所有单词再重组”被替换成“整体变换 + 局部修复 + 原地压缩”。

优化类型：完整中间结果 -> 原地结构变换；并直接复用 LC-283 的稳定压缩模型。""",

    252: """没有排序时，最直接的判断方法是比较每一对会议区间是否重叠，时间 O(m^2)。这很自然，因为任意两场会议看起来都可能冲突。

排序真正省掉的是“大量远距离区间对的比较”。按 start 排序后，如果当前会议和紧邻的前一场都不冲突，那么更早的会议不可能越过这条已经按时间排好的边界来制造新的冲突；潜在冲突被压缩到相邻边界。

于是全局的“两两关系”先通过排序获得单调顺序，再退化成一次线性相邻检查。

优化类型：无序全局比较 -> 排序制造单调性，把 O(m^2) 的任意两两检查压缩为 O(m log m) 排序 + O(m) 扫描。""",

    56: """一种直接做法，是反复寻找任意一对重叠区间，把它们合并后再从头检查；虽然正确，但同一批区间可能被多轮重新比较，最坏接近 O(m^2)。

真正困难的原因是输入无序：一个刚合并好的区间，不知道后面是否还有起点更靠左的区间回来与它相接。按起点排序后，这种“未来回头”被消除了。

此时只需维护一个尚未封口的当前并集 [L,R]：新区间能接上就扩 R；一旦 start>R，后面所有 start 只会更大，旧并集永久不可能再被连接，可以立即输出。

优化类型：重复全局重扫 -> 排序后维护一个可延续的活动区间；全局合并被压缩为 O(m log m)+O(m)。""",

    92: """最容易理解的做法，是先定位 [left,right]，把这段节点值或节点指针暂存到数组/栈里，再按逆序写回或重新连接；时间 O(n)，但需要 O(right-left) 额外空间。

如果要求 O(1) 空间，就不能保存整段反转结果。观察局部反转过程，其实每一步只需要知道三个边界：区间前驱 before、当前反转区间的尾 first，以及 first 后面下一个待搬节点 moved。

每次把 moved 从 first 后摘下，再插到 before 后，已反转区间就多一个节点，未处理区间就少一个节点；过去的中间状态完全体现在 next 指针里，不需要另存数组。

优化类型：保存完整局部结果 -> 把进度编码进链表连接本身，只维护常数个边界指针。""",

    142: """最自然的入口定位方法，是沿链表扫描并把访问过的节点地址放进 unordered_set；第一次再次遇到的节点就是环入口。它清晰且 O(n) 时间，但需要 O(n) 额外空间。

要做到 O(1) 空间，就不能保存完整访问历史。LC-141 已经告诉我们：快慢指针的速度差可以在不记 visited 的情况下制造一个环内相遇点。关键是这个相遇点不仅证明“有环”，它还携带一个路程同余关系。

把第一次相遇时 fast 比 slow 多走的整圈写成等式，可推出：head 到入口的距离，等于相遇点到入口的距离再加若干整圈。因此一个指针回到 head、另一个留在相遇点并改为同速，二者会恰好在入口会合。

优化类型：显式保存访问历史 -> 用周期中的路程关系编码历史信息；空间从 O(n) 降为 O(1)。""",

    31: """一个完全直接但不可扩展的办法，是生成所有排列、按字典序排序，再找到当前排列的下一个；它准确表达了 next permutation 的定义，但排列数是 n!。

真正需要的不是知道所有排列，而是直接构造“刚刚比当前大”的那个。因为字典序由从左到右第一个不同位置决定，所以越靠左的位置变化，影响越大；想让变化最小，就应尽量保留更左前缀。

从右向左找到的最长非递增后缀已经是这些元素能组成的最大顺序，只改这个后缀不可能让整体继续增大。因此必须修改后缀左侧的 pivot，而且要让它只增大到后缀中最小的严格较大值；高位完成最小增量后，再把后缀降到最小排列。由于原后缀本来非递增，交换后仍可直接 reverse 成升序，无需再 sort。

优化类型：全排列搜索 -> 利用字典序“第一个不同位置决定大小”的高位支配性，直接构造最小严格后继。时间从阶乘级降为 O(n)，空间 O(1)。""",

    57: """一个自然的复用方案，是把 newInterval 直接追加到 intervals，然后完整调用 LC-56：重新排序全部区间再线性合并，时间 O(n log n)，而且完全正确。

但本题额外给了比 LC-56 更强的条件：原 intervals 已经按 start 排序且互不重叠。重新排序等于把已经存在的顺序信息丢掉再计算一次。

利用这个条件，新区间只可能经历三个连续阶段：先经过所有完全在它左侧的区间；随后吸收一段与它相交的连续区间；一旦遇到 start>当前 new.end，后面所有区间都永久在右侧。于是无需排序，只做一次从左到右扫描。

优化类型：重复整理已有顺序 -> 复用输入已经建立的单调结构，把通用 LC-56 特化为 O(n) 三阶段插入。""",

    98: """一个很自然且容易证明正确的办法，是先做一次中序遍历，把所有值保存到数组；BST 当且仅当这个中序数组严格递增。时间 O(n)，但需要 O(n) 额外结果空间。

如果不想保存整条中序序列，可以把“已经看到的全局约束”直接携带到递归里。每经过一个祖先，当前子树允许的值域都会被进一步收紧：进入左子树增加一个严格上界，进入右子树增加一个严格下界。

这样不再事后生成序列再验证，而是在节点第一次被访问时立即用祖先累计出的 (lower, upper) 判断是否合法；递归栈本身就是唯一需要的路径状态。

优化类型：完整遍历结果缓冲 -> 把必要的历史约束压缩成两个边界参数；额外状态从 O(n) 序列降为 O(h) 递归栈。""",

    455: """最直接但代价很高的做法，是把“每个孩子是否拿哪块饼干”当成一个二分匹配问题：对每个孩子尝试所有尚未使用且尺寸足够的饼干，必要时回溯不同分配，最终取能满足孩子数最多的方案。它不会漏解，但会反复探索大量只在“谁拿了更大的饼干”上不同的分支。

真正可以删除这些分支的信息来自排序。把胃口 g 与饼干 s 都升序后，只看当前最小未满足孩子和当前最小未处理饼干：若这块饼干连最小胃口都不够，它对后面更大的胃口也永远无用，可以直接丢弃；若它已经够用，则总能通过交换把它分给当前最小胃口，而不减少最优方案能满足的孩子数，同时把更大的饼干留给更难满足的未来需求。

于是“枚举大量资源分配组合”被压成两个单调向右的指针；每一步都永久删除一块无用饼干，或永久固定一个不损失最优性的匹配。

优化类型：无序匹配组合搜索 -> 排序制造支配关系，再用交换论证进行贪心候选淘汰。""",
}
