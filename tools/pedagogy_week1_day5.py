"""High-touch pedagogy overrides for Week 1 Day 5."""

PEDAGOGY_WEEK1_DAY5 = {
    14: {
        "visual": """把所有字符串竖着对齐来看：

flower
flow
flight

第 0 列：f f f  -> 全部相同
第 1 列：l l l  -> 全部相同
第 2 列：o o i  -> 第一次失败

公共前缀就像从左往右逐列验收：只要某一列有人缺字符，或者字符不同，这一列以及后面的所有列都不可能再属于公共前缀。
所以第一次失败的位置，天然就是最长公共前缀的右边界。""",
        "core": "逐列检查所有字符串；前面每一列都通过，直到第一列失败，失败位置之前就是最长公共前缀。",
        "formula": """设 first = strs[0]。
对位置 i：

若存在某个 j，使得
  i == strs[j].size()
或
  strs[j][i] != first[i]

则答案长度就是 i。

如果 first 的所有位置都通过，答案就是 first 本身。""",
        "steps": [
            "把 strs[0] 当作公共前缀长度的最大上界",
            "从 i=0 开始逐列扫描 first",
            "对每一列 i，检查其余所有字符串是否仍有该位置且字符相同",
            "第一次越界或字符不同，立即返回 first.substr(0,i)",
            "如果 first 全部检查完成，返回 first",
        ],
        "proof": "公共前缀要求从第 0 个字符开始连续一致，因此一旦第 i 列失败，任何长度大于 i 的字符串片段都不可能成为公共前缀；而 [0,i) 的每一列都已经经过所有字符串验证，所以返回它既不会短也不会长。",
        "pitfalls": "字符串数组非空，但某个字符串可以为空；空串会在第 0 列立即终止。不要把“公共子串”和“公共前缀”混淆：前缀必须从下标 0 开始，所以第一次失败后无需继续搜索。",
        "transfer": "这是“从共同起点向前推进，首次失败决定边界”的模型。共同路径、版本号共同前缀、Trie 中沿唯一公共分支向下走，都有相同直觉。",
        "memory": "逐列验收；第一次失败就是答案边界。",
    },

    151: {
        "visual": """例：s = \"  the   sky is  blue \"。

第一步整体反转：
\" eulb  si yks   eht  \"

此时单词顺序已经变成：blue / is / sky / the，
但每个单词内部也被反转了，而且空格仍很乱。

接下来把字符串看成 LC-283 的稳定压缩：

read  -> 负责寻找下一个单词字符
write -> 负责把有效内容紧凑写回前面

压缩过程中，每复制完一个单词，就把这个单词局部再反转一次：

eulb -> blue
si   -> is
yks  -> sky
eht  -> the

最终得到：\"blue is sky the\"。""",
        "core": "整体反转先解决“单词顺序”，read/write 再压缩空格并复制单词，每个单词复制完后局部反转恢复字符顺序。",
        "formula": """整体反转后：
原词序 W1 W2 ... Wk
变成 reverse(Wk) ... reverse(W2) reverse(W1)

再对每个 reverse(Wi) 单独反转：
=> Wk ... W2 W1

压缩阶段维护：
[0, write) = 已整理好的结果
read        = 下一个尚未处理的位置""",
        "steps": [
            "reverse 整个字符串，让单词整体顺序先倒过来",
            "用 read 跳过连续空格，找到下一个单词起点",
            "若结果中已经有单词，先由 write 写入一个空格",
            "把当前单词逐字符复制到 write，并记录该单词在结果中的 [wordStart,write)",
            "局部 reverse 这个新写入的单词，恢复其字符顺序",
            "全部处理后 resize(write)，删除尾部旧内容",
        ],
        "proof": "整体反转已经唯一确定了正确的单词排列；之后的 read/write 只删除冗余空格、按出现顺序复制这些反转后的单词，不会改变单词之间的相对次序；每个单词再局部反转一次，恰好恢复原字符顺序。",
        "pitfalls": "本题输入可能有前导、尾随和多个连续空格，本地测试必须整行读取；write 不会超过 read，因此原地覆盖不会破坏尚未读取的有效字符；空格压缩时只在两个已输出单词之间写一个空格。",
        "transfer": "这里直接复用 LC-283 的 read/write 稳定压缩思想：read 负责找要保留的内容，write 决定它应落在哪里。再叠加“整体变换 + 局部修复”，可迁移到数组旋转、块反转等原地重排问题。",
        "memory": "整体反转改词序 -> read/write 压空格 -> 每个单词再反转回来。",
    },

    28: {
        "visual": """KMP 最难的地方不是“怎么比较字符”，而是：已经匹配了一大段后失配，哪些工作可以不重做？

模式串举例：
needle = \"ababaca\"

假设文本刚刚成功匹配了 needle 的前 5 个字符：

已匹配：a b a b a
模式串：a b a b a c a
                   ^
             下一位期待 c

此时如果文本下一字符不是 c，就发生失配。
但已匹配部分 \"ababa\" 的最长真前后缀是 \"aba\"：

前缀：a b a
后缀：    a b a

所以文本指针不用回退；文本末尾的 \"aba\" 已经确定等于 needle[0..2]，
只需让模式指针从 j=5 回到 j=lps[4]=3，再继续比较当前文本字符。

lps 保存的就是这种“失配后还能保留下来的最长已匹配边界”。""",
        "core": "失配时不把已匹配内容全部丢掉，而是把模式指针退到最长的“前缀=当前已匹配后缀”的位置，复用已经确认过的字符。",
        "formula": """lps[i] = needle[0..i] 的最长真前缀长度，
         且这个前缀同时也是 needle[0..i] 的后缀。

文本匹配时若 needle[j] 失配：
  j > 0  -> j = lps[j-1]
  j == 0 -> 文本 i++

完整匹配时：
  j == needle.size()
  起点 = i - j""",
        "steps": [
            "先为 needle 构造 lps：记录每个前缀在失配时可退回的最长边界",
            "扫描 haystack，i 指向文本位置，j 表示 needle 已匹配长度",
            "字符相等时同时推进；若 j 到达 needle.size()，返回 i-j",
            "字符失配且 j>0 时，只回退 j=lps[j-1]，i 不动",
            "字符失配且 j==0 时，说明没有可复用边界，才推进文本 i",
        ],
        "proof": "当 j 个字符已经匹配后发生失配，lps[j-1] 对应的前缀与这段已匹配文本的后缀完全相同，因此这些字符无需再次与文本比较；所有可能更长的边界都已被排除。文本指针从不回退，每次模式回退又严格缩短 j，所以总复杂度是 O(n+m)。",
        "pitfalls": "lps 表示最长真前后缀，不包含整个字符串自身；构造 lps 时失配也可能连续回退多次，不能直接清零；文本失配且 j>0 时不能推进 i，否则会跳过潜在匹配。",
        "transfer": "KMP 的核心不是背代码，而是“状态失败后退到仍然成立的最长边界”。字符串周期、重复子串、前后缀边界问题都在复用同一个 lps 状态。",
        "memory": "文本不回头；模式失配时退到还能复用的最长边界。",
    },

    5: {
        "visual": """任何回文都可以从“中心”向两边展开。

奇数长度：\"racecar\"
             e
          <- | ->
中心落在一个字符上。

偶数长度：\"abba\"
             ||
          <-    ->
中心落在两个字符之间的缝隙上。

所以对每个位置 i，只需要枚举两种中心：
(i,i)     -> 奇数回文
(i,i+1)   -> 偶数回文

然后只要左右字符相等，就继续同时向外走。""",
        "core": "枚举所有可能的回文中心，并从中心向两侧扩展；字符一旦不同，该中心能得到的最长回文就已经确定。",
        "formula": """扩展条件：
left >= 0
right < n
s[left] == s[right]

循环结束时已经多走了一步，因此真实回文区间是：
[left+1, right-1]
长度：
right - left - 1""",
        "steps": [
            "遍历每个 center",
            "调用 expand(center,center)，覆盖所有奇数长度回文",
            "调用 expand(center,center+1)，覆盖所有偶数长度回文",
            "expand 中左右字符相等就同时向外扩张",
            "停止后计算 length=right-left-1；若更长就更新 bestStart/bestLength",
        ],
        "proof": "每个非空回文都有且只有一个几何中心：奇数回文中心是某个字符，偶数回文中心是相邻字符之间的间隙；我们枚举了全部这两类中心。对固定中心，向外扩展直到首次失败，得到的就是该中心下最大的回文，因此全局最长回文必会被某次扩展完整发现。",
        "pitfalls": "最容易漏掉偶数中心 (i,i+1)；循环退出时 left/right 已经越过真实回文一格，所以长度是 right-left-1，起点是 left+1；若只在 length>bestLength 时更新，可稳定保留最早发现的同长度答案。",
        "transfer": "中心扩展不仅能求最长回文，也能把每次成功扩展计数后得到“回文子串总数”。如果规模更大，再学习 Manacher，本质仍是复用已经得到的回文半径。",
        "memory": "每个位置看两种中心；左右相等就扩，第一次不等就停。",
    },
}
