"""Full-plan reasoning-first bridge fields.

Generated once from the previously reviewed per-problem pedagogy. These fields
separate the natural solution, its limitation, the reusable rule, and the point
where the named algorithm is finally introduced.
"""

REASONING_FIRST_OVERRIDES = {1: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“从暴力配对到哈希索引：边扫描边查补数”。通用机制见 '
                             'docs/algorithms/hash-indexing.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
     'general_solution': '最直接的做法是固定一个下标，再在线性候选里寻找另一个下标；但当前值 x 一旦确定，真正要找的值其实已经唯一确定为 '
                         'target-x。哈希表的作用，就是给这些被反复查找的历史值建立“值 -> 下标”索引，把一次线性寻找变成平均 O(1) 查询。\n'
                         '\n'
                         '按实现顺序可以直接写成：\n'
                         '1. 初始化 unordered_map<int,int> indexOf；它不是只记录“出现过没有”，而是保存“值 -> 历史下标”\n'
                         '2. 扫描当前 nums[i]，先计算唯一需要的补数 complement = target - nums[i]\n'
                         '3. 用 indexOf.find(complement) 做纯查询；命中说明历史下标与当前 i 已经组成答案，立即返回\n'
                         '4. 只有查询未命中时，才执行 indexOf[nums[i]] = i，把当前元素加入历史，供后面的元素查询',
     'generalization': '这一题最值得迁移的不是“Two Sum 就用哈希表”，而是一个更通用的优化动作：先看暴力算法在重复查什么；如果每次都在历史数据里寻找一个可直接算出的 '
                       'key，就考虑为历史建立索引。',
     'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：最重要的是“先查后记”。'},
 3: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“滑动窗口 + 字符最后位置”。通用机制见 '
                             'docs/algorithms/sliding-window.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
     'general_solution': '最直接的做法可以枚举每个起点，并向右扩展直到遇到重复字符；不同起点之间会反复检查大量相同的字符串片段，最坏 O(n^2)。',
     'generalization': '这里复用的是“最近冲突位置”这一小段历史状态。由于 left 只向右移动，被排除的前缀永远不会重新成为合法窗口的一部分。',
     'limitations': '扫描到新字符 c 时，旧窗口本来已经无重复，因此唯一新增的冲突只可能来自 c 自己。若记住 c 最近一次出现的位置，就无需从 left '
                    '开始一个字符一个字符重新检查；可以直接把 left 跳到那个冲突位置之后。'},
 4: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“有序数组分割二分：搜索一条让左右数量平衡且值域合法的切线”。通用机制见 '
                             'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
     'general_solution': '最自然且正确的算法是像归并排序那样合并两个有序数组，再直接读取中间一个或两个元素，时间 O(m+n)、额外空间 '
                         'O(m+n)；即使不真正保存结果，只用双指针走到中间，也仍需要 O(m+n) 的最坏时间。',
     'generalization': '若 nums1 切出 cut1 个元素，那么 nums2 必须切出 leftSize-cut1 个，第二个切点无需搜索。于是只剩一个变量 '
                       'cut1。若 left1>right2，说明 nums1 左边拿多了，cut1 必须左移；若 left2>right1，说明 nums1 '
                       '左边拿少了，cut1 必须右移。这种方向性让 cut1 可以二分。优化类型：利用两个数组内部有序性，把完整归并压缩成对一个分割边界的搜索。',
     'limitations': '这里浪费的是：为了得到中位数，我们处理了大量远离中间边界的元素，而真正决定答案的只有‘整体左半最大的数’和‘整体右半最小的数’。由于两个输入内部已经有序，可以不构造完整归并序列，而是直接寻找一条分割线：左侧元素数量固定，且左边所有值都不大于右边所有值。'},
 5: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“中心扩展：统一奇偶回文”。通用机制见 '
                             'docs/algorithms/README.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
     'general_solution': '最直接的正确做法，是枚举所有子串 [left,right]，再逐个检查它是否回文；子串有 O(n^2) 个，每次检查又可能 O(n)，总时间 '
                         'O(n^3)。',
     'generalization': '于是搜索空间从“枚举两个端点 + 再检查内部”改成“枚举 O(n) 个字符中心和 O(n) 个缝隙中心 + '
                       '向外扩展”。每个中心的最大回文直接在一次扩展里得到。',
     'limitations': '这里重复做的工作是：同一个回文内部的镜像字符，会在许多包含它的子串检查里被反复比较。换一个视角，每个回文其实都有唯一的几何中心；只要固定中心，回文能延伸多远就只需从内向外顺序比较一次。'},
 11: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“相向双指针：移动短板”。通用机制见 '
                              'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最直接的做法是枚举所有左右边界 (left,right)，计算每个容器面积，时间 O(n^2)。',
      'generalization': '因此真正可尝试的方向只有“换掉短板”。每轮都能永久删除一个端点，O(n^2) 的端点组合枚举被压缩成 O(n) 次安全淘汰。',
      'limitations': '优化的关键是看清一次比较能永久淘汰谁。若左板更短，那么当前面积已经使用了“保留这块左板时能够得到的最大宽度”；以后只移动右边界，宽度一定更小，而有效高度仍不可能超过这块左板，所以这些候选全部被当前状态支配。'},
 14: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“纵向扫描：逐列验证公共前缀”。通用机制见 '
                              'docs/algorithms/README.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '公共前缀的第 i 个字符必须存在于每个字符串且都等于第一个字符串的第 i 个字符。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. 把 strs[0] 当作公共前缀长度的最大上界\n'
                          '2. 从 i=0 开始逐列扫描 first\n'
                          '3. 对每一列 i，检查其余所有字符串是否仍有该位置且字符相同\n'
                          '4. 第一次越界或字符不同，立即返回 first.substr(0,i)\n'
                          '5. 如果 first 全部检查完成，返回 first',
      'generalization': '这是“从共同起点向前推进，首次失败决定边界”的模型。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：字符串数组非空，但某个字符串可以为空；空串会在第 '
                     '0 列立即终止。'},
 15: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“排序 + 枚举锚点 + 双指针去重”。通用机制见 '
                              'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然的 3Sum 是三层循环枚举 i、j、k，时间 O(n^3)。真正的突破不是直接记住“三数之和要排序双指针”，而是先固定一个数 '
                          'nums[i]：剩余问题立即变成“在后面的元素里寻找两数之和等于 -nums[i]”。',
      'generalization': '优化类型：问题降维 + 排序制造单调性。复杂度从 O(n^3) 降为排序 O(n log n) 加 n 次 O(n) 双指针，即 O(n^2)。',
      'limitations': '如果剩余区间无序，这个二数问题仍要反复搜索；排序之后，就可以完整复用 LC-167 的单调淘汰：和小排除 left，和大排除 right。这样每个固定 '
                     'i 只需线性扫描一次。'},
 19: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“哑节点 + 固定间距双指针定位倒数位置”。通用机制见 '
                              'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '让 fast 比 slow 领先 n 个节点；fast 到末尾时，slow 正好位于倒数第 n 个节点的前驱。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. 建立 dummy->head，让删除原 head 也拥有统一的前驱节点。\n'
                          '2. fast 和 slow 都从 dummy 出发，先让 fast 走 n+1 步。\n'
                          '3. 随后 fast/slow 同步每次走一步，直到 fast==nullptr。\n'
                          '4. 此时 removed=slow->next，令 slow->next=removed->next，再释放 removed。',
      'generalization': '它和 LC-141 的快慢指针外形相似，但目的不同：LC-141 利用速度差，LC-19 维护固定距离。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：“领先 n 还是 '
                     'n+1”不能死记，要先明确 slow 最终要停在目标还是目标前驱。'},
 20: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“栈保存未闭合结构：最近开启，最先关闭”。通用机制见 '
                              'docs/algorithms/stack-queue.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '左括号开启一个尚未完成的结构，右括号必须关闭最近开启且同类型的结构，正符合栈的后进先出。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. 遇到左括号就压栈，表示开启了一个尚未完成的嵌套结构\n'
                          '2. 遇到右括号先检查栈是否为空，再检查栈顶是否是与它匹配的左括号；不匹配立即失败\n'
                          '3. 匹配成功就弹出栈顶；全部扫描结束后，只有栈为空才说明所有开启结构都已闭合',
      'generalization': '这是 Day 10 迭代 DFS 中“栈保存暂停现场”的同一思想：栈保存尚未完成的上下文。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：不能只统计三种括号数量是否相等，`([)]` '
                     '数量完全相等但嵌套顺序错误；遇到右括号必须先判空再访问 top；扫描完后别忘记检查残余左括号。'},
 21: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“哑节点 + 双路归并”。通用机制见 '
                              'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': 'tail 指向结果链表末尾，每次从两个链表当前头中选择较小者接入，等价于归并排序的 merge 步骤。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. 建立 dummy 和 tail，避免单独处理结果链第一个节点。\n'
                          '2. 两链都非空时比较当前表头，把较小节点直接接到 tail->next。\n'
                          '3. 只推进被选中的来源指针，再把 tail 移到新接入节点。\n'
                          '4. 一条链耗尽后，另一条剩余后缀已经整体有序且不小于当前 tail，可一次性接上。',
      'generalization': '这是归并排序 merge 阶段的链表版本。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：题目要求拼接原链表节点，不需要复制节点值；相等时从任一边取都能保持非递减。'},
 22: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“约束生成：把合法前缀写进搜索树，只生成仍可能成功的节点”。通用机制见 '
                              'docs/algorithms/backtracking.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': "先从能写出来的直接算法开始：长度固定为 2n，每一位都枚举 '(' 或 ')'。",
      'generalization': '问题在于很多候选在很短的前缀阶段就已经“判死刑”。例如：',
      'limitations': '总候选数 = 2^(2n) = 4^n。\n生成完整字符串后，再用 LC-20 的括号匹配规则判断是否合法。'},
 23: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“k 路归并：最小堆只维护每条有序链的当前 frontier”。通用机制见 '
                              'docs/algorithms/heap-topk.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一个直接正确做法是反复扫描 k '
                          '条链表的当前头，每次找出最小头并输出，再推进那一条链。因为每条链内部有序，它不会选错；但每输出一个节点都重新线性扫描 k 个候选，总时间 '
                          'O(Nk)。',
      'generalization': '这是 merge two sorted lists 的 k '
                        '路版，也是外部归并排序、多个有序日志流、多个有序迭代器合并的通用模型：每个来源只暴露当前最小 frontier，再用堆仲裁全局下一个。',
      'limitations': '重复工作恰好是‘在几乎相同的一组链头中再次找最小值’。每次输出后，只有一条链的候选发生变化，其余 k-1 个头完全没变。于是需要一个支持‘取最小 + '
                     '删除最小 + 插入一个新候选’的数据结构；最小堆把每次选择从 O(k) 降到 O(log '
                     'k)。重要的是堆里不放所有剩余节点，只放每条链当前最小未输出节点。优化类型：重复查找消除 + frontier 压缩。'},
 28: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“KMP：失配时复用已匹配前缀”。通用机制见 '
                              'docs/algorithms/kmp.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然的字符串匹配，是让 needle 从 haystack 的每个可能起点重新比较；一旦后面失配，就把文本起点右移一格并从 '
                          'needle[0] 重来。最坏情况下，大段已经比较相等的字符会被反复比较，达到 O(nm)。',
      'generalization': 'lps 就是在预处理 needle 时，把每个状态失配后还能保留的最长边界提前算好。',
      'limitations': '真正可以复用的是“已经匹配成功的那一段”本身。若它的某个后缀同时也是 needle '
                     '的前缀，那么失配后无需让文本指针回退；这段后缀已经被证明和模式前缀相同，只要让模式指针跳到对应长度继续比较即可。'},
 31: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“字典序下一排列：最小幅度增大”。通用机制见 '
                              'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一个完全直接但不可扩展的办法，是生成所有排列、按字典序排序，再找到当前排列的下一个；它准确表达了 next permutation '
                          '的定义，但排列数是 n!。',
      'generalization': '从右向左找到的最长非递增后缀已经是这些元素能组成的最大顺序，只改这个后缀不可能让整体继续增大。因此必须修改后缀左侧的 '
                        'pivot，而且要让它只增大到后缀中最小的严格较大值；高位完成最小增量后，再把后缀降到最小排列。由于原后缀本来非递增，交换后仍可直接 '
                        'reverse 成升序，无需再 sort。',
      'limitations': '真正需要的不是知道所有排列，而是直接构造“刚刚比当前大”的那个。因为字典序由从左到右第一个不同位置决定，所以越靠左的位置变化，影响越大；想让变化最小，就应尽量保留更左前缀。'},
 33: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“旋转数组二分：整体失序时，每轮先找到仍然有序的那一半”。通用机制见 '
                              'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然正确的方法是线性扫描 target，O(n)。普通 LC-704 二分不能直接套，因为 nums[mid]<target 并不再意味着 '
                          'target 一定在右边：例如 [4,5,6,7,0,1,2] 中 middle=7，而小值都在旋转断点之后。',
      'generalization': '如果左半有序，就能用 nums[left] <= target < nums[middle] 精确判断 target '
                        '是否属于左半值域；属于则保留左半，否则整段左半可淘汰。若左半无序，则右半必有序，做对称判断。这样每轮仍能删除约一半候选，恢复 O(log n)。',
      'limitations': '关键不是强求整个候选区间单调，而是问：一次比较还能否找到一块‘确定有序、因此可以整段判断’的区域？旋转后的两个升序片段只在一个断点处失序；在元素互异的前提下，中点不可能让左右两侧都跨过那个唯一断点，所以至少一半保持升序。'},
 34: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“边界二分：从找一个 target 升级为找单调谓词第一次成立的位置”。通用机制见 '
                              'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一个自然正确做法是线性扫描数组，记录第一次和最后一次遇到 target 的位置，时间 O(n)。即使先用普通二分 O(log n) 找到某个 '
                          'target，再向左右扩展，极端输入全部都是 target 时扩展仍会退化到 O(n)。',
      'generalization': '所以问题不再是‘某个位置等不等于 target’，而是‘假区间和真区间的分界点在哪里’。二分每次可以整段排除仍为假的左半或已经为真的右半，直接找到 '
                        'first true。用 value=target 找到 target 段开始，用 value=target+1 找到第一个严格大于 target '
                        '的位置，再减一得到结束位置。',
      'limitations': '浪费在于：我们已经知道数组非递减，却还在边界附近逐个确认重复值。对于固定 value，谓词 P(i)=[nums[i]>=value] '
                     '具有单调性：一旦某位置为真，右边所有位置都为真；一旦某位置为假，左边所有位置都为假。'},
 39: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“组合回溯：先规定唯一构造顺序，再用正数与排序剪掉无效后缀”。通用机制见 '
                              'docs/algorithms/backtracking.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '先写一个真正能工作的直接搜索：\n'
                          'remaining=target；每一层都可以从所有 candidates 中任选一个正数继续减，remaining==0 就得到一个答案。',
      'generalization': '1. 同一个组合被不同选择顺序重复生成：\n'
                        '   2 -> 2 -> 3\n'
                        '   2 -> 3 -> 2\n'
                        '   3 -> 2 -> 2\n'
                        '   三条路径其实都是组合 {2,2,3}。',
      'limitations': '它能找到答案，但有两个明显浪费：'},
 41: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“原地哈希：把值 x 放到下标 x-1”。通用机制见 '
                              'docs/algorithms/hash-indexing.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一个很自然的 O(n) 解法，是先把所有正整数放进 unordered_set，再从 1 开始查询第一个不存在的值；它已经满足时间目标，但需要 '
                          'O(n) 额外空间。',
      'generalization': '把每个合法值原地送到自己的槽位后，数组本身就变成了“值是否出现”的索引表。',
      'limitations': '题目进一步要求 O(1) 额外空间。关键观察是：长度为 n 的数组，答案只可能在 1..n+1；而值 x∈[1,n] 恰好可以映射到唯一槽位 '
                     'x-1。既然数组本身就有 n 个槽位，就无需再额外建立哈希桶。'},
 42: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“双侧边界压缩：从每格左右扫描到 O(1) 双指针”。通用机制见 '
                              'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最直接的正确算法是对每个位置 i，分别向左扫描最高柱、向右扫描最高柱，再计算 '
                          'min(leftMax,rightMax)-height[i]。每个位置都重新寻找几乎相同的两侧最大值，时间 O(n^2)。',
      'generalization': '这条推导比直接背双指针更重要：先把 O(n^2) 重复边界查询变成前后缀缓存，再问缓存中哪些信息可以在线延续、何时一侧答案已被较小边界确定。',
      'limitations': '第一步可复用这些查询：预处理 prefixMax[i] 和 suffixMax[i]，每个位置 O(1) 得到两侧最高柱，总时间 O(n)，但额外空间 '
                     'O(n)。继续观察这些数组真正提供的信息：处理左端时，只要 leftMax<=rightMax，就已经知道右侧至少存在一根高度为 rightMax '
                     '的柱，因此左端水位一定由较小的 leftMax 决定；右侧具体还有多高不重要。于是只需一边向内走一边维护 leftMax/rightMax，把两个 '
                     'O(n) 边界数组压缩成四个标量。优化类型：重复计算消除 -> 状态可延续 -> 边界状态压缩。'},
 45: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“贪心分层：把 Jump Game 的可达前沿升级成隐式 BFS 层”。通用机制见 '
                              'docs/algorithms/greedy.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一个自然且正确的做法是 BFS：下标是节点，从 i 到 i+1..i+nums[i] 都有一条边；BFS 第一次到达 n-1 '
                          '的层数就是最少跳数。但显式枚举每个节点的所有下一跳，最坏会产生 O(n^2) 条边，而且队列里存着大量连续下标。',
      'generalization': '当扫描指针 i 到达 currentEnd，说明当前层所有候选都已考察完，此时才 jumps++ 并令 '
                        'currentEnd=farthest。于是显式 BFS 队列和 O(n^2) 邻边枚举被两个边界变量替代。优化类型：BFS frontier '
                        '的区间压缩。',
      'limitations': 'LC-55 已经告诉我们，这些可达节点往往能压成一个连续前沿。对最短跳数再保留一层信息即可：currentEnd 表示当前 BFS '
                     '层的右边界，farthest '
                     '表示扫描完整个当前层以后下一层的最右边界。层内不需要立刻决定‘到底从哪个位置跳’，因为在增加跳数之前，我们应该先比较这一层所有起点能把下一层推到哪里。'},
 46: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“回溯母模型：DFS 决策树 + 做选择 / 撤销选择”。通用机制见 '
                              'docs/algorithms/backtracking.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '把排列看成一棵按位置逐层展开的决策树：第 d 层决定排列的第 d 个位置，path 是从根走到当前节点的选择历史，used '
                          '是仍可选择元素的状态。回溯并不是另一种神秘算法，而是 DFS 在离开子树时把共享状态恢复到进入该子树之前。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. answer/path 清空，used 初始化为全 false；根节点代表还没有确定任何位置\n'
                          '2. 进入一层后遍历所有下标 i，已经 used 的元素不能再次用于当前排列，直接跳过\n'
                          '3. 选择 nums[i]：先标记 used[i]=true，再 push 到 path，使状态变成子节点对应的排列前缀\n'
                          '4. 递归搜索这个前缀之下的全部排列；当 path 长度达到 n 时把完整路径复制进 answer\n'
                          '5. 递归返回后 pop path 并恢复 used[i]=false，严格恢复父节点状态，再尝试下一个兄弟候选',
      'generalization': '把 Day 10 的树 DFS 迁移过来：以前树节点和边由输入给定，现在“搜索树”由选择动态生成；递归调用栈仍然表示当前路径。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：`used` '
                     '表示的是当前路径状态，不是全局访问标记，所以递归返回后必须恢复。'},
 53: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“从枚举所有区间到 Kadane：只保留最有前途的结尾状态”。通用机制见 '
                              'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最直接的做法是枚举每个起点，再不断向右扩展终点并累计区间和，时间 O(n^2)。优化的关键不是先背 Kadane '
                          '公式，而是换一个观察方向：当终点固定为 i 时，所有“以 i '
                          '结尾”的候选中，只需要保留和最大的那个。因为未来无论再接上什么元素，这些候选都会加上完全相同的后缀；当前已经更小的候选以后永远不可能反超。于是原本大量区间候选可以压缩成一个 '
                          'current 状态。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. 先想清楚暴力：枚举 left，向右扩 right，用 running sum 检查所有 O(n^2) 个连续区间\n'
                          '2. 改为从左到右枚举终点 i，并令 current 表示“必须以 i 结尾”的最大子数组和\n'
                          '3. 处理 nums[i] 时，只比较两种合法来源：从 nums[i] 重开，或把 nums[i] 接到上一位置的 current 后面\n'
                          '4. 执行 current=max(nums[i], current+nums[i])，把所有以 i 结尾的候选再次压缩成一个最优状态\n'
                          '5. 执行 best=max(best,current)，因为全局答案可能在任何一个终点结束，而 current 之后还会继续变化',
      'generalization': '这题最值得迁移的是“按终点组织候选 + 丢掉被支配状态”的动态规划视角。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：不要把 '
                     'current 和 best 混为一谈：样例扫描到 -5 时 current 从 6 降到 1，但历史最优 best 仍是 6。'},
 55: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“贪心前沿：把所有可达路径压缩成最远可达位置”。通用机制见 '
                              'docs/algorithms/greedy.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然的正确做法是 DFS/BFS：从每个可达位置 i 枚举 1..nums[i] '
                          '的所有下一跳，直到到达末尾或搜索完所有状态。它不会漏掉任何路径，但同一个位置可能由很多不同跳法到达，而且从这些路径继续向后的能力只由‘当前下标’决定；枚举具体路径制造了大量重复状态。',
      'generalization': '于是把整片搜索状态压缩成一个整数：扫描所有 i<=farthest 的位置，并持续扩大 farthest。若扫描下标第一次超过 '
                        'farthest，说明出现了不可跨越的空隙；若 farthest 覆盖 n-1，则成功。优化类型：重复状态消除 + 候选状态压缩。',
      'limitations': '进一步观察，这题甚至不必为每个可达下标保存 visited。因为每次从可达位置 i 可以跳到 '
                     'i+1..i+nums[i]，所有已发现位置会形成一个连续前缀 '
                     '[0,farthest]。对于“是否最终可达”这个问题，前缀内部到底经由哪条路径到达已经失去意义；未来只需要知道最右边界 farthest。'},
 56: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“排序后线性合并区间”。通用机制见 '
                              'docs/algorithms/interval-sort-sweep.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一种直接做法，是反复寻找任意一对重叠区间，把它们合并后再从头检查；虽然正确，但同一批区间可能被多轮重新比较，最坏接近 O(m^2)。',
      'generalization': '此时只需维护一个尚未封口的当前并集 [L,R]：新区间能接上就扩 R；一旦 start>R，后面所有 start '
                        '只会更大，旧并集永久不可能再被连接，可以立即输出。',
      'limitations': '真正困难的原因是输入无序：一个刚合并好的区间，不知道后面是否还有起点更靠左的区间回来与它相接。按起点排序后，这种“未来回头”被消除了。'},
 57: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“有序区间的三阶段扫描”。通用机制见 '
                              'docs/algorithms/interval-sort-sweep.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一个自然的复用方案，是把 newInterval 直接追加到 intervals，然后完整调用 LC-56：重新排序全部区间再线性合并，时间 '
                          'O(n log n)，而且完全正确。',
      'generalization': '利用这个条件，新区间只可能经历三个连续阶段：先经过所有完全在它左侧的区间；随后吸收一段与它相交的连续区间；一旦遇到 start>当前 '
                        'new.end，后面所有区间都永久在右侧。于是无需排序，只做一次从左到右扫描。',
      'limitations': '但本题额外给了比 LC-56 更强的条件：原 intervals 已经按 start 排序且互不重叠。重新排序等于把已经存在的顺序信息丢掉再计算一次。'},
 62: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“二维网格 DP：从重复路径递归到一行滚动状态”。通用机制见 '
                              'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然的正确解法是递归：站在 '
                          '(row,col)，若还没到终点，就分别尝试向右和向下，最后把两边返回的路径数量相加。它会枚举所有合法路径，所以正确。',
      'generalization': '于是把问题反过来看：不再记录“我是怎么走到这里的”，只记录“到达这个格子一共有多少种方法”。设 ways[r][c] 为到达 (r,c) '
                        '的路径数，最后一步只可能来自上或左，所以每个格子只算一次：\n'
                        'ways[r][c] = ways[r-1][c] + ways[r][c-1]。\n'
                        '这把指数搜索压成 O(m*n) 个状态。',
      'limitations': '真正的浪费是重复子问题。比如从多个不同路径都可能走到同一个格子 '
                     '(r,c)；从这个格子到终点的剩余问题完全相同，却会被递归重新计算很多次。网格越大，重复越严重。'},
 64: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“网格最短路径 DP：复用 LC-62 的上/左依赖，把计数改成最小代价”。通用机制见 '
                              'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然的正确做法仍然可以是 '
                          'DFS：从左上角向右/下枚举所有合法路径，走到右下角时计算路径和并取最小。它当然不会漏答案，但不同路径会反复进入同一个格子；从同一个格子到终点的最优剩余成本是同一个子问题，却被重复求解，路径数量又是组合爆炸的。',
      'generalization': '和 LC-62 一样，当前格只依赖上一行同列与当前行左侧，因此二维 best 表可以压成一行 dp。',
      'limitations': '把问题改成“到达每个格子的最小累计成本”，每个格子只需解一次。因为最后一步只能来自上或左，所以：\n'
                     'best[r][c] = min(best[r-1][c], best[r][c-1]) + grid[r][c]。\n'
                     '这就是最短路径意义下的最优子结构：到当前格时，如果某条来自上方的前缀已经比 best[r-1][c] '
                     '更贵，那么后面再加完全相同的当前格和未来成本，它永远不可能反超，可以永久丢弃。'},
 70: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“线性 DP 入门：从重复递归到只保存前两个已解子问题”。通用机制见 '
                              'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然的正确解法是直接递归：',
      'generalization': '它为什么正确？任何到达 i 的方案，最后一步不是 1 阶就是 2 阶，两类互斥且覆盖全部方案。',
      'limitations': 'ways(i) = ways(i-1) + ways(i-2)'},
 76: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“最小覆盖滑动窗口”。通用机制见 '
                              'docs/algorithms/sliding-window.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最自然的做法，是枚举每个 left，再向右寻找第一个覆盖 t 的窗口；换一个 left '
                          '后又从头统计字符需求，很多相邻候选会反复扫描和计数同一段字符。',
      'generalization': '因此算法并不是“套一个滑动窗口模板”，而是把大量重叠窗口的重复统计压缩为两个单调边界上的增量事件。',
      'limitations': '真正可复用的是当前窗口的字符账本：right 加入一个字符只修改一项 need，left 移出一个字符也只恢复一项 need。更关键的是，left '
                     '没必要回退——当窗口已经覆盖 t 时，只需向右收缩 left 寻找当前 right 下的最短合法窗口；一旦刚刚失去覆盖，再继续推进 right。'},
 78: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“子集搜索：用 start 固定唯一选择顺序，每个搜索节点都是答案”。通用机制见 '
                              'docs/algorithms/backtracking.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '子集本质上是每个元素“选或不选”的 2^n 个决策。当前实现把二叉选/不选树改写成更紧凑的 start-index 搜索：path '
                          '表示已经选中的下标递增序列，start 表示下一次只能从哪里往后选。因为子集不关心排列顺序，这个单调下标约束为每个子集规定了唯一构造顺序。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. 从 path=[]、start=0 开始；进入 search 后立即记录当前 path，所以空集首先被收集\n'
                          '2. 从 i=start 到 n-1 枚举下一个加入子集的元素；push nums[i] 后，path 仍按原下标递增\n'
                          '3. 递归传 i+1，明确当前元素以后不能再选，且更小下标永远不会回头进入这个分支\n'
                          '4. 子树探索完成后 pop 当前元素，恢复父 path，再枚举下一个可能的子集分支',
      'generalization': '和 LC-46 对照最重要：排列关心顺序，所以每层都能从所有“未使用元素”中选；子集不关心顺序，所以用 start 强制下标递增，直接消除 '
                        '[1,2]/[2,1] 这类重复表示。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：最容易把 '
                     'LC-46 的“只在叶子记录”照搬过来，这会漏掉长度小于 n 的子集；本题每个 path 都合法，所以必须在进入每层时记录。'},
 79: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“路径回溯：把 Day 13 的选择-递归-撤销迁移到二维网格”。通用机制见 '
                              'docs/algorithms/backtracking.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '状态由当前位置 (r,c) 与正在匹配的 word[index] '
                          '组成。当前格字符匹配后，它只能在这一条递归路径里被占用；临时把格子改成输入不可能出现的哨兵，相当于 '
                          'used[r][c]=true，搜索四邻结束后再恢复。这里 visited '
                          '不能是全局的，因为同一个格子可以属于不同候选路径，只是不能在同一条路径中重复使用。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. 外层枚举每个格子作为 word[0] 的候选起点；search 自己负责字符不匹配时立即失败\n'
                          '2. 进入 search 后先检查边界和字符；若当前 index 已是最后一个字符，直接成功\n'
                          '3. 保存 board[r][c]，再把它改成哨兵，表示这个格子只在当前递归路径中暂时不可用\n'
                          '4. 依次尝试四个相邻方向去匹配 index+1；任一方向成功即可令 found=true\n'
                          '5. 无论 found 是否为 true，都恢复原字符后再返回，保证输入结构和兄弟搜索分支不受污染',
      'generalization': '直接复用 Day 13 的回溯框架：状态选择 -> 做选择 -> 递归 -> 撤销选择。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：不要使用永久的全局 '
                     'visited：某个格子在路径 A 中失败后，仍可能在路径 B 中合法使用。'},
 92: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“哑节点 + 头插法反转局部区间”。通用机制见 '
                              'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '最容易理解的做法，是先定位 [left,right]，把这段节点值或节点指针暂存到数组/栈里，再按逆序写回或重新连接；时间 O(n)，但需要 '
                          'O(right-left) 额外空间。',
      'generalization': '每次把 moved 从 first 后摘下，再插到 before 后，已反转区间就多一个节点，未处理区间就少一个节点；过去的中间状态完全体现在 '
                        'next 指针里，不需要另存数组。',
      'limitations': '如果要求 O(1) 空间，就不能保存整段反转结果。观察局部反转过程，其实每一步只需要知道三个边界：区间前驱 before、当前反转区间的尾 '
                     'first，以及 first 后面下一个待搬节点 moved。'},
 94: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“中序遍历：栈保存“左子树回来后还要处理的祖先””。通用机制见 '
                              'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '把显式栈理解为递归调用栈：沿左链压栈就是不断调用 left；弹栈就是一次 left 调用返回。\n'
                          '\n'
                          '按实现顺序可以直接写成：\n'
                          '1. current 从 root 开始；只要 current 非空，就压入 ancestors 并继续 '
                          'current=current->left\n'
                          '2. 走到空节点后，弹出最近祖先；它的左子树已经完成，因此此时记录它的值\n'
                          '3. 令 current 指向该节点的右孩子，相当于开始递归处理右子树\n'
                          '4. 只要 current 非空或 ancestors 非空就继续，直到没有当前子树也没有待恢复祖先',
      'generalization': 'LC-144 的栈保存“未来子树”，LC-94 的栈保存“暂停的祖先现场”。',
      'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：外层条件必须是 '
                     'current || !stack.empty()；否则 current 变成 null 时会提前结束，来不及恢复祖先。'},
 98: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“BST 全局约束下传：每深入一层，就把合法值域收紧成更小的开区间”。通用机制见 '
                              'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
      'general_solution': '一个很自然且容易证明正确的办法，是先做一次中序遍历，把所有值保存到数组；BST 当且仅当这个中序数组严格递增。时间 O(n)，但需要 O(n) '
                          '额外结果空间。',
      'generalization': '这样不再事后生成序列再验证，而是在节点第一次被访问时立即用祖先累计出的 (lower, upper) '
                        '判断是否合法；递归栈本身就是唯一需要的路径状态。',
      'limitations': '如果不想保存整条中序序列，可以把“已经看到的全局约束”直接携带到递归里。每经过一个祖先，当前子树允许的值域都会被进一步收紧：进入左子树增加一个严格上界，进入右子树增加一个严格下界。'},
 102: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“层序遍历：BFS 队列 + 冻结当前层宽度”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '把 queue.size() 当成当前 BFS frontier 的边界快照；新产生的 frontier 留到下一轮。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 空树返回空结果；非空时只把 root 放入队列\n'
                           '2. 每轮开始读取 width=pending.size()，创建这一层的结果数组\n'
                           '3. 恰好循环 width 次：弹出节点、记录值，并按左后右顺序把非空孩子加入队尾\n'
                           '4. 这一批处理完成后保存 level；此时队列只剩下一层，继续下一轮',
       'generalization': '这一题建立的 frontier 模型会直接迁移到 Week 3 图 BFS：腐烂橘子、01 矩阵、多源 BFS 都是在一层层推进“距离”。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：不能写成 '
                      'for (i=0; i<pending.size(); ++i)，因为 size 在循环中会随着孩子入队变化，层边界会被破坏。'},
 104: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“树形递归：先定义子树返回值，再由左右答案合成当前节点”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '树形递归的第一原则：先写清“这个函数给父节点返回什么”，再写如何由孩子返回值合成自己。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 遇到空节点返回 0，这是最小子问题，也是递归终点\n'
                           '2. 递归取得左子树最大深度和右子树最大深度\n'
                           '3. 当前节点必须占一层，因此返回 1 + max(leftDepth, rightDepth)\n'
                           '4. 根节点拿到的返回值就是整棵树的最大深度',
       'generalization': '这是 Day 11/14 树形 DP 的基础语言：LC-543 直径会复用左右深度，但把 left+right '
                         '作为经过当前节点的候选答案；LC-124 最大路径和也会区分“向父节点返回什么”和“当前节点更新什么全局答案”。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：本题深度按“节点数”计，所以空树 '
                      '0、叶子 1。'},
 105: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“前序定根 + 中序切左右：用两个遍历序列给递归子树划边界”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '递归结构本身很自然：preorder 当前第一个未消费值确定根，然后在当前 inorder 区间里找到这个根的位置，再切成左右子树。',
       'generalization': '题目保证节点值唯一，因此 rootValue 在 inorder 中的位置可以提前建立成 value -> '
                         'index。每个递归层的“重新线性找根”就变成一次 O(1) 查询，而每个节点只消费一次。',
       'limitations': '如果每层递归都在线性扫描 inorder 寻找 rootValue，这个算法仍然正确，但极端链状树会产生 n+(n-1)+...+1 次查找，最坏 '
                      'O(n^2)。'},
 124: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“树形 DP：当前节点闭合双臂，向父只返回单臂最大贡献”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个直接但代价较高的思路，是把每个节点都当成路径最高点，再分别重新计算它左、右子树能提供的最大向下路径和。这样当然能覆盖答案，但祖先和子节点会反复计算同一棵子树的最佳向下贡献，最坏可到 '
                           'O(n^2)。',
       'generalization': '本题比 LC-543 多出的关键只有负数：如果某侧向下贡献 < 0，把它接进路径只会让总和更差，因此对当前节点而言，这一侧等价于贡献 '
                         '0，可以永久丢弃。',
       'limitations': 'Day 11 的 LC-543 '
                      '已经告诉我们怎样消掉这类重复子问题：后序遍历时让每棵子树只计算一次“向父还能延伸多少”，并把结果直接上报。当前节点收到左右两个上报值后，顺手计算一次“经过我闭合的完整候选”。'},
 127: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“隐式无权图最短路：按规则现场生成邻居，而不是先显式两两建图”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个完全正确的直接方案，是先显式建图：对 wordList 中每一对单词逐字符比较，若恰好一位不同就连边，再从 beginWord 做 '
                           'BFS。它容易证明正确，但 N 个单词会产生 O(N^2) 对比较，每次比较还要看 O(L) 个字符，建图本身就可能达到 O(N^2·L)。',
       'generalization': '于是“先枚举所有节点对找边”变成“BFS 走到一个节点时现场生成它可能存在的边”。图仍然完全相同，只是不再物化无用的 O(N^2) 候选边。',
       'limitations': '真正重复的工作是：为了知道当前 word '
                      '的邻居，提前把它与所有其他单词都比较了一遍。其实邻居必须只改变一个位置，因此候选空间可以由规则直接生成：固定位置 i，把该字符改成 '
                      'a..z，最多只产生 25L 个真正可能相邻的字符串，再用哈希集合 O(1) 平均查询它是否在字典中。'},
 128: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“哈希集合 + 只从序列起点扩展”。通用机制见 '
                               'docs/algorithms/hash-indexing.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接且容易想到的做法是排序后扫描连续段，时间 O(n log n)。题目要求 O(n)，所以排序这一步必须被拿掉。',
       'generalization': '关键是只允许链头启动扫描：当 x-1 不存在时，x 才是某条连续段唯一的起点；链内部节点全部跳过。于是每条连续段只会被完整走一次。',
       'limitations': '用哈希集合可以把“x+1 是否存在”降成平均 O(1) 查询，但如果从每个 x 都向后走一遍连续链，像 1,2,3,...,n '
                      '这样的输入仍会重复扫描同一条链，退化到 O(n^2)。'},
 133: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“图的深拷贝：遍历负责发现结构，old->new 映射负责保持对象身份”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '图深拷贝的难点不是 BFS，而是环和共享邻居：同一个原节点可能从多条边被再次遇到，但克隆图里必须始终对应同一个新对象。因此需要一张 '
                           'original Node* -> clone Node* 的映射。它同时承担 visited 和对象身份索引两个角色。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 空输入直接返回 nullptr；否则先创建起点克隆，立刻写入 cloneOf，再把原起点入队\n'
                           '2. BFS 每次弹出一个 original，遍历它的全部 neighbors\n'
                           '3. 若某 neighbor 尚未出现在 cloneOf，立即创建其克隆、登记映射并把原 neighbor 入队\n'
                           '4. 无论 neighbor 是否第一次出现，都把 cloneOf[neighbor] 接入 cloneOf[original] 的 '
                           'neighbors\n'
                           '5. 遍历结束返回 cloneOf[node]；原图地址与克隆图地址完全独立，但拓扑关系保持一致',
       'generalization': '它和带 random 指针链表、对象依赖图、AST/IR 克隆本质相同：先建立 old->new 身份映射，再按原引用关系重建新引用。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：关键是先登记映射再入队，否则环可能导致重复创建。'},
 134: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“贪心候选淘汰：一次失败排除整段起点”。通用机制见 '
                               'docs/algorithms/greedy.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接的正确算法是把每个站都当起点模拟一整圈：从 s 出发逐站累加 gas[i]-cost[i]，一旦油量为负就判 s 失败，否则走满 n '
                           '站即成功。它显然不会漏答案，但最多 n 个起点各走 O(n) 站，最坏 O(n^2)。',
       'generalization': '因此一次失败可以把 start,start+1,...,i 全部永久排除，下一候选直接设为 i+1，不需要重新模拟这些起点。最后再用 '
                         'total=sum(gain) 判断全局是否有足够总油量。优化类型：失败前缀的候选支配 / 批量淘汰。',
       'limitations': '浪费发生在失败模拟之间的大量重叠。设从 start 出发，直到 i 之前油量始终非负，而到 i 后第一次变负：\n'
                      'sum(start..i) < 0。\n'
                      '对于中间任意 j∈(start,i]，从 start 到 j-1 的累计因为此前未失败而 >=0，所以：\n'
                      'sum(j..i) = sum(start..i) - sum(start..j-1) <= sum(start..i) < 0。\n'
                      '也就是说 j 自己出发到 i 时会更早或同样没油，绝不可能成为答案。'},
 139: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“前缀可达 DP：把指数分割搜索压缩成字符串位置上的 DAG 可达性”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然且正确的直接算法是 DFS：从 start 开始枚举下一段结束位置 end，只要 s[start:end] 在字典中，就递归处理 '
                           'end；到达 n 时成功。它枚举所有合法切分前缀，因此不会漏解。',
       'generalization': '把这个重复状态记下来就得到 memo；再反过来自底向前传播，就是前缀可达 DP。我们甚至可以把它看成一个隐式 DAG：位置 i 到 j '
                         '有边，当且仅当 s[i:j] 是字典词。reachable[i]=true 就是节点 i 已经从 0 可达。',
       'limitations': '真正的浪费在于不同切分路径会反复到达同一个 start。例如 s 的前缀既可以按若干短词到达位置 i，也可能按一个长词到达同一 i；一旦 start '
                      '相同，后缀 s[start:] 能否拆分与之前怎样到达这里完全无关。直接 DFS 会重复计算同一“从位置 start 出发能否成功”的子问题。'},
 141: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“Floyd 快慢指针：用速度差发现周期”。通用机制见 '
                               'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的判环方法，是边走边把访问过的节点地址放进 unordered_set；某个节点第二次出现就说明有环。这个方法直观且 O(n)，但需要 '
                           'O(n) 额外空间。',
       'generalization': 'fast 每轮比 slow 多走一个节点。在有限环上，这个相对位移按 +1 mod L 演化，最终必回到 0；无环时 fast 则会先到 '
                         'nullptr。',
       'limitations': '链表的 next '
                      '是确定性的：从任意节点出发，下一状态只有一个。若存在环，进入环后状态会周期性重复；因此可以不保存全部历史，而用两个不同速度的观察者检测“是否进入周期”。'},
 142: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“Floyd 两阶段：从环内相遇点反推出入口”。通用机制见 '
                               'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的入口定位方法，是沿链表扫描并把访问过的节点地址放进 unordered_set；第一次再次遇到的节点就是环入口。它清晰且 O(n) '
                           '时间，但需要 O(n) 额外空间。',
       'generalization': '把第一次相遇时 fast 比 slow 多走的整圈写成等式，可推出：head 到入口的距离，等于相遇点到入口的距离再加若干整圈。因此一个指针回到 '
                         'head、另一个留在相遇点并改为同速，二者会恰好在入口会合。',
       'limitations': '要做到 O(1) 空间，就不能保存完整访问历史。LC-141 已经告诉我们：快慢指针的速度差可以在不记 visited '
                      '的情况下制造一个环内相遇点。关键是这个相遇点不仅证明“有环”，它还携带一个路程同余关系。'},
 143: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“链表模型组合：找中点 + 反转后半段 + 拉链式交替合并”。通用机制见 '
                               'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确做法，是先把所有 ListNode* 放进 vector。这样既能 O(1) 访问头部，也能 O(1) 访问尾部，再用左右下标按 '
                           'L0,Ln,L1,Ln-1... 重新连 next。时间 O(n)，但需要 O(n) 额外空间。',
       'generalization': '于是已有工具可以直接拼起来：\n'
                         '1. 快慢指针找到中点并断链；\n'
                         '2. 复用 LC-206 反转第二段；\n'
                         '3. 两条链都能沿 next 正向读取，再做交替合并。',
       'limitations': '若要求 O(1) '
                      '额外空间，真正要消掉的是“为了从尾往前读而保存整张节点索引表”。单链表本身只能向后走，所以把问题变形：不要让指针去尾部倒着读，而是把后半段整个反过来，让尾部顺序主动变成可向前读取。'},
 144: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“前序遍历：根先处理，栈只负责保存之后要走的分支”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '把栈看成“未来待进入的子树清单”，而不是把前序遍历记成一套固定模板。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 空树直接返回；否则把 root 作为第一个待进入节点压栈\n'
                           '2. 每次弹出栈顶 node，因为它就是当前前序中的下一个节点，所以立即记录 node->val\n'
                           '3. 若右孩子存在先压右孩子，若左孩子存在再压左孩子，使左孩子位于栈顶\n'
                           '4. 重复直到栈为空，此时所有节点都已经恰好进入一次',
       'generalization': 'Day 10 的 LC-94/145 只是在改变“什么时候处理根”。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：最常见错误是把左右孩子的压栈顺序写成 '
                      'left 再 right，这会得到根-右-左。'},
 145: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“后序遍历：先构造根-右-左，再整体反转成左-右-根”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '把后序转化成一个已学的前序变体，再利用序列反转完成顺序转换。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 空树返回空结果；否则 root 入栈\n'
                           '2. 每次弹出 node 并立即记录，当前正在生成的是根-右-左序列\n'
                           '3. 先压左孩子、再压右孩子，使右孩子下一次先弹出\n'
                           '4. 所有节点记录完后 reverse(order)，得到真正的左-右-根后序',
       'generalization': '这是典型的“目标顺序难直接维护 -> 构造一个容易的逆序 -> 最后统一变换”。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：这里的压栈顺序与 '
                      'LC-144 正好相反：为了生成根-右-左，要先 push(left) 再 push(right)。'},
 148: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“自底向上归并排序：把 LC-21 的有序链合并扩展成长度 1、2、4... '
                               '的有序段”。通用机制见 '
                               'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最容易想到的办法，是把所有节点值拷进 vector，调用 sort，再写回链表；时间 O(n log n)，但需要 O(n) '
                           '额外空间，而且没有利用链表本身“改指针即可重排节点”的特点。',
       'generalization': '要同时满足 O(n log n) 时间和常数级额外指针空间，就需要一种只做顺序访问、又能稳定把问题规模翻倍解决的排序。LC-21 '
                         '已经提供核心积木：两个有序链表可以 O(a+b) 合并。',
       'limitations': '若直接在链表上做插入排序，可以 O(1) 空间，但最坏 O(n^2)，达不到题目的时间目标。'},
 151: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“整体反转 + 原地压缩 + 单词复原”。通用机制见 '
                               'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然也最容易理解的做法，是先按空格解析出所有单词放进 vector<string>，反转单词数组，再用一个空格 join；时间 '
                           'O(n)，但会额外保存整套单词，空间 O(n)。',
       'generalization': '随后用 LC-283 的 read/write 稳定压缩去掉多余空格，并在每个单词写完后局部 reverse '
                         '恢复字符顺序。这样“保存所有单词再重组”被替换成“整体变换 + 局部修复 + 原地压缩”。',
       'limitations': '若进一步要求原地处理，就要问：单词顺序怎样在不保存所有单词的情况下整体翻转？把整个字符串先 '
                      'reverse，会一次性把单词块的先后顺序倒过来；副作用只是每个单词内部也反了。'},
 152: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“双极值状态 DP：负数会翻转候选优劣，所以同时保留最大与最小后缀”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个自然正确的直接算法是枚举每个左端点，再向右不断累乘并更新全局最大值；它检查了所有连续子数组，时间 O(n^2)。',
       'generalization': '乘法却破坏了这个单状态支配关系。假设以当前位置结尾的两个候选乘积是 6 和 -10。面对未来 x=-2：\n'
                         '6 * -2 = -12，\n'
                         '-10 * -2 = 20。\n'
                         '原来最差的 -10 反而成为最好。因此“只留最大值”会丢掉未来可能翻身的候选。',
       'limitations': '对最大子数组和 '
                      'LC-53，我们曾经利用候选支配：固定同一个终点时，只保留最大的后缀和就够了，因为未来无论再加什么值，两个候选都会加同一个量，较差者永远不能反超。'},
 153: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“旋转边界二分：用右端锚点判断 middle 在断点哪一侧”。通用机制见 '
                               'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然正确的方法是线性扫描所有元素取最小值，O(n)。也可以扫描相邻元素寻找唯一下降点 '
                           'nums[i]>nums[i+1]，其后一项就是最小值，但最坏仍需 O(n)。',
       'generalization': '于是一次比较把一半位置排除，得到寻找旋转边界的 O(log n) 二分。',
       'limitations': '这里真正可利用的是旋转结构只有一个断点。我们不必逐个寻找下降点，只需判断 middle 在断点左侧还是右侧。为什么选择 nums[right] '
                      '作为锚点？当前候选区间的 right 位于包含最小值的那一侧或恰好就是候选末端；元素互异时：\n'
                      '- nums[mid] > nums[right]：mid 与 right 之间跨过断点，mid 一定在高值段，最小值严格在 mid 右侧；\n'
                      '- nums[mid] < nums[right]：mid 到 right 已经处于同一升序段，最小值不可能在 mid 右侧更深处，mid '
                      '自己却可能就是最小值。'},
 155: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“栈顶携带前缀摘要：把历史最小值一起压栈”。通用机制见 '
                               'docs/algorithms/stack-queue.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '如果只使用普通栈，push/pop/top 都是 O(1)，但 getMin 只能临时扫描整个栈找最小值，单次 O(n)。若 getMin '
                           '被频繁调用，就会反复扫描同一批历史元素。',
       'generalization': 'pop 时也无需重新计算——弹掉当前层后，下面一层保存的摘要天然就是恢复后的正确最小值。',
       'limitations': '最小值其实可以在 push 时增量维护：新状态的最小值只取决于“上一层最小值”和“新压入的 val”。所以每层除了保存 '
                      'value，再保存进入这一层后的 minimum 摘要。'},
 160: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“双指针拼接两条虚拟路径以消除长度差”。通用机制见 '
                               'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的线性解法之一，是先把链表 A 的所有节点地址放进 unordered_set，再扫描 B，遇到第一个已存在地址就是交点；时间 '
                           'O(m+n)，但需要 O(m) 额外空间。',
       'generalization': '因此无需存 visited，也无需先显式计算两条链长度，换头本身就在重新排列路径以完成对齐。',
       'limitations': '若要 O(1) 空间，真正需要消除的是两条独有前缀的长度差。让 pA 依次走 A 再走 B，让 pB 依次走 B 再走 A，两者都会走完全相同的总路程 '
                      'm+n；只是把各自的“长出来的那一段”放到了不同阶段。'},
 167: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“有序数组的相向双指针”。通用机制见 '
                               'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接的做法仍然是枚举所有下标对 (i,j)，检查 numbers[i]+numbers[j] 是否等于 target，时间 O(n^2)。',
       'generalization': '优化类型：存在单调性 -> 一次判断排除整批候选。于是两层枚举被压缩成两根只向内移动的指针，总扫描 O(n)。',
       'limitations': '题目额外给出的“数组有序”意味着一次比较不只排除当前这一对。当两端和太小时，left 已经与当前最大的 right 相加都不够，那么 left '
                      '与中间任何更小元素相加都不可能成功；这一整批候选可以同时删除。和太大时对 right 完全对称。'},
 179: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“拼接序的自定义排序”。通用机制见 '
                               'docs/algorithms/greedy.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个完全直接且正确的思路，是枚举所有排列、把每种排列拼成字符串后取最大；它说明了目标函数是什么，但排列数是 n!，无法实际使用。',
       'generalization': '于是全局排列搜索可以压缩成一个两两比较规则：a 应排在 b 前面，当且仅当 a+b > b+a。排序负责系统地消除所有这类局部逆序。',
       'limitations': '观察任意相邻的两个元素 a、b：在其他前缀和后缀固定时，这两项只存在 ab 与 ba 两种局部顺序。如果 ab < '
                      'ba，那么交换这两个相邻元素一定让整个结果变大；因此最优排列中不允许存在这种可改进的相邻逆序。'},
 198: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“选择 DP：把所有方案按“当前房偷不偷”分成两类，只保留前两个前缀最优”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确算法是 DFS 枚举每间房“偷 / '
                           '不偷”。若偷当前房，就跳过下一间；若不偷，就继续看下一间。所有合法选择集合都会被枚举，因此正确，但最坏会产生指数级搜索树。',
       'generalization': '换成前缀视角更直观：设 dp[i] 是处理完前 i 间房的最大金额。加入当前房 money 后，完整方案按是否选择它分成：\n'
                         '- 不偷当前：仍是 dp[i-1]；\n'
                         '- 偷当前：前一间不能偷，所以是 dp[i-2] + money。',
       'limitations': '重复在哪里？从不同选择路径走到同一个位置时，后面面对的是完全相同的一段房屋。例如前面如何到达“现在开始考虑第 i '
                      '间”并不重要；在只关心最大金额时，历史细节可以压缩成一个最优值。'},
 199: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“层序遍历的层内聚合：每层只取最右一个节点”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '这题几乎不需要新算法。直接复用 Day 10 / LC-102 的 BFS 层边界：一轮开始时 queue 恰好保存当前层全部节点，先冻结 '
                           'width，再处理这 width 个节点。因为当前实现按左孩子、右孩子入队，所以这一层最后被处理的节点就是最右节点；LC-199 只是把 '
                           'LC-102 的“收集整层”改成“只收集层末一个值”。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 空树直接返回空 answer；否则根节点入队，建立第 0 层 frontier\n'
                           '2. 每轮开始先保存 width=pending.size()，把它冻结为当前层节点数量\n'
                           '3. 循环 width 次从队首取节点；只有 i==width-1 时把 node->val 加入答案\n'
                           '4. 仍按 left、right 顺序把非空孩子加入队尾，让下一轮队列保持从左到右的层序顺序\n'
                           '5. 当前层恰好处理 width 个节点后进入下一轮，直到队列为空',
       'generalization': '它是 LC-102 的最小改造：BFS 框架、frontier、width 不变量全部不变，只改变“每层如何聚合”。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：不要在 for '
                      '条件里动态使用 `pending.size()` 作为当前层长度，因为处理中会持续加入下一层孩子；必须先冻结 width。'},
 200: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“网格连通分量：把二维格子看成隐式图，用 BFS 一次染完一个岛”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '不要把岛屿题当成特殊矩阵技巧。每个陆地格子就是一个图节点，上下左右的陆地之间存在边；题目要求的岛屿数量，就是这个隐式无向图中连通分量的数量。外层扫描负责发现一个尚未访问的新分量，BFS '
                           '负责把这个分量一次全部标记掉。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 从左到右、从上到下扫描所有格子；水域或已经染色的位置直接跳过\n'
                           "2. 第一次遇到仍为 '1' 的格子时，说明它不属于此前任何分量，因此 islands 加一\n"
                           "3. 把该格入队并立即改为 '0'；入队即标记，避免同一个格被多个邻居重复加入队列\n"
                           "4. 不断弹出队首，检查上下左右四个邻居；合法且仍为 '1' 的邻居立即染色并入队\n"
                           '5. 队列清空表示这一整座岛已处理完，外层扫描继续寻找下一座尚未访问的岛',
       'generalization': '这是 Week 3 的图论母模型：网格只是没有显式邻接表的图。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：必须只使用四方向，不能把对角线算作连通。'},
 206: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“链表原地反转：三指针”。通用机制见 '
                               'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '遍历时将当前节点的 next 指向已反转前缀；prev 是新链表头，current 是尚未处理部分的头。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. previous=nullptr、current=head，先把“已处理”和“未处理”两段分开。\n'
                           '2. 保存 current->next 到 next，确保改链后仍能进入原后缀。\n'
                           '3. 令 current->next=previous，把当前节点接到已反转前缀前面。\n'
                           '4. previous 移到 current，current 再移到 next，继续处理下一个节点。',
       'generalization': '这是本周最基础的链表指针模型。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：最危险的是先写 '
                      'current->next=previous 再取原 next，这会直接丢失剩余链表；返回值应是 previous，而不是已经变成尾节点的原 '
                      'head。'},
 207: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“拓扑排序：把“先修依赖”变成有向边，反复删除当前入度为 0 的课程”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '课程表首先是一个有向图建模题：prerequisites=[a,b] 表示 b 必须先于 a，因此边必须是 '
                           'b->a。能否完成全部课程，等价于依赖图是否无环。Kahn '
                           '算法不直接寻找环，而是不断拿走当前没有剩余前置依赖的节点；若最后仍有节点拿不走，剩余部分就被环互相卡住。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 建立邻接表 next，并按 b->a 的方向统计每门课 indegree；边方向反了会把依赖语义完全颠倒\n'
                           '2. 把所有初始 indegree==0 的课程放进 ready，包括没有任何依赖关系的孤立课程\n'
                           '3. 每次从 ready 取一门课，completed 加一；这代表它的所有先修都已经被移除，可以安全完成\n'
                           '4. 遍历它指向的后修课，把这些课程的剩余入度减一；只有在入度刚降到 0 时才入队一次\n'
                           '5. 队列耗尽后检查 completed；若小于 numCourses，剩余子图没有入度 0 节点，因此存在环',
       'generalization': '从 LC-200/133 的“遍历可到达节点”升级到“遍历必须遵守依赖顺序”。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：最容易错的是边方向：题目给 '
                      '[a,b]，必须建立 b->a。'},
 209: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“正数数组上的可变滑动窗口”。通用机制见 '
                               'docs/algorithms/sliding-window.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确算法，是从每个 left 出发重新向右累加，找到第一个 sum>=target 的 right；最坏会为不同 left '
                           '重复扫描大量相同元素，达到 O(n^2)。',
       'generalization': '相邻候选窗口高度重叠，旧窗口的 sum 可以通过“右边加一个、左边减一个”继续沿用，不必为每个 left 重新求和。',
       'limitations': '本题所有 nums[i] 都是正数，这让窗口拥有可利用的单调性：right 右移只会让和变大，left 右移只会让和变小。于是当某个 right '
                      '已经让窗口合法时，寻找更短答案只需继续推进 left；窗口变非法后，这个 left 永远不需要回到过去，只需继续推进 right。'},
 210: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“LC-207 的输出型迁移：Kahn 拓扑排序把可完成性证明直接变成课程顺序”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '完全复用 LC-207：prerequisites=[a,b] 建边 b->a，indegree[a] '
                           '记录尚未完成的先修数量。队列中的课程都是当前依赖已清零、可以立即学习的 frontier。LC-207 '
                           '只统计最终处理数来回答能否完成；本题在每次出队时把课程追加到 order，于是同一消除过程本身就是一份合法拓扑序。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 按照 [a,b] 表示 b 是 a 的先修，建立 b->a 邻接表并累计 indegree[a]；不要把方向写反\n'
                           '2. 把所有初始入度为 0 的课程放入 ready，它们不依赖任何尚未完成课程，可以作为拓扑序的起点\n'
                           '3. 循环弹出一个 ready 课程，立即追加到 order；这个动作就是 LC-207 中‘处理一个可完成课程’的可见输出\n'
                           '4. 遍历它的所有后继并递减入度；某后继恰好降到 0 时，说明它最后一个未完成先修刚被解除，将其加入 ready\n'
                           '5. 队列清空后比较 order.size() 与 numCourses；相等返回 order，否则返回空 vector 表示存在环',
       'generalization': '这是 Day 15 LC-207 的零新模板迁移：同一个 Kahn 过程，LC-207 的输出是 bool，LC-210 的输出是过程轨迹 '
                         'order。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：最常见错误仍是把 '
                      '[a,b] 建成 a->b。'},
 213: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“环形约束拆分：先用首尾互斥把环断开，再原样复用 LC-198”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确算法仍然可以 DFS '
                           '枚举每间房偷/不偷，并额外记住第一间是否被偷；走到最后一间时若第一间已偷，就禁止再偷最后一间。它能覆盖所有合法方案，但仍是指数级枚举，而且把一个只有首尾额外冲突的问题处理得过于复杂。',
       'generalization': '关键结构只有一个：首尾不能同时被选。于是所有合法方案一定属于下面至少一类：\n'
                         'A. 不选择最后一间 -> 只考虑 [0, n-2]；\n'
                         'B. 不选择第一间 -> 只考虑 [1, n-1]。',
       'limitations': '如果尝试直接把 LC-198 的线性 recurrence 套到整个数组，又会出错：线性 DP 只知道相邻 i-1/i 的冲突，它不会记住第 0 '
                      '间是否已选，因此可能同时选首尾。'},
 215: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“顺序统计量：从完整排序收缩到只定位第 k 大”。通用机制见 '
                               'docs/algorithms/heap-topk.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确做法是完整排序：sort(nums.begin(), nums.end()) 后返回 nums[n-k]，时间 O(n log '
                           'n)。它正确，但真正需要的信息只有一个顺序统计量；排序额外确定了其他 n-1 个元素之间的大量相对次序。',
       'generalization': '与 LC-347 的 Top-K 对照：静态数组只要一个排名时可用 Quickselect/nth_element；数据流或只愿保留 k '
                         '个候选时常用大小为 k 的堆。',
       'limitations': '把目标写成升序下标 target=n-k。一次 partition 以后，pivot 已经落到最终排名位置：若 pivotIndex==target '
                      '就结束；若 target 在左边，右分区的内部顺序以后永远无关，反之亦然。于是每轮都丢掉一整个不可能包含目标排名的分区，只继续处理一侧，这就是 '
                      'Quickselect。标准库 nth_element 提供同一类选择语义：目标位置变成完整排序后应有的值，但两侧内部无需排序。优化类型：候选淘汰 + '
                      '避免建立无用全序。'},
 226: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“树结构变换：先让左右子树完成职责，再交换当前节点的两个结果”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '复用 Day 10 的递归职责模型：子树先返回一个完整结果，父节点只负责组合，不跨层操心子树内部。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 空节点没有结构可翻转，直接返回 nullptr\n'
                           '2. 递归翻转原左子树并保存返回根 left\n'
                           '3. 递归翻转原右子树并保存返回根 right\n'
                           '4. 把 right 接到 root->left、left 接到 root->right，然后返回 root',
       'generalization': '这与 Day 10 LC-105“左右子树先递归构造，再挂回根”是同一种组合式递归。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：如果直接写 '
                      'root->left = invertTree(root->right) 后又基于已经改过的 root->left '
                      '去处理另一边，可能覆盖原指针；本实现先把两个递归结果保存到局部变量最清晰。'},
 234: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“快慢指针找中点 + 复用 LC-206 反转 + 同向比较”。通用机制见 '
                               'docs/algorithms/linked-list-pointer.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的做法是把链表值复制到 vector，然后像数组一样左右双指针比较；时间 O(n)，但需要 O(n) 额外空间。',
       'generalization': '比较完成后再反转一次恢复结构。',
       'limitations': '若希望 O(1) '
                      '额外空间，问题只剩一个障碍：单链表没有“从尾部向前”的指针。与其保存所有节点，不如改变后半段的方向：先用快慢指针找到中点，再原地反转后半段，就把“左边向右、右边向左”转换成“两边都沿 '
                      'next 向右”。'},
 236: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“后序证据汇聚：子树向上报告 p/q，第一次左右同时有证据的位置就是 LCA”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '把 Day 10 的“子树向父返回状态”升级为“子树向父返回证据”；父节点只根据左右证据是否同时出现做决策。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 遇到空节点返回 nullptr；若当前节点就是 p 或 q，直接返回当前节点作为证据\n'
                           '2. 后序递归获取左子树证据 left 和右子树证据 right\n'
                           '3. 若 left、right 都非空，说明 p/q 分居两侧，当前 root 就是最低汇合点，返回 root\n'
                           '4. 否则把唯一非空证据继续向父节点上报；两边都空则自然返回 nullptr',
       'generalization': '这是一类非常通用的后序汇聚：子树返回“证据”，父节点在证据首次同时出现时完成答案。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：这里比较的是节点身份 '
                      'root==p/root==q，而不是只比较 val；虽然本题值唯一，但 LeetCode 方法签名语义仍是节点。'},
 238: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“左右乘积：复用输出数组”。通用机制见 '
                               'docs/algorithms/prefix-sum-difference.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接的做法，是对每个 i 再遍历一遍数组，把所有 j!=i 的值相乘；这样每个答案都重新计算大量相同乘积，总时间 O(n^2)。',
       'generalization': '于是把“每个 i 重新乘 n 个元素”改成“两次线性扫描维护可延续累计状态”。最终 '
                         'answer[i]=leftProduct*rightProduct。',
       'limitations': '对固定 i，真正需要的信息天然只有两块：i 左边所有元素的乘积，以及 i 右边所有元素的乘积。相邻位置之间，这两块累计值可以持续复用：从左扫描时 '
                      'prefix 只需乘上刚经过的一个元素；从右扫描时 suffix 同理。'},
 239: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“单调队列：保存仍可能成为最大值的下标”。通用机制见 '
                               'docs/algorithms/monotonic-queue.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的做法是每滑动一个长度 k 的窗口，就重新扫描窗口里的 k 个数求最大值，时间 O(nk)。相邻两个窗口共享 k-1 '
                           '个元素，这些最大值候选被一遍遍重复比较。',
       'generalization': '因此窗口不再保存所有元素，只保存仍有资格成为未来最大值的候选，并额外从队首删除过期下标。',
       'limitations': '真正没必要保存的是“已经被新元素完全支配”的旧候选：若旧元素更小或相等，而且下标更早，那么只要新元素还在窗口里，旧元素就不可能成为最大值；等新元素离开时，旧元素反而更早已经过期。'},
 252: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“按开始时间排序检测相邻冲突”。通用机制见 '
                               'docs/algorithms/interval-sort-sweep.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '没有排序时，最直接的判断方法是比较每一对会议区间是否重叠，时间 O(m^2)。这很自然，因为任意两场会议看起来都可能冲突。',
       'generalization': '于是全局的“两两关系”先通过排序获得单调顺序，再退化成一次线性相邻检查。',
       'limitations': '排序真正省掉的是“大量远距离区间对的比较”。按 start '
                      '排序后，如果当前会议和紧邻的前一场都不冲突，那么更早的会议不可能越过这条已经按时间排好的边界来制造新的冲突；潜在冲突被压缩到相邻边界。'},
 253: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“时间线资源复用：按开始时间扫描 + 最早结束时间堆”。通用机制见 '
                               'docs/algorithms/heap-topk.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个直接正确的做法是先按开始时间排序；处理每场会议时，扫描此前所有会议，重新判断哪些仍与当前会议重叠，再计算当前同时进行的会议数。它不会算错，但每到一个新开始时间都重复检查大量已经处理过的会议，最坏 '
                           'O(m^2)。',
       'generalization': '直接复用 Day 25 的“只维护未来仍有资格的 frontier”：LC-23 '
                         '的堆顶是多个有序来源的下一个值，这里堆顶是多个占用资源里最早释放者。',
       'limitations': '观察一场旧会议一旦满足 end <= '
                      'currentStart，它不仅对当前会议已经结束，对以后开始得更晚的所有会议也永久结束，再也没有保留价值。反过来，仍活跃的会议只需要记住 '
                      'end；下一批能被淘汰的一定是 end 最小者。于是把“反复扫描历史会议”改成“最小堆维护仍活跃的结束时间”：不断弹出所有 end <= '
                      'currentStart，再加入当前 end。优化类型：状态可延续 + 永久失效候选淘汰。'},
 283: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“快慢指针：稳定压缩”。通用机制见 '
                               'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个最容易写对的办法，是新建数组，把所有非零元素按原顺序复制过去，再补足 0；时间 O(n)，但额外空间 O(n)，不满足题目的原地要求。',
       'generalization': '遇到非零时，把它放到 write；遇到 0 时 write 不动。当前实现用 swap，使被覆盖位置自然留下 '
                         '0，于是既保持稳定顺序，又不需要第二个数组。',
       'limitations': '如果不允许新数组，其实不需要记住全部输出，只需要记住“下一个非零元素应该落到哪里”。扫描到 read '
                      '时，前面已经处理过的非零元素顺序不会再变化，因此一个 write 下标就足以表示整个已压缩前缀。'},
 295: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“双堆动态分割：持续维护有序序列中间边界”。通用机制见 '
                               'docs/algorithms/heap-topk.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '直接算法可以把新数加入数组，每次 findMedian 时排序后读取中间位置；若查询频繁，就会反复重排大量旧数据。即使始终维护一个有序 '
                           'vector，插入仍可能搬动 O(n) 个元素。',
       'generalization': '与 LC-23 一样，堆只暴露当前最关键边界；区别是 LC-23 维护多个来源的最小 frontier，LC-295 '
                         '维护一个动态全体的中间分割线。',
       'limitations': '真正需要长期维护的不是完整顺序，而是中间分割线：左边有多少元素、右边有多少元素，以及分割线两侧的最大/最小边界。最大堆能 O(1) '
                      '看到左半最大值，最小堆能 O(1) 看到右半最小值，插入与跨边界搬运都是 O(log '
                      'n)。于是把完整动态有序表压成两个‘只暴露边界’的集合。优化类型：状态压缩 + 动态边界维护。'},
 300: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“LIS：从 O(n²) 结尾 DP 到候选支配压缩，再用二分维护最优结尾”。通用机制见 '
                               'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个真实而自然的正确算法是 O(n²) DP：',
       'generalization': '对每个 i，扫描所有 j<i；若 nums[j] < nums[i]，就可以把 nums[i] 接到以 j 结尾的序列后：\n'
                         'dp[i] = max(dp[i], dp[j] + 1)。\n'
                         '答案是所有 dp[i] 的最大值。',
       'limitations': 'dp[i] = 以 nums[i] 结尾的最长严格递增子序列长度。'},
 303: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“一维前缀和”。通用机制见 '
                               'docs/algorithms/prefix-sum-difference.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '如果 sumRange 只调用一次，最自然也最合适的做法就是直接遍历 [left,right] 求和，时间 '
                           'O(n)，完全没有必要使用前缀和。',
       'generalization': '于是把“每次查询现场重复求和”改成“构造时保存每个边界的累计值”。查询 [left,right] 时，两份已保存累计值相减即可消掉共同前缀。',
       'limitations': '题目的关键是同一个不可变数组会被查询很多次。不同查询会一次又一次把相同的前缀元素重新相加；这些累计结果与具体查询无关，可以只预处理一次。'},
 304: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“二维前缀和 + 容斥”。通用机制见 '
                               'docs/algorithms/prefix-sum-difference.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '二维情况下，最直接的 sumRegion 会遍历查询矩形里的每个单元格；矩形面积越大，单次查询越慢，而且多次查询会反复累加大量重叠区域。',
       'generalization': '这和 LC-303 完全同源：一维用两个边界累计值消掉公共前缀，二维用四个累计矩形消掉公共区域。',
       'limitations': '矩阵本身在构造后不会修改，因此可以把“从左上原点到每个二维边界的矩形和”预处理一次。以后任何目标矩形都不再逐格扫描，而是用四个已经保存的左上矩形做容斥。'},
 312: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“区间 DP：把难确定的“先戳谁”反过来改成邻居固定的“最后戳谁””。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接且正确的方法是枚举戳气球的顺序：当前剩哪些气球，就尝试戳其中任意一个，按它此刻的左右邻居计算收益，再递归处理剩余气球。所有 n! '
                           '种顺序都被覆盖，因此当然能得到最大值。',
       'generalization': '反过来问：在某个边界区间内，哪一个气球最后被戳？假设最后是 k。当它被戳时，(left,right) 内除 k 外的气球已经全部消失，因此 k '
                         '当时的两个邻居确定就是边界 left 和 right，最后一步收益固定为 '
                         'values[left]*values[k]*values[right]。',
       'limitations': '仅仅说‘它是指数/阶乘复杂度’还不够。真正难以复用状态的原因是：如果按第一枚被戳的 k 来划分，k '
                      '消失以后，它左右两侧的气球会重新成为邻居。之后左侧的操作收益可能依赖右侧还剩谁，右侧也可能依赖左侧，所以‘先戳 k’并不能把问题切成两个独立子问题。'},
 322: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“完全背包最优化：从重复 remainder 子问题到金额 '
                               'DP，并看清为何允许复用硬币”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个自然且正确的直接算法是 DFS：还剩 remainder 时，尝试每一种 coin<=remainder，递归求 '
                           'remainder-coin 的最少硬币数，再取所有选择中的最小值。它枚举了所有可能的硬币序列，因此能找到最优答案。',
       'generalization': '于是定义 dp[a]=凑出金额 a 的最少硬币数，让每个金额只求一次。固定一个目标金额 a，任何非空最优方案都可以按最后一枚硬币 coin '
                         '分类；拿掉它后得到金额 a-coin。如果那部分不是 dp[a-coin] 所代表的最优方案，用更优前缀替换后会让原方案更优，产生矛盾。因此：\n'
                         'dp[a] = min(dp[a-coin] + 1)。',
       'limitations': '浪费在于同一个 remainder 会从许多不同路径反复出现。例如 coins=[1,2,5] 时，金额 6 可以通过先选 1 再到 5，也可以先选 '
                      '2 再经过其他金额；一旦递归落到 remainder=4，从那里到 0 的最少硬币数只由 4 决定，与此前选择顺序无关。直接 DFS 会重复求这些 '
                      'remainder。'},
 347: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“频次哈希 + 大小为 k 的最小堆：只保留当前最强 Top-K”。通用机制见 '
                               'docs/algorithms/heap-topk.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '自然正确做法是哈希统计频率后，把所有 distinct 元素按 frequency 排序，再取前 k 个。若不同元素数为 m，排序成本 '
                           'O(m log m)，但答案只需要 Top-K 集合，Top-K 内部也无需排序。',
       'generalization': '与 LC-215 对照选择工具：只要第 k 大一个静态排名，可用 Quickselect；要保留 Top-K 集合且 k 小，用大小 k '
                         '的堆；频率范围较小时还可桶排序。',
       'limitations': '扫描每个 (value,frequency) 时，假设已经保留当前见过元素中的最强 k 个。把新候选临时放进来后只有 k+1 '
                      '个候选；其中频率最小的那个不可能属于这 k+1 项的 '
                      'Top-K，而且以后其他旧元素的频率已经固定，它也没有机会‘后来变强’，因此可以永久删除。于是始终只保存 k 个候选，用最小堆 O(log k) '
                      '找到并淘汰最弱者。优化类型：候选支配 + 避免完整排序。'},
 406: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“约束支配顺序：高个先固定，再按 k 插入”。通用机制见 '
                               'docs/algorithms/greedy.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个自然且正确的构造思路是回溯：从尚未安排的人中选一个放到下一位置，维护前方身高计数；若某个部分队列已经违反某人的 k '
                           '就剪枝，最终枚举到合法排列。它能覆盖所有可能队列，但最坏仍接近排列枚举，因为大量顺序尝试其实不会影响某些人的约束。',
       'generalization': '这类贪心的核心是先找“谁的决定不会被后续弱对象影响”。',
       'limitations': '关键观察是约束有单向依赖：身高 h 的人只被 >=h 的人影响，所有更矮者对他的 k '
                      '完全不可见。于是先处理“会影响更多人的”高个，把未来更矮的人从当前约束中排除。处理 [h,k] 时，队列里只包含身高 >=h 的人，所以把他插到位置 '
                      'k，前面恰有 k '
                      '个会计数的人；以后插入更矮者不会破坏这个事实。这把全局排列搜索变成一个可逐步固定且无需回退的贪心构造。优化类型：利用约束支配关系消除无意义排列分支。'},
 410: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“答案二分 + 贪心判定：把最小最大段和转成最小可行上限”。通用机制见 '
                               'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接的正确做法是枚举 k-1 个切分位置。数组有 n-1 个缝隙，从中选择 k-1 '
                           '个后就得到一种完整分割；计算每种分割的最大段和，再取最小值。它不会漏答案，但组合数量 C(n-1,k-1) 会迅速爆炸。',
       'generalization': '于是 check(limit) 可以 O(n) 得到最少需要多少段。limit 越大，限制越宽松，所需段数只会不增；可行 limit 再次形成 '
                         'F...F | T...T。这样就从‘枚举组合切点’转成了‘二分答案 + 线性贪心判定’。优化类型：用单调决策问题替代巨大方案枚举。',
       'limitations': '与其问‘最佳切点到底在哪里’，换一个更容易回答的问题：如果我先猜答案上限 limit，能不能把数组切成 k 段，使每段和都 <= limit？因为 '
                      'nums 全部非负，给定 limit '
                      '后有一个确定的最省段策略：当前元素只要还能放进本段就继续放，只有再放就超限时才切段。提前切只会让当前段更空，不可能让最终使用段数更少。'},
 416: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“0/1 背包可达性：把等分问题改写成每个数最多选一次的目标和”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个自然且正确的直接算法是对每个 nums[i] 做“选 / 不选”DFS，并维护当前子集和。走到末尾时检查是否等于 '
                           'total/2；它枚举了所有下标子集，所以一定不会漏解。',
       'generalization': '先做一个更重要的建模化简。若两组和相等，总和 total 必须为偶数，并且其中任意一组的和都必须是 '
                         'target=total/2。于是问题从“两组如何分配”变成：每个元素最多使用一次，是否能凑出 target。',
       'limitations': '问题在于不同选择路径会不断遇到相同的子问题。例如已经处理到下标 i、当前还需要凑 remaining '
                      '时，之前具体选了哪些元素并不再重要；未来能否成功只取决于“还剩哪些下标”和 remaining。直接 DFS 会把这些相同状态重复计算，最坏需要枚举 '
                      '2^n 个子集。'},
 417: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“反向多源可达性：把“每个格子能否到终点”改成“终点能反向到哪些格子””。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接的正确做法，是对每一个格子 (r,c) 分别执行搜索：沿着 nextHeight<=currentHeight '
                           '的真实水流方向，看能否碰到太平洋边界、又能否碰到大西洋边界。单次搜索最坏 O(mn)，一共有 mn 个起点，因此最坏可能达到 '
                           'O((mn)^2)，而且相邻起点会一遍遍探索相同区域。',
       'generalization': '于是无需从 mn 个起点重复问同一个问题。把太平洋全部边界格同时作为源做一次反向洪泛，得到 pacific；再对大西洋做一次，得到 '
                         'atlantic。每个格子在每次洪泛中最多访问一次。',
       'limitations': '问题问的是“很多起点能否到少数终点集合”。把边方向反过来后，关系完全等价：\nA 正向能到 Ocean\n<=> Ocean 在反向图中能到 A。'},
 438: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“定长滑动窗口 + 频次差”。通用机制见 '
                               'docs/algorithms/sliding-window.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接的做法，是对 s 中每个长度 |p| 的子串重新统计 26 个字符频次，再与 p 比较。相邻窗口只有两个字符不同，却把中间 |p|-1 '
                           '个字符反复统计；按窗口长度重新计数会造成大量重复工作。',
       'generalization': 'missing 再把“检查 26 个字符是否全部满足”压缩成一个总欠账数，使每次窗口移动只做 O(1) 状态更新。',
       'limitations': '滑动一格时真正发生的变化只有两个事件：右边进入一个字符、左边离开一个字符。因此只要维护一张可增量更新的 need '
                      '账本，就可以把上一窗口的状态直接延续到下一窗口，而不是从零重建频次。'},
 455: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“排序 + 双指针贪心：让最小够用资源匹配最小需求”。通用机制见 '
                               'docs/algorithms/two-pointers.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接但代价很高的做法，是把“每个孩子是否拿哪块饼干”当成一个二分匹配问题：对每个孩子尝试所有尚未使用且尺寸足够的饼干，必要时回溯不同分配，最终取能满足孩子数最多的方案。它不会漏解，但会反复探索大量只在“谁拿了更大的饼干”上不同的分支。',
       'generalization': '于是“枚举大量资源分配组合”被压成两个单调向右的指针；每一步都永久删除一块无用饼干，或永久固定一个不损失最优性的匹配。',
       'limitations': '真正可以删除这些分支的信息来自排序。把胃口 g 与饼干 s '
                      '都升序后，只看当前最小未满足孩子和当前最小未处理饼干：若这块饼干连最小胃口都不够，它对后面更大的胃口也永远无用，可以直接丢弃；若它已经够用，则总能通过交换把它分给当前最小胃口，而不减少最优方案能满足的孩子数，同时把更大的饼干留给更难满足的未来需求。'},
 494: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“0/1 背包计数：先用代数把正负号选择变成 LC-416 的子集和”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确算法是 DFS：来到 nums[i] 时分别尝试 +nums[i] 和 -nums[i]，处理完全部元素后若累计和等于 '
                           'target 就给答案加一。每个表达式对应一条唯一根到叶路径，因此算法完全正确，但会枚举 2^n 种符号选择。',
       'generalization': '设加正号的元素和为 P，加负号的元素和为 N：\n'
                         'P - N = target\n'
                         'P + N = total\n'
                         '两式相加得到 2P = total + target，因此 P=(total+target)/2。',
       'limitations': '可以先做记忆化，把状态压成 (i,currentSum)，因为走到同一 i '
                      '且累计和相同时，后面的选择完全相同。这已经消除了重复子问题。但这题还有更强的结构：所有 nums[i] '
                      '都是非负数，正负号实际上是在把下标分成两个集合。'},
 542: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“多源 BFS 最近距离：把所有目标同时放进第 0 层，让最近的波前自然获胜”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确做法，是对每个值为 1 的格子单独做 BFS，直到找到第一个 0。单个 BFS 最坏访问整个矩阵 '
                           'O(mn)，如果几乎所有格子都是 1，就会把同一片网格重复搜索很多次，最坏接近 O((mn)^2)。',
       'generalization': '把所有 0 先放到距离 0 层，相当于增加一个虚拟超级源，用 0 代价连到所有真实 0；普通 BFS 每向外一层距离加 '
                         '1。不同源的波前相遇时，无需比较第二次，因为首次到达已经是全体源中的最短距离。',
       'limitations': '观察所有这些独立 BFS 的目标完全相同：都在寻找“0 集合”。距离又是对称的——A 到 B 的四邻步数和 B 到 A 相同。因此无需让每个 1 '
                      '去寻找 0，可以让所有 0 同时向外传播。'},
 543: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“树形 DP 双角色：向父只能交一条臂，当前节点却可以把左右两臂拼成完整路径”。通用机制见 '
                               'docs/algorithms/tree-traversal.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个直接思路是枚举每个节点作为路径最高点，再分别计算它左、右子树的高度，用 leftHeight+rightHeight '
                           '更新直径。逻辑正确，但若每个节点都重新递归求高度，同一棵子树会被多个祖先重复计算，最坏 O(n^2)。',
       'generalization': '于是“求高度”和“枚举最高点”被融合到同一趟 DFS。',
       'limitations': '后序遍历本来就会先完成孩子再回到父亲，所以可以让每棵子树只计算一次高度，并在返回父节点前顺手上报这个高度；当前节点拿到左右高度时同时完成一次直径候选更新。'},
 547: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“并查集母模型：把连通分量压缩成可持续维护的代表元”。通用机制见 '
                               'docs/algorithms/union-find.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确解法，是直接把 isConnected 当成无向图：外层枚举城市，遇到未访问城市就 DFS/BFS，把所有可达城市标记 '
                           'visited，并让答案加一。因为输入本身就是 n×n 矩阵，读取邻接关系已经需要 O(n^2)，这个方案清晰而且完全合格。',
       'generalization': '所以这里先换一种表达方式：每个集合只保存一个代表根 parent；看到连接 i-j 时，不重新搜索整个分量，而是把两个根合并。`find(x)` '
                         '回答“x 当前属于哪个分量”，`unite(a,b)` 回答“把两个分量合成一个”。',
       'limitations': '为什么这一题还值得学习并查集？因为后面的 LC-684/685 '
                      '不再只是“最后数一次连通块”，而是边一条条到来时反复问：这两个点现在是否已经连通？如果每次都重新 DFS，会重复遍历旧图。'},
 560: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“前缀和 + 频次哈希表”。通用机制见 '
                               'docs/algorithms/prefix-sum-difference.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然的正确做法，是枚举每个 left，再向右扩展 right 并维护区间和；这样确实能检查所有连续子数组，但会产生 O(n^2) 个区间。',
       'generalization': '因此暴力真正重复做的不是“计算 k”，而是在大量历史起点里反复寻找这个已经确定的 oldPrefix。把历史前缀建立成 prefix -> '
                         'frequency 的索引后，就把每次重新扫描历史改成一次哈希查询。',
       'limitations': '换成“固定右端点”来看：扫描到当前位置时，当前前缀和 prefix 已经确定。若某个历史位置之后到当前的区间和为 '
                      'k，那么那个历史前缀也被唯一确定：oldPrefix = prefix-k。'},
 621: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“频率骨架：由最拥挤任务给出调度长度下界”。通用机制见 '
                               'docs/algorithms/greedy.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个直接正确的算法是逐个时间单位模拟：维护每类任务剩余次数和下一次允许执行时间；每一时刻从当前可执行任务里挑剩余次数最多者，否则 '
                           'idle。这个堆/冷却队列模拟能得到最短调度，但题目只有 26 种、每项耗时固定且冷却规则完全相同，实际保留了远多于答案需要的逐时刻状态。',
       'generalization': '与 Day 25 的 Top-K/边界维护不同，这里进一步问‘能否不模拟每一步，只保留决定最优值的紧约束’。',
       'limitations': '要问最短总长度，最紧的约束来自最高频任务。设最高频为 f：它的前 f-1 次出现后面都必须留出 n 个位置，所以形成 f-1 个长度至少 n+1 '
                      '的完整块；若有 c 种任务都达到频率 f，最后一列需要容纳这 c 个最高频任务，于是骨架下界为 (f-1)(n+1)+c。另一方面任何调度都至少要执行 '
                      'tasks.size() 个真实任务。其他任务若能填进骨架空位，就不会增加长度；若多到填满所有空位，它们本身把长度推到 '
                      'tasks.size()。因此答案正好是两者最大值。优化类型：从逐时刻状态模拟压缩为决定答案的频率下界。'},
 684: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“在线无向判环：加边前先问两端是否已经连通”。通用机制见 '
                               'docs/algorithms/union-find.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个自然且正确的在线算法是：维护目前已经加入的邻接表。处理新边 (u,v) 前，从 u 做一次 DFS/BFS；若能到 v，说明旧图中已有 '
                           'u->...->v 路径，新边会成环；否则把新边加入图继续。',
       'generalization': 'LC-547 的 DSU 正好把整个连通分量压缩成代表根：\n'
                         '- find(u) != find(v)：旧图不存在连接两端的路径，可以安全 union；\n'
                         '- find(u) == find(v)：旧图已经有路径，再加当前边必形成环。',
       'limitations': '问题在于每来一条边都可能重新遍历大量已经看过的节点和边。我们并不关心那条旧路径具体经过谁，只关心一个二值事实：u、v 是否已经连通。'},
 685: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“有向树修复：先分类二父冲突，再用 DSU 验证是否仍有环”。通用机制见 '
                               'docs/algorithms/union-find.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最直接的正确方法，是依次尝试删除每一条边；每次删除后重新检查剩余 n-1 '
                           '条边是否构成一棵有根树：恰好一个根、其余节点各一个父亲、所有节点可达且无环。这样容易理解，但每个候选都要重新验证整张图，最坏 O(n^2)。',
       'generalization': '于是候选从 n 条边缩成至多 2 条。把较晚的二父边 later 暂时跳过，再用 DSU 重放其余边：\n'
                         '- 若没有环，删 later 就已经恢复树；\n'
                         '- 若仍有环，说明 earlier 位于那个环中，必须删 earlier；\n'
                         '- 若根本没有二父，则直接返回 DSU 检出的成环边。',
       'limitations': '要减少重复验证，先利用“原始输入 = 一棵有根树 + 恰好一条额外边”这个强条件。额外边造成的破坏不可能任意复杂，只会落入：\n'
                      '1. 没有节点二父：那唯一问题就是一个环，退化成 LC-684；\n'
                      '2. 某节点有两个父亲：真正答案只可能是指向这个节点的两条入边之一。'},
 695: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“连通分量聚合：复用 LC-200 的 BFS，只改变分量内统计量”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '这题不需要学习第二套岛屿算法。LC-200 已经能准确找到一个连通分量的全部格子；现在只是在一次 BFS 内把访问到的格子数累计成 '
                           'area，并在一个分量结束后用它更新 best。遍历结构不变，变化的只是分量聚合规则。\n'
                           '\n'
                           '按实现顺序可以直接写成：\n'
                           '1. 扫描到新的 1 时令 area=0，并像 LC-200 一样入队且立即把它改为 0\n'
                           '2. 每弹出一个格子就执行 ++area；这个格子只会弹出一次，因此每块陆地只贡献一次面积\n'
                           '3. 继续四方向扩展，把同一岛的所有陆地都染成 0 并加入队列\n'
                           '4. 队列清空时当前连通分量已经完整遍历，用 best=max(best,area) 更新答案\n'
                           '5. 继续外层扫描；若全图没有陆地，best 保持 0',
       'generalization': '掌握的是“遍历框架与聚合逻辑分离”：同一个连通分量 BFS 可以统计节点数、边界、颜色频次、周长或包围盒。',
       'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：不要在发现邻居和出队时同时计数，否则会重复。'},
 704: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“标准二分：把一次比较升级成整段候选淘汰”。通用机制见 '
                               'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最自然且正确的算法是从左到右逐个比较 nums[i] 与 target；找到就返回 i，否则扫描结束返回 '
                           '-1。它不会漏，因为每个元素都检查了，但最坏需要 O(n) 次比较。',
       'generalization': '因此一次比较不再只排除一个元素，而是排除至少约一半候选。只要维护‘答案若存在一定仍在当前候选区间’这个不变量，每轮把不可能的一半永久丢掉，候选规模 '
                         'n -> n/2 -> n/4 -> ...，最终就是 O(log n) 的二分查找。',
       'limitations': '真正可以省掉的工作来自数组已经严格升序。若 nums[mid] < target，那么 left..mid 中每个值都 <= nums[mid] < '
                      'target，这整段无需再逐个检查；若 nums[mid] > target，mid..right 同理可以整体淘汰。'},
 746: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“路径 DP：复用 LC-70 的前两状态，把计数改成最小累计代价”。通用机制见 '
                               'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '自然的正确做法是递归求“到达位置 i 的最低成本”：最后一步只可能从 i-1 或 i-2 '
                           '来，因此分别递归求这两个位置的最低成本，再加上离开对应台阶的费用，最后取 min。',
       'generalization': '所以沿用 LC-70 的优化：每个位置最低成本只算一次。再观察 dp[i] 仍然只依赖 dp[i-1]、dp[i-2]，整张表可以继续压成 '
                         'previous1/previous2。',
       'limitations': '和 LC-70 一样，直接递归会重复计算同一个 minCost(i)。例如求顶部时会递归到 n-1、n-2，而 n-1 的递归内部又会再次求 '
                      'n-2。重复子问题完全相同，只是聚合操作从加法变成了 min。'},
 875: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“答案二分：把最小吃速转成速度轴上的第一个可行点”。通用机制见 '
                               'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个自然且正确的做法是按 speed=1,2,3,... 逐个尝试，计算每个速度需要的总小时数，第一次满足 hours<=h '
                           '就返回。因为速度按升序枚举，所以得到的一定是最小可行速度；但 piles[i] 可以到 10^9，逐个试速度会浪费巨大范围。',
       'generalization': '所以速度候选再次形成 false...false | true...true。和 LC-1011 相比，二分边界模板完全不用改，只需替换 '
                         'check(speed) 的业务含义。优化类型：利用答案可行性的单调性，跳过大量无需逐个测试的速度。',
       'limitations': '关键观察是：每堆所需小时 ceil(pile/speed) 随 speed 增大单调不增，因此总小时数也单调不增。如果某个 speed 已能在 h '
                      '小时内完成，那么任何更快速度都可行；如果它超时，那么更慢速度一定也超时。'},
 990: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“等价类约束：先合并所有 ==，再验证 != 是否跨集合”。通用机制见 '
                               'docs/algorithms/union-find.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '一个自然解法，是先根据所有 `==` 建无向图，再对 26 个变量做 DFS/BFS，给每个连通分量编号；第二遍检查 `a!=b` '
                           '是否落在同一编号。这个方案完全正确，而且因为变量只有 26 个，性能也足够好。',
       'generalization': '为什么必须先处理全部等式？因为输入顺序不代表逻辑推导顺序。例如先看到 `a!=c` 时，后面可能还有 '
                         '`a==b`、`b==c`；只有把等式传递闭包全部建立后，才能判断不等式是否矛盾。',
       'limitations': '并查集在这里仍然不是为了制造虚假的数量级优势，而是因为问题本质就是“不断声明两个对象等价，并反复查询它们是否属于同一等价类”。这正是 DSU '
                      '的原生语义：\n'
                      '`a==b` -> unite(a,b)\n'
                      '`a!=b` -> find(a) != find(b)'},
 994: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“多源 BFS 时间传播：同一层代表同一分钟，状态改变时立即入下一层”。通用机制见 '
                               'docs/algorithms/graph-search-toposort.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
       'general_solution': '最贴近题意的直接模拟，是每一分钟都重新扫描整张网格：找到当前腐烂橘子附近的新鲜橘子，先记录本分钟将腐烂的位置，扫描结束后统一更新，再进入下一分钟。它正确，但每分钟都要 '
                           'O(mn) 全表扫描；传播最长又可能持续 O(mn) 分钟，因此最坏 O((mn)^2)。',
       'generalization': '队列正好保存这个活动前沿。把所有初始腐烂橘子同时作为第 0 层；固定当前 layer '
                         '大小时，这一层产生的新腐烂橘子只入队，不在本层继续传播，下一轮再处理，于是队列层边界天然实现了“同步一分钟”。',
       'limitations': '浪费在于：绝大多数格子每一分钟都被重新检查，即使它与当前传播边界毫无关系。真正能在下一分钟制造新变化的，只有“这一分钟刚刚处于腐烂前沿”的那些橘子。'},
 1011: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“答案二分：把最小运载能力改写成第一个可行容量”。通用机制见 '
                                'docs/algorithms/binary-search.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
        'general_solution': '一个真实且正确的直接做法是从最小可能容量 max(weights) 开始逐个尝试 '
                            'capacity=maxWeight,maxWeight+1,...，对每个容量都顺序模拟装船；第一次能在 days '
                            '天内完成的容量就是答案。因为容量按从小到大枚举，所以它一定不会漏掉更小答案。问题是容量范围最坏可以延伸到 '
                            'sum(weights)，每个候选又要 O(n) 模拟，总工作可能接近 O(n*S)。',
        'generalization': '这正是 Day 22 / LC-34 的边界二分，只不过“下标 i 是否满足 nums[i]>=target”换成了“容量 C '
                          '是否能按时运完”。因此不再线性枚举容量，而是二分寻找第一个 true。优化类型：利用可行性的单调性，一次判定淘汰整段答案候选。',
        'limitations': '真正可以省掉的是对大量连续容量的逐个尝试。固定一个 capacity 后，若它已经能在 days '
                       '天内完成，那么任何更大的容量也一定能完成；若它不能完成，那么任何更小的容量也不可能突然变得可行。于是所有候选容量天然分成：\n'
                       'false false ... false | true true ... true。'},
 1109: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“差分数组：区间更新转端点更新”。通用机制见 '
                                'docs/algorithms/prefix-sum-difference.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
        'general_solution': '先从最直观的模拟出发：建立 answer[1..n]，每条 booking=[first,last,seats] 就把 '
                            'first..last 每个航班都 '
                            '+=seats。差分没有改变这件事的含义，它只是把一段连续、完全相同的重复写入压缩成两个“状态变化事件”。真正让这种压缩成立的是最后那次从左到右的累积：running '
                            '会继承上一个位置的值，因此一次 +seats 可以自动持续影响后续位置，直到遇到对应的 -seats 才把这份状态撤销。\n'
                            '\n'
                            '按实现顺序可以直接写成：\n'
                            '1. 先写出朴素模型：answer[flight-1]+=seats，flight 从 first 走到 '
                            'last；确认题目本质就是连续区间逐点加\n'
                            '2. 观察重复工作：同一条 booking 在整个区间内不断执行完全相同的 +=seats\n'
                            '3. 把 diff 看成事件表，而不是答案表：它只记录当前位置要让当前状态改变多少\n'
                            '4. 在 first 对应位置放 +seats，把这份贡献加入 running；从这里开始状态生效\n'
                            '5. 中间位置如果 diff=0，就执行 running+=0；由于 running 保留前值，这份贡献自然持续，不需要再次 '
                            '+=seats\n'
                            '6. 在 last 后面的边界放 -seats，把之前加入 running 的同一份贡献移除；从这里开始状态失效\n'
                            '7. 多条 booking 的状态可以同时存在，running 始终等于当前位置所有仍处于 active 状态的 seats 之和\n'
                            '8. 最后把每个真实航班位置的 running 写进 answer；额外 diff 槽位只用于承接结束事件',
        'generalization': '这其实是一类通用的“事件驱动 + 累积状态”技巧：公交上车时人数加入当前人数、下车后移除；会议开始时 active meeting '
                          '+1、结束时 -1；扫描线进入区间时加入贡献、离开时删除贡献。',
        'limitations': '这道题的一般性逻辑本身已经接近目标复杂度，没有必要为了形式统一而人为制造更慢的“暴力解”。这里真正需要约束的是状态定义、边界和更新顺序：不要把 '
                       'diff 当成每个航班最终的座位数，也不要把 -seats 理解成业务上的负座位。'},
 1143: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“双序列 DP：两个前缀形成二维网格，用 diagonal '
                                '保存被一维覆盖的左上角”。通用机制见 '
                                'docs/algorithms/dynamic-programming.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
        'general_solution': '自然的正确递归是从两个字符串末尾开始：\n'
                            '- 若末字符相等，就把这个匹配计入答案，然后递归处理两个更短前缀；\n'
                            '- 若末字符不同，就分别尝试丢掉 text1 的末字符或 text2 的末字符，取两种结果较大者。',
        'generalization': '把每个前缀对只算一次，就得到二维 DP：共有 (m+1)*(n+1) 个状态，时间 O(m*n)。',
        'limitations': '它正确，但不同分支会反复询问相同的前缀对 (i,j)。例如一次先删 text1 字符、下一层再删 text2，与先删 text2、再删 '
                       'text1，都会走到同一个 (i-1,j-1) 子问题。直接递归因此会产生大量重复子问题。'},
 1248: {'algorithm_background': '到这里才需要给这套规律命名：本题对应“奇偶映射 + 前缀和 + 频次哈希表（复用 LC-560）”。通用机制见 '
                                'docs/algorithms/prefix-sum-difference.md；回到本题时只保留与当前题意直接相关的状态、边界和更新顺序。',
        'general_solution': '最自然的做法，是枚举每个子数组并数里面有多少个奇数；即使向右扩展时复用当前 oddCount，仍然要检查 O(n^2) 个连续区间。',
        'generalization': '问题到这里已经完整变成 LC-560：固定当前前缀 prefix 后，需要寻找的历史状态唯一是 '
                          'prefix-k，于是把枚举所有起点改成一次历史频次查询。',
        'limitations': '真正值得先做的不是立刻发明一个新窗口公式，而是把题目里的“奇数个数”翻译成可累加数值：奇数记 1，偶数记 0。这样一个区间“恰好有 k '
                       '个奇数”就等价于这个 0/1 区间“元素和恰好为 k”。'}}
