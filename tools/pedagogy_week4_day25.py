"""High-touch pedagogy overrides for Week 4 / Day 25.

Day 25 studies selection/frontier data structures: keep only the candidates the
future can still distinguish, and make the heap top the next boundary that must
be inspected or evicted.
"""

PEDAGOGY_WEEK4_DAY25 = {
    215: {
        "pattern": "顺序统计量：从完整排序收缩到只定位第 k 大",
        "model": "如果把数组完全升序排序，第 k 大就是下标 n-k。但题目只问这个位置的值，并不关心其余元素内部顺序，因此完整排序做了大量答案根本不会读取的工作。nth_element / Quickselect 只反复保留仍可能覆盖目标下标的分区。",
        "derivation": """最自然的正确做法是完整排序：sort(nums.begin(), nums.end()) 后返回 nums[n-k]，时间 O(n log n)。它正确，但真正需要的信息只有一个顺序统计量；排序额外确定了其他 n-1 个元素之间的大量相对次序。

把目标写成升序下标 target=n-k。一次 partition 以后，pivot 已经落到最终排名位置：若 pivotIndex==target 就结束；若 target 在左边，右分区的内部顺序以后永远无关，反之亦然。于是每轮都丢掉一整个不可能包含目标排名的分区，只继续处理一侧，这就是 Quickselect。标准库 nth_element 提供同一类选择语义：目标位置变成完整排序后应有的值，但两侧内部无需排序。优化类型：候选淘汰 + 避免建立无用全序。""",
        "visual": """nums = [3,2,1,5,6,4], k=2
升序目标下标：n-k = 4

完整排序会得到：
1 2 3 4 [5] 6
          ^ 只真正关心这里

选择算法的目标不是把整行排好，而是做到：
[ <= 5 的若干元素 ] [5] [ >= 5 的若干元素 ]
                      ^ target

左右内部顺序可以完全乱，因为答案不会读取它们。""",
        "core": "只定位第 k 大所在的排名位置，不为答案永远不会读取的其他元素建立完整排序。",
        "formula": """升序视角：
targetIndex = n - k

nth_element(first, first + targetIndex, last) 结束后：
nums[targetIndex] 等于完整升序排序后该位置的值；
其左侧元素不大于它，右侧元素不小于它，但两侧内部不保证有序。

Quickselect 不变量：目标排名始终位于当前仍被保留的分区内。""",
        "steps": [
            "把‘第 k 大’转换为升序中的 targetIndex=n-k",
            "调用 nth_element，只要求 targetIndex 对应元素就位，而不是完整排序",
            "直接返回 nums[targetIndex]；不要依赖左右两侧已经有序",
        ],
        "proof": "nth_element 保证目标迭代器处的元素与完整排序后的同位置元素相同，因此它就是升序第 n-k 小、也就是第 k 大。选择算法只丢弃排名区间不包含 targetIndex 的一侧，所以不会漏掉目标；重复值按出现次数参与排名，正好符合题意。",
        "pitfalls": "题目要第 k 个元素，不是第 k 个不同值。nth_element 会修改 nums。不要把 nth_element 误认为 sort：目标两侧只有分区关系，没有内部排序保证。当前实现依赖 C++ 标准库合法的 C++17 API；若要求手写，可实现随机化 Quickselect。",
        "transfer": "与 LC-347 的 Top-K 对照：静态数组只要一个排名时可用 Quickselect/nth_element；数据流或只愿保留 k 个候选时常用大小为 k 的堆。核心都不是‘堆模板’，而是拒绝维护答案不需要的完整全序。",
        "memory": "只问一个排名，就只把那个排名放对；别替答案排序整个世界。",
    },

    23: {
        "pattern": "k 路归并：最小堆只维护每条有序链的当前 frontier",
        "model": "每条链表内部已经升序，所以一条链未来最小的未输出节点永远是它当前链头。全局下一个元素不可能藏在某条链的第二、第三个节点后面；因此每条链只暴露一个 frontier，最小堆只在最多 k 个 frontier 中选最小者。",
        "derivation": """一个直接正确做法是反复扫描 k 条链表的当前头，每次找出最小头并输出，再推进那一条链。因为每条链内部有序，它不会选错；但每输出一个节点都重新线性扫描 k 个候选，总时间 O(Nk)。

重复工作恰好是‘在几乎相同的一组链头中再次找最小值’。每次输出后，只有一条链的候选发生变化，其余 k-1 个头完全没变。于是需要一个支持‘取最小 + 删除最小 + 插入一个新候选’的数据结构；最小堆把每次选择从 O(k) 降到 O(log k)。重要的是堆里不放所有剩余节点，只放每条链当前最小未输出节点。优化类型：重复查找消除 + frontier 压缩。""",
        "visual": """L1: 1 -> 4 -> 5
L2: 1 -> 3 -> 4
L3: 2 -> 6

堆里只放当前头：
        1(L1)
       /     \
    1(L2)   2(L3)

弹出 1(L1) 后，只有 L1 frontier 改变：
L1 暴露 4；L2 仍是 1；L3 仍是 2。

不需要把 5、3、4、6 提前全部放进堆，
因为它们前面各自还有更小的同链节点挡着。""",
        "core": "每条有序链只贡献一个当前最小候选；弹出哪条链的头，就只把那条链的下一个节点补进堆。",
        "formula": """设 heap 保存所有尚未耗尽链表的当前头。
不变量：对每条链，heap 中若有该链节点，它就是这条链最小的未输出节点。

因此：
heap.top() = 所有剩余节点中的全局最小值。

每个节点至多入堆一次、出堆一次；heap 大小 <= k：
时间 O(N log k)，额外堆空间 O(k)。""",
        "steps": [
            "把所有非空链表的首节点放入最小堆，建立每条链的 frontier",
            "反复弹出堆顶，把它接到结果链表尾部；此时它就是全局最小未输出节点",
            "若弹出节点还有 next，只把这个 next 入堆，作为该链新的 frontier",
            "堆为空时所有链都耗尽，返回 dummy.next",
        ],
        "proof": "任意时刻，对每条未耗尽链，当前头不大于该链后面的任何节点；因此所有剩余节点的全局最小值必然属于这些当前头之一，而堆顶又是所有当前头中的最小值，所以每次输出都正确。弹出后只推进对应链，立即恢复同一不变量；每个原节点只会被暴露和输出一次，不会漏也不会重。",
        "pitfalls": "比较器必须比较 node->val。堆中允许不同链出现相同值。现有实现复用原节点，不额外 new 合并节点；空 lists 或全空链自然返回 nullptr。不要一次把所有节点入堆，那会把空间从 O(k) 放大到 O(N)，也丢掉有序链 frontier 的结构优势。",
        "transfer": "这是 merge two sorted lists 的 k 路版，也是外部归并排序、多个有序日志流、多个有序迭代器合并的通用模型：每个来源只暴露当前最小 frontier，再用堆仲裁全局下一个。",
        "memory": "每路只露一个头；谁被取走，只补谁的下一个。",
    },

    295: {
        "pattern": "双堆动态分割：持续维护有序序列中间边界",
        "model": "若每次把所有历史数据完整排序，中位数只读取正中间一个或两个值。把有序序列想成从中间切一刀：lower 保存较小一半且堆顶是左半最大值，upper 保存较大一半且堆顶是右半最小值。只要顺序边界和两边大小始终平衡，中位数就在两个堆顶。",
        "derivation": """直接算法可以把新数加入数组，每次 findMedian 时排序后读取中间位置；若查询频繁，就会反复重排大量旧数据。即使始终维护一个有序 vector，插入仍可能搬动 O(n) 个元素。

真正需要长期维护的不是完整顺序，而是中间分割线：左边有多少元素、右边有多少元素，以及分割线两侧的最大/最小边界。最大堆能 O(1) 看到左半最大值，最小堆能 O(1) 看到右半最小值，插入与跨边界搬运都是 O(log n)。于是把完整动态有序表压成两个‘只暴露边界’的集合。优化类型：状态压缩 + 动态边界维护。""",
        "visual": """当前数据排序后：
1 2 3 | 7 9
lower   upper

lower=max-heap：top=3
upper=min-heap：top=7

加入 4：
先进入 upper：1 2 3 | 4 7 9
此时 upper 比 lower 多，需要把 upper 最小值 4 搬到 lower：
1 2 3 4 | 7 9

现在 lower 多一个，奇数总数的中位数就是 lower.top()=4。

关键不是维护两边内部排序，
而是维护‘左边都 <= 右边’和大小差至多 1。""",
        "core": "把数据流持续切成大小平衡的左右两半，只维护分割线两侧的极值；中位数始终由堆顶直接给出。",
        "formula": """顺序不变量：
forall x in lower, y in upper: x <= y

大小不变量：
lower.size() == upper.size()
或
lower.size() == upper.size() + 1

因此：
总数奇数：median = lower.top()
总数偶数：median = (lower.top() + upper.top()) / 2

插入后若大小失衡，只搬运较大一侧的边界元素即可恢复平衡。""",
        "steps": [
            "新数 <= lower.top() 时进入 lower，否则进入 upper；lower 为空时直接进入 lower",
            "若 lower 比 upper 多超过 1，把 lower.top() 移到 upper；若 upper 更大，把 upper.top() 移到 lower",
            "查询时，lower 多一个就返回 lower.top()；大小相等就返回两个堆顶的平均值",
        ],
        "proof": "插入时按 lower.top() 分边，结合插入前的顺序不变量，新元素不会破坏‘左不大于右’。若某侧元素过多，只把该侧最靠近分割线的边界元素移到另一侧：lower.top() 是左侧最大值，upper.top() 是右侧最小值，因此搬运后顺序关系仍成立。大小恢复后，中间排名恰落在一个或两个堆顶上，所以查询不会漏掉真正中位数。",
        "pitfalls": "findMedian 按题意只在至少一个元素时调用。偶数时两个 int 相加可能溢出，现有实现先转 long long。必须固定并始终遵守‘lower 可以比 upper 多一个’的大小约定；若换约定，查询逻辑也必须一起换。",
        "transfer": "与 LC-23 一样，堆只暴露当前最关键边界；区别是 LC-23 维护多个来源的最小 frontier，LC-295 维护一个动态全体的中间分割线。滑动窗口中位数会在双堆上再加入延迟删除。",
        "memory": "左半看最大，右半看最小；顺序对、数量平，中位数就在堆顶。",
    },

    347: {
        "pattern": "频次哈希 + 大小为 k 的最小堆：只保留当前最强 Top-K",
        "model": "先把原数组压缩成 value->frequency。若把所有不同元素按频率完整排序，会确定很多答案不需要的次序。只保留 k 个最强候选时，最需要随时知道的其实是‘这 k 个里面谁最弱’，因为新候选来了以后，若需要淘汰一个，必然淘汰当前最弱者；所以用最小堆让最弱者位于堆顶。",
        "derivation": """自然正确做法是哈希统计频率后，把所有 distinct 元素按 frequency 排序，再取前 k 个。若不同元素数为 m，排序成本 O(m log m)，但答案只需要 Top-K 集合，Top-K 内部也无需排序。

扫描每个 (value,frequency) 时，假设已经保留当前见过元素中的最强 k 个。把新候选临时放进来后只有 k+1 个候选；其中频率最小的那个不可能属于这 k+1 项的 Top-K，而且以后其他旧元素的频率已经固定，它也没有机会‘后来变强’，因此可以永久删除。于是始终只保存 k 个候选，用最小堆 O(log k) 找到并淘汰最弱者。优化类型：候选支配 + 避免完整排序。""",
        "visual": """频次：
A:5  B:2  C:4  D:1，k=2

处理 A：heap=[5]
处理 B：heap=[2,5]
处理 C：临时 [2,5,4]
             ^ 最弱=2，弹掉 B
         heap=[4,5]
处理 D：临时 [1,4,5]
             ^ 弹掉 1

最终只剩频率 4、5 的两个元素。

为什么用最小堆而不是最大堆？
因为我们保留的是强者，反复需要快速删除的是‘保留集合中的最弱者’。""",
        "core": "先统计频率，再让大小为 k 的最小堆充当 Top-K 门槛：新候选进来，超过 k 就立刻淘汰当前最弱者。",
        "formula": """frequency[value] = value 的总出现次数

处理任意若干 distinct 元素后：
heap = 这些已处理元素中频率最高的 min(k, seen) 个候选。

对新候选 push 后若 size>k：
pop 最小频率项。

因此最终 heap 恰为全体 Top-K。
时间：统计 O(n)，堆处理 O(m log k)，m=distinct 数；空间 O(m+k)。""",
        "steps": [
            "一次扫描 nums，用 unordered_map 统计每个 value 的最终 frequency",
            "遍历频次表，把 {frequency,value} 放进最小堆",
            "每次入堆后若大小超过 k，立即 pop；堆顶就是当前保留集合中最弱、最该淘汰的候选",
            "扫描结束后弹出堆中 k 个 value 作为答案；题目允许任意顺序",
        ],
        "proof": "归纳假设处理前 heap 已保存此前元素的 Top-K。加入新元素后，真正需要考虑的候选只有旧 Top-K 加新元素：任何此前已被淘汰元素都不强于旧 Top-K 门槛，频率又已经固定，不可能重新反超。对这至多 k+1 个候选删除最弱者后，剩余恰是新前缀的 Top-K。重复到结束即可得到全局答案，不漏也不重。",
        "pitfalls": "先完成频率统计再做 Top-K，因为扫描原数组期间 frequency 还会变化。答案顺序任意。priority_queue<pair<int,int>,...,greater<...>> 会在频率相同后继续比较 value，但题目保证 Top-K 集合唯一，因此不影响合法性。结构化绑定和 greater<pair<...>> 都是 C++17 合法能力。",
        "transfer": "与 LC-215 对照选择工具：只要第 k 大一个静态排名，可用 Quickselect；要保留 Top-K 集合且 k 小，用大小 k 的堆；频率范围较小时还可桶排序。选结构前先问：我真正需要维护完整顺序、一个排名，还是只有 k 个强候选？",
        "memory": "要留最强 k 个，就让最弱那个站在堆顶，随时准备被淘汰。",
    },
}
