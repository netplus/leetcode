"""High-touch pedagogy overrides for Week 1 Day 7."""

PEDAGOGY_WEEK1_DAY7 = {
    239: {
        "visual": """先把窗口想成一个不断向右移动的观察框。真正的问题不是“窗口里有哪些数”，而是：\n\n哪些数还有资格在未来某一刻成为最大值？\n\n例如 nums = [1,3,-1,-3,5]，k = 3。\n\n看到 1：\n候选队列（存下标，对应值）：[1]\n\n看到 3：\n1 比 3 小，而且 1 还更早离开窗口。\n只要 3 还在窗口里，1 永远不可能压过 3；等 3 离开时，1 早就先离开了。\n所以 1 可以永久淘汰：\n[1] -> [] -> [3]\n\n看到 -1：\n3 > -1，二者都保留：\n[3, -1]\n队首 3 就是当前窗口最大值。\n\n后来看到 5：\n队尾 -3、-1、3 都 <= 5，并且都比 5 更早过期，因此全部失去未来竞争资格：\n[3,-1,-3] -> [] -> [5]\n\n单调队列不是保存窗口本身，而是在保存一条“仍有机会成为最大值”的候选名单。""",
        "core": "新元素进入时，把队尾所有“更小且更早过期”的候选永久淘汰；队首始终是当前窗口中最强且仍未过期的最大值候选。",
        "formula": """deque 存下标 i0,i1,...,im，并始终满足：\n\ni0 < i1 < ... < im                  （时间顺序）\nnums[i0] > nums[i1] > ... > nums[im] （值严格递减）\n\n当前扫描位置为 i，窗口左边界为 i-k+1。\n\n过期条件：\ncandidates.front() < i-k+1\n等价于：\ncandidates.front() <= i-k\n\n新元素 i 入队前：\nwhile nums[candidates.back()] <= nums[i]:\n    pop_back()\n\n窗口形成后：\nmax(window) = nums[candidates.front()]\n\n核心支配关系：旧下标 j < i 且 nums[j] <= nums[i] 时，j 比 i 更小/相等，又更早过期，所以 j 永远不可能在 i 之前重新成为最大值。""",
        "steps": [
            "扫描到下标 i 时，先从队首删除已经离开当前窗口的下标",
            "从队尾反复删除值 <= nums[i] 的旧候选，因为它们已经被更晚、更大的 nums[i] 支配",
            "把当前下标 i 压入队尾，此时队列重新保持下标递增、对应值严格递减",
            "当 i >= k-1，窗口已经形成，队首对应值就是这一窗的最大值",
        ],
        "proof": "队首为什么不会漏掉真正最大值：窗口内任何比队首更早的元素都已过期；任何仍在队首后面的候选值都更小，所以队首必为最大值。队尾删除为什么安全：若旧元素 j 被新元素 i 删除，则 nums[j] <= nums[i] 且 j<i；从此直到 j 过期前，i 都与 j 同窗且不小于 j，而 j 还会更早过期，因此 j 再也不可能成为窗口最大值。每个下标只入队一次，并且至多从队首或队尾删除一次，所以不会重复处理成 O(n^2)。",
        "pitfalls": "队列必须存下标而不能只存值，否则无法判断元素何时滑出窗口。重复值使用 <= 淘汰旧下标更自然：保留更晚的相同值可以活得更久；使用 < 也能设计正确版本，但队列会保留重复候选。窗口答案从 i==k-1 才开始输出。先处理过期还是先处理队尾支配都可以构造正确版本，但代码中的不变量要前后一致。",
        "transfer": "它和此前滑动窗口的区别很重要：LC-209/76/438 的 deque/指针是在维护“窗口是否合法”，LC-239 维护的是“窗口内部谁还有资格成为最优解”。这个“删掉被新状态完全支配的旧候选”会迁移到单调队列 DP、受长度限制的前缀最值，以及后续单调栈问题。",
        "memory": "队首管过期，队尾管淘汰；更小又更老，就永远出局。",
    },

    41: {
        "visual": """先忽略题目的大整数范围，只问：长度为 n 的数组，最小缺失正数可能在哪里？\n\n如果 1,2,...,n 全都出现了，答案就是 n+1；\n否则答案一定属于 1..n。\n\n于是数组本身刚好有 n 个槽位，可以把它当成一张不用额外空间的哈希表：\n\n值 1 应住下标 0\n值 2 应住下标 1\n值 3 应住下标 2\n...\n值 x 应住下标 x-1\n\n例如：[3,4,-1,1]\n\n下标:  0  1  2  3\n期望:  1  2  3  4\n当前: [3, 4,-1, 1]\n\n位置 0 的 3 应去位置 2：\n[-1,4,3,1]\n\n位置 1 的 4 应去位置 3：\n[-1,1,3,4]\n位置 1 现在是 1，又应去位置 0：\n[1,-1,3,4]\n\n最终扫描：\n位置 0 是 1，正确；\n位置 1 不是 2，所以 2 就是第一个缺失正数。\n\n0、负数、>n 的值都没有合法“住址”，直接留在原处即可。""",
        "core": "答案只可能在 1..n+1；把每个出现过的 x∈[1,n] 原地送到唯一槽位 x-1，最后第一个槽位和值不匹配的位置就是答案。",
        "formula": """n = nums.size()。\n\n候选答案范围：\nanswer ∈ {1,2,...,n,n+1}\n\n原地映射：\nx ∈ [1,n]  <=>  home(x) = x-1\n\n归位条件：\nnums[x-1] == x\n\n在位置 i 上，若当前 x=nums[i] 满足：\n1 <= x <= n\n且 nums[x-1] != x\n则交换：\nswap(nums[i], nums[x-1])\n\n最终：\n第一个 nums[i] != i+1  =>  answer=i+1\n若全部匹配             =>  answer=n+1\n\n循环中的 nums[x-1] != x 是重复值保险：目标槽已经放着 x 时，不再交换。""",
        "steps": [
            "令 n=nums.size()；遍历每个位置 i，把 nums[i] 看成一个可能需要搬家的值 x",
            "只要 x 落在 [1,n] 且自己的目标槽 x-1 还没有 x，就把 x 与目标槽中的值交换",
            "交换后不要立刻前进 i，因为换回来的新 nums[i] 也可能有自己的合法槽位；继续 while 归位",
            "所有位置处理后，从左到右寻找第一个 nums[i] != i+1，返回 i+1",
            "如果 1..n 全部归位，返回 n+1",
        ],
        "proof": "不会漏：任何出现过的 x∈[1,n]，只要它尚未归位，扫描到承载它的位置时都会把它送向 x-1；交换回来的值继续在同一个 i 上处理，因此不会因为一次交换跳过新值。重复值不会死循环，因为如果 x 的目标槽已经是 x，条件立即停止。最终若 nums[i]!=i+1，说明值 i+1 没能占据它唯一的槽位，也就说明输入中不存在 i+1；而更小的槽位都已经匹配，所以它正是最小缺失正数。每次真正交换至少让一个合法值进入自己的最终槽位，最多只有 n 个槽位，因此总交换次数 O(n)。",
        "pitfalls": "最关键的不是 for，而是里面必须用 while：一次交换后当前位置可能又来了一个需要继续归位的合法值。重复值必须检查 nums[nums[i]-1] != nums[i]，否则 [1,1] 一类输入会无限交换。不要处理 <=0 或 >n 的值，它们与答案槽位无关。该算法会修改原数组，这是换取 O(1) 额外空间的代价。",
        "transfer": "这和 Day 2 的 move-zeroes 都是“让元素回到它应该占据的位置”，但 LC-283 的目标位置由扫描顺序动态决定，LC-41 则由值 x 直接决定 home=x-1。看到“值域大小与数组长度同阶 + 要求 O(1) 空间”的缺失/重复问题，应优先想到数组本身能否充当哈希桶。",
        "memory": "1 住 0，2 住 1，x 住 x-1；先把住户送回家，再找第一个空错号。",
    },

    31: {
        "visual": """“下一个排列”不是随便找一个更大的排列，而是找刚刚大一点的那个。\n\n因此要遵守两个优先级：\n1. 尽量不要动高位；\n2. 如果必须增大某一位，增幅要尽量小；之后的低位再尽量小。\n\n看 [1, 3, 5, 4, 2]。\n\n从右往左看：5,4,2 是非递增后缀。\n这个后缀已经处于这些元素能形成的“最大排列”：\n[5,4,2]\n无论只重排它，都不可能得到更大的整体序列。\n\n所以必须动它左边第一位 3：\n[1, 3 | 5,4,2]\n    ^ pivot\n\n要让整体只增大一点，应从后缀里找“比 3 大的最小值”。\n由于后缀非递增，从最右往左遇到的第一个 >3 的就是 4：\n[1, 4 | 5,3,2]\n\n高位已经完成最小幅度增大。现在为了让整体尽可能小，把后缀排成最小顺序：\n[5,3,2] 原本仍是非递增，只需 reverse：\n[1,4,2,3,5]\n\n这就是紧邻的下一个字典序。""",
        "core": "从右找最右还能增大的 pivot，用后缀中刚好比它大的最小值替换，再把后缀降到最小排列；这样高位变化最晚、增幅最小。",
        "formula": """先找最长非递增后缀：\nnums[pivot+1] >= nums[pivot+2] >= ... >= nums[n-1]\n\npivot 是最右满足：\nnums[pivot] < nums[pivot+1]\n的位置。\n\n若 pivot 不存在：\n整个数组非递增 = 当前最大排列\nnext = reverse(all) = 最小排列\n\n若 pivot 存在，从右向左找第一个 successor 满足：\nnums[successor] > nums[pivot]\n\n由于后缀非递增，这个 successor 就是后缀中“最小的严格较大值”。\n交换 pivot/successor 后，后缀仍保持非递增结构，只需：\nreverse(pivot+1, end)\n即可得到该前缀下的最小后缀。""",
        "steps": [
            "从 n-2 向左寻找最右的 pivot，使 nums[pivot] < nums[pivot+1]；越过的部分就是最长非递增后缀",
            "若找到 pivot，从数组最右端向左找第一个 nums[successor] > nums[pivot] 的位置",
            "交换 pivot 与 successor，使最靠右的可变高位产生最小的严格增量",
            "反转 pivot 之后的整个后缀，把低位变成当前前缀下的最小排列",
            "若一开始没有 pivot，同一个 reverse 会直接把整个最大排列变回最小排列",
        ],
        "proof": "为什么最右 pivot 不会漏掉更近答案：pivot 右侧是非递增后缀，它已经是这些元素的最大字典序，只改后缀不可能再增大，所以想得到更大排列至少必须修改 pivot 或更左的位置；选择最右 pivot 比修改任何更左高位造成的变化更小。为什么 successor 要选刚好较大的值：在 pivot 固定需要增大的前提下，选择后缀中最小的严格较大值可让这一位增幅最小。最后把后缀变成升序最小排列，就排除了所有“同一新前缀下还能更小”的候选，因此得到的正是紧邻下一排列。",
        "pitfalls": "pivot 条件必须是严格 <；寻找 successor 也必须是严格 >，这样重复值才能正确处理，例如 [1,5,1]。后缀之所以可以 reverse 而无需 sort，是因为找到 pivot 时已经证明它原本非递增；交换最右的严格较大 successor 后，这一性质仍足够让反转得到非递减后缀。长度 1 时 pivot=-1，reverse 整段仍正确。",
        "transfer": "它延续了 Day 6 LC-179 的“从最终目标反推局部排序规则”，但这里不是给所有元素定义 comparator，而是利用字典序的高位支配性：先确定最右可改变高位，再让剩余后缀极小化。求上一排列时完全对称：找最右下降点、换成刚好更小值、后缀变最大。",
        "memory": "右边已经最大了才动左一位；换成刚大一点，再把右边降到最小。",
    },

    128: {
        "visual": """把数组里的数放到数轴上，不关心它们原来的下标：\n\nnums = [100,4,200,1,3,2]\n\n数轴上实际形成三段：\n1 -- 2 -- 3 -- 4        100        200\n^\n真正值得启动搜索的只有每一段最左端。\n\n如果从 2 开始向右数，会得到 2,3,4；\n从 3 又数 3,4；\n从 4 又数 4。\n这样同一条链会被重复扫描。\n\n怎样一眼认出链头？\n对 x 看 x-1：\n\nx=1：0 不存在 -> 1 是链头，开始向右扩展 1,2,3,4\nx=2：1 存在  -> 2 不是链头，跳过\nx=3：2 存在  -> 跳过\nx=4：3 存在  -> 跳过\n\n于是每条连续链只完整扫描一次。\n哈希集合负责 O(1) 地回答“邻居存在吗”，而 x-1 不存在这个条件负责消除重复起点。""",
        "core": "先用哈希集合抹掉原数组顺序，只从“没有前驱 x-1”的数启动一次向右扩展；这样每条连续链恰好扫描一次。",
        "formula": """values = set(nums)\n\nx 是连续段起点当且仅当：\nx ∈ values 且 x-1 ∉ values\n\n从起点 x 出发：\nlength = 1\nwhile x+length ∈ values:\n    length++\n\n等价地不断检查 current+1。\n\n关键不变量：\n只有连续段的最小值负责遍历整段；段内其余元素因为存在前驱而永不启动第二次扩展。\n\n因此若不同连续段长度为 L1,L2,...：\n总扩展次数 = L1 + L2 + ... <= |values| <= n。""",
        "steps": [
            "把 nums 全部放入 unordered_set，顺便消除重复值",
            "遍历集合中的每个 x；若 x-1 存在，说明 x 位于某条连续链内部，直接跳过",
            "只有 x-1 不存在时才把 x 当作链头，并从 x 开始反复查询 current+1",
            "记录这条连续链长度，并更新全局 best",
            "所有链头处理完后返回 best；空集合自然保持 0",
        ],
        "proof": "不会漏：任何非空连续段都有唯一最小值 s，而 s-1 不在集合中，所以 s 一定会启动扩展并沿 s+1,s+2... 访问完整段。不会重：段内任意 x>s 都有前驱 x-1，因此不会再次启动扫描。同一个集合元素至多作为某一条链扩展中的成员被访问一次，因此虽然代码外层 for 加内层 while 看起来像双层循环，总扩展工作量仍是 O(n)。",
        "pitfalls": "不要从每个元素都向右扩展，否则 [1,2,3,...,n] 会退化为 O(n^2)。set 的去重很重要，否则重复输入会产生无意义重复外层扫描。当前题面值域只有 ±1e9，本来不会触碰 int 边界；实现仍保留 INT_MIN/INT_MAX 防护，使代码对更宽输入也不会在 x-1/current+1 上产生有符号溢出。空数组答案为 0。",
        "transfer": "这与 Day 6 的区间扫描和本日 LC-239 有同一个优化思想：找到一个“支配/归属规则”，只保留真正有资格启动工作的候选。LC-239 删除被支配的最大值候选；LC-128 则让只有无前驱节点拥有一条链的扫描权。以后看到隐式链、连续编号段或哈希图连通块，可先问：能否定义唯一的 canonical start？",
        "memory": "有前驱就不是头；只从链头向右走，每条链只走一遍。",
    },
}
