"""High-touch pedagogy rewrites for Week 2 / Day 12."""

PEDAGOGY_WEEK2_DAY12 = {
    20: {
        "pattern": "栈保存未闭合结构：最近开启，最先关闭",
        "visual": """把扫描过程看成一叠还没关上的括号：

输入：([{}])

读 '('  -> [(]
读 '['  -> [(, []
读 '{'  -> [(, [, {]
读 '}'  -> [(, []      只能关掉最近的 '{'
读 ']'  -> [(]
读 ')'  -> []

栈顶永远代表“下一只右括号唯一允许关闭的左括号”。""",
        "core": "右括号不是去历史里随便找同类型左括号，而是必须关闭最近一个尚未闭合的左括号。",
        "formula": """扫描完任意前缀后：
stack = 这个前缀中尚未闭合的左括号，按开启顺序保存
stack.top() = 下一次最先允许被关闭的左括号

遇到右括号 close：
stack 非空 && stack.top() == match(close)
才合法，然后 pop。

扫描结束还要求 stack.empty()。""",
        "steps": [
            "遇到左括号就压栈，表示开启了一个尚未完成的嵌套结构",
            "遇到右括号先检查栈是否为空，再检查栈顶是否是与它匹配的左括号；不匹配立即失败",
            "匹配成功就弹出栈顶；全部扫描结束后，只有栈为空才说明所有开启结构都已闭合",
        ],
        "memory": "左括号负责“欠一笔闭合账”，右括号只能还最近的一笔。",
        "proof": """不会漏：每个左括号进入栈后一直保留到自己真正被关闭，因此任何未闭合结构都不会消失。
不会错配：合法嵌套要求后开启的结构先结束；若当前右括号连栈顶都不能匹配，就不可能绕过这个更近的未闭合结构去匹配更早的括号。
不会残留：最终栈非空说明仍有左括号没有右括号与之配对，所以必须判非法。""",
        "pitfalls": "不能只统计三种括号数量是否相等，`([)]` 数量完全相等但嵌套顺序错误；遇到右括号必须先判空再访问 top；扫描完后别忘记检查残余左括号。",
        "transfer": "这是 Day 10 迭代 DFS 中“栈保存暂停现场”的同一思想：栈保存尚未完成的上下文。后续表达式求值、路径回退、HTML/XML 标签匹配都在复用这个模型。",
    },

    155: {
        "pattern": "栈顶携带前缀摘要：把历史最小值一起压栈",
        "visual": """普通栈只记“这一层放了什么”；Min Stack 让每一层再带一张摘要卡：

push -2 : (-2, min=-2)
push  0 : ( 0, min=-2)
push -3 : (-3, min=-3)  <- top

pop 后：
          ( 0, min=-2)  <- top

无需重新扫描下面所有元素，上一层早已保存了当时的最小值。""",
        "core": "每次 push 都把“到这一层为止的最小值”一起保存，因此 pop 天然回滚，getMin 只读栈顶。",
        "formula": """设第 i 层实际值为 value[i]，摘要为 min[i]：

min[0] = value[0]
min[i] = min(value[i], min[i-1])

栈顶 pair = {当前值, 当前整个栈的最小值}

因此：
top()    = stack.top().value
getMin() = stack.top().minimum""",
        "steps": [
            "push(val) 时读取旧栈顶最小值，并计算 minimum = min(val, oldMinimum)；空栈时 minimum=val",
            "把 {val, minimum} 作为一个整体压栈；pop 时也整体弹出，不做任何重新计算",
            "top 读取 pair 的实际值字段，getMin 读取同一 pair 的最小值字段，二者都是 O(1)",
        ],
        "memory": "不要在查询时回头找答案；在状态进入栈时，就把未来查询需要的摘要一起存好。",
        "proof": """归纳看每一层：底层摘要显然就是底层值；若上一层摘要已经是此前所有元素的最小值，新层取 min(newValue, oldMin)，便得到包含新元素后的全局最小值。
pop 不需要“恢复”任何变量，因为弹掉当前层后，上一层保存的摘要本身就是删除该元素之前的正确状态。
重复最小值也不会丢：每一层都独立记录自己的前缀最小值。""",
        "pitfalls": "若只额外保存一个全局 minimum，弹出当前最小值后就不知道第二小是谁；辅助最小栈若只在严格变小时压入，则重复最小值需要额外计数，而 pair-per-layer 方案天然避开这个坑。",
        "transfer": "这和持久化/回滚思想很接近：每一层保存进入该状态后的摘要。把 minimum 换成 maximum、gcd、按位与等，就得到一系列“栈 + 可回滚聚合状态”结构。",
    },

    739: {
        "pattern": "单调递减栈：把向未来重复搜索改成未决问题一次结算",
        "visual": """先不要想“递减栈”，先看暴力算法在重复什么。

对每一天 day，最自然的做法是向右逐日扫描，直到找到第一个严格更暖的日期：

for each day:
    scan day+1, day+2, ...
    stop at first warmer day

不同 day 会反复扫过大量相同位置。换成从左到右后，只需要保存“已经出现、但答案还没有确定”的日期。

temperatures = [73, 74, 75, 71, 69, 72, 76]

扫描到 72 前，未决日期可能是：
下标       2    3    4
温度      75   71   69
           |    |    |
          等待  等待  等待

72 到来：
69 < 72 -> 69 第一次等到更暖日，答案确定，pop
71 < 72 -> 71 第一次等到更暖日，答案确定，pop
75 > 72 -> 75 仍然未决，停止

所以 pop 的原因不是“为了把栈排好序”，而是这个历史问题已经被当前元素解决。剩余未决元素自然呈现温度单调不增。""",
        "core": "栈保存的不是一组需要排序的温度，而是一组“还没找到右侧第一个更暖日”的历史问题；当前温度第一次能解决谁，就把谁结算并弹出。",
        "formula": """扫描到 day 前：
pending = 尚未找到右侧第一个严格更暖日期的历史下标
T[pending] 从栈底到栈顶单调不增

若：
T[day] > T[pending.top()]

则当前 day 就是栈顶日期的第一个更暖日：
previous = pending.top()
answer[previous] = day - previous
pop(previous)

相等不能弹，因为题目要求 strictly warmer。
默认存 index 而不是只存温度，因为答案需要位置差 day-previous。""",
        "steps": [
            "先从题意写出“每一天向右找第一个更暖日”的 O(n^2) 基线，并识别重复的向右搜索",
            "从左到右扫描，把尚未找到答案的历史下标保存在 pending；当前温度严格更高时，反复结算并弹出栈顶",
            "把当前 day 压栈，表示它从此也成为一个等待未来更暖日来结算的未决问题；扫描结束仍未决的位置保持答案 0",
        ],
        "memory": "先问“栈顶在等什么、为什么恰好现在能解决”，最后再观察栈是递增还是递减；单调性是未决候选被不断结算后的结果。",
        "proof": """为什么 day 是被弹日期的第一个更暖日？因为我们按时间从左到右扫描；若中间更早出现过严格更暖日，该下标已经在那个时刻被弹，不可能继续留到现在。
为什么可以连续弹？当前温度如果高于栈顶，就能解决这个最近的未决日期；弹掉后继续检查新的栈顶，直到剩余问题都不能由当前温度解决。
每个下标只入栈一次、出栈至多一次，所以所有 while 的总 pop 数不超过 n，整体是摊还 O(n)。""",
        "pitfalls": "必须存下标而非只存温度，因为答案是天数差；比较条件必须是 `>` 而不是 `>=`；不要把 pop 解释成单纯维护排序；也不要看到 for+while 就误判 O(n^2)，应按每个下标的完整生命周期做摊还分析。",
        "transfer": "LC-739 是单调栈第一层：pop 时确定一个右侧答案。LC-84 在此基础上升级为边界题：pop 时确定右侧阻挡位置，pop 后的新栈顶再提供左侧阻挡信息；再往后，子数组 min/max 贡献题会把左右边界距离乘起来统计贡献。",
    },

    84: {
        "pattern": "单调递增栈：从区间最小值模型推到左右边界",
        "visual": """这题不要从单调栈开始。先问：一个连续区间为什么能够形成某个矩形？

任意区间 [L,R]：
宽度 = R-L+1
高度 = min(heights[L..R])
area(L,R) = (R-L+1) * min(heights[L..R])

例如 heights = [2,1,5,6,2,3]，选区间 [2,3]：
高度由较矮的 5 决定，宽度为 2，所以面积是 5*2=10。

暴力枚举所有区间有 O(n^2) 个候选。更好的视角是反过来枚举“谁来当矩形的限制高度”：
固定 index=2, h=5，问它最多能向左右扩多远。

index:   0  1 | 2  3 | 4  5
height: [2, 1 | 5, 6 | 2, 3]
              ^ h=5

左边 index=1 的 1 < 5，不能进入矩形；
右边 index=4 的 2 < 5，也不能进入矩形；
中间 [2,3] 的高度都 >=5，所以可用范围是开区间 (1,4)。

若直接为每根柱子分别向左、向右扫描最近更矮柱，模型已经正确，但最坏仍是 O(n^2)。单调栈只是用来消除这些重复边界扫描。

扫描到 index=4, current=2 前，栈里的未决高度可为 [1,5,6]：
2 < 6 -> 高度 6 的右侧阻挡事件第一次出现，pop
2 < 5 -> 高度 5 的右侧阻挡事件第一次出现，pop
1 < 2 -> 停止

一个新的更矮柱可以一次结算多根历史柱，这才是单调栈进入本题的原因。""",
        "core": "先把“枚举矩形”改写成“每根柱子作为限制高度时能覆盖多宽”，再把反复寻找左右更矮边界的 O(n^2) 工作压缩成未决柱子的单调栈；栈是优化工具，不是题目的起点。",
        "formula": """概念模型：若 left/right 是目标高度两侧第一个不能进入矩形的更矮阻挡位置，则真实可用闭区间是：
[left+1, right-1]

由闭区间长度公式 end-start+1：
width
= (right-1) - (left+1) + 1
= right-left-1

所以 `-1` 不是需要背的技巧，而是因为左右两个阻挡柱本身不能进入矩形。

一趟扫描实现中，当 currentHeight < heights[stack.top()] 时弹出 mid：
rightBoundary = i
leftBoundary  = stack.empty() ? -1 : stack.top()
height        = heights[mid]
width         = i - leftBoundary - 1
area          = height * width

这里还必须区分“数学上的最近严格更矮边界”和“具体实现的等高归属策略”。当前代码使用严格 `>` 弹栈，因此等高柱可以共存；pop 后的 leftBoundary 可能与 height 相等，不一定是严格更矮柱。后一根等高柱可能先得到较窄区间，但更早的等高柱后续会覆盖更宽范围，因此全局最大面积仍不会遗漏。""",
        "steps": [
            "先从 area(L,R)=(R-L+1)*min(...) 建模，再把问题改写成：固定每根柱为限制高度，寻找它能覆盖的连续区间；这样 width 的含义先于单调栈建立",
            "识别 O(n^2) 基线慢在每根柱都重复寻找左右阻挡边界；从左到右扫描时，用栈保存“右侧终止边界尚未出现”的历史柱下标",
            "当前高度严格低于栈顶时反复 pop：当前 i 是该高度向右第一次遇到的更低事件；pop 后的新栈顶给出本实现的左侧阻挡位置，再由 `i-leftBoundary-1` 计算本次矩形",
            "扫描到 n 时把当前高度视作 0 的右哨兵，强制结算仍留在栈中的递增尾巴；若 pop 后栈空则用虚拟左边界 -1，使同一宽度公式继续成立",
        ],
        "memory": "先建模“某个高度能覆盖哪段区间”，再问“边界什么时候第一次被未来元素确定”；不要从 `while + pop` 模板倒推题意。",
        "proof": """右侧为什么正确：mid 直到 i 才因更低高度被弹，说明从 mid+1 到 i-1 从未出现过能触发它出栈的更低高度，因此 i 是本次右侧终止事件。
区间为什么可用：在 mid 被弹时，leftBoundary 之后到 i-1 的柱子都没有被当前高度淘汰掉，能够支撑本次 height；两侧阻挡位置本身不计入，因此宽度是 i-leftBoundary-1。
等高为什么不漏：严格 `>` 允许相等高度保留，所以某个较晚等高柱的左边界可能也是等高柱；它得到的区间可以较窄，但较早的同高柱会在更晚的下降事件中继续向左代表同一高度并得到不小于它的范围，故求全局 maximum 时不丢答案。
复杂度为什么 O(n)：每个真实下标只入栈一次、出栈一次；while 的所有 pop 合计 O(n)。""",
        "pitfalls": "最容易错的四点：一是还没理解区间模型就死记 `i-top-1`；二是把严格 `>` 实现中的 pop 后栈顶误说成永远“严格更矮”；三是忽略相等高度的归属策略；四是忘记末尾虚拟 0 / 左侧 -1 都是为了让边界复用主公式。栈要存 index，因为宽度来自位置差。",
        "transfer": "学习顺序建议固定为：LC-739 先理解“未决问题 + pop 时确定右侧答案”；LC-84 再理解“限制高度 + 左右边界 + width”；以后遇到 LC-907/LC-2104 一类贡献题，再把左右边界距离变成 `leftChoices*rightChoices`，并用一严一松的比较关系解决等值元素重复归属。",
    },
}
