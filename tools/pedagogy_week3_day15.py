"""High-touch pedagogy overrides for Week 3 / Day 15.

Day 15 establishes graph thinking from three representations:
- grid as an implicit graph (LC-200, LC-695),
- explicit object graph traversal with identity mapping (LC-133),
- directed dependency graph and topological elimination (LC-207).
"""

PEDAGOGY_WEEK3_DAY15 = {
    200: {
        "pattern": "网格连通分量：把二维格子看成隐式图，用 BFS 一次染完一个岛",
        "model": "不要把岛屿题当成特殊矩阵技巧。每个陆地格子就是一个图节点，上下左右的陆地之间存在边；题目要求的岛屿数量，就是这个隐式无向图中连通分量的数量。外层扫描负责发现一个尚未访问的新分量，BFS 负责把这个分量一次全部标记掉。",
        "visual": """例：\n\n1 1 0 0\n1 0 0 1\n0 0 1 1\n\n把每个 1 看成节点，只连上下左右：\n\n(0,0)--(0,1)        (1,3)\n  |                    |\n(1,0)                (2,3)--(2,2)\n\n外层扫描第一次碰到 (0,0)：islands++，然后 BFS 会一次吃掉左上整块。\n之后这些格子已经被染成 0，不会再启动 BFS。\n扫描到 (1,3) 时，才说明发现了第二个此前从未覆盖的连通分量。\n\n所以“答案加一”的时机不是每看到一个陆地，而是每发现一个尚未被旧 BFS 覆盖的陆地。""",
        "core": "扫描只负责发现新的连通分量；每发现一个未访问陆地就计数一次，再用 BFS 把与它四连通的整座岛全部标记。",
        "formula": """隐式图：\n节点 = grid[r][c] == '1' 的格子\n边 = 两个陆地格子上下左右相邻\n\n外层不变量：扫描到当前位置之前，所有已经发现过的岛都已被完整染色。\n\n发现 grid[r][c]=='1'：\nislands += 1\nBFS((r,c))\n\nBFS 入队不变量：一个格子一旦进入 pending，就立即把 grid 改为 '0'，因此它已经被声明为 visited，之后不会再次入队。""",
        "steps": [
            "从左到右、从上到下扫描所有格子；水域或已经染色的位置直接跳过",
            "第一次遇到仍为 '1' 的格子时，说明它不属于此前任何分量，因此 islands 加一",
            "把该格入队并立即改为 '0'；入队即标记，避免同一个格被多个邻居重复加入队列",
            "不断弹出队首，检查上下左右四个邻居；合法且仍为 '1' 的邻居立即染色并入队",
            "队列清空表示这一整座岛已处理完，外层扫描继续寻找下一座尚未访问的岛",
        ],
        "proof": "不会漏：任意岛屿至少有一个格子会被外层扫描遇到；如果它此前没被处理，该格仍为 '1'，于是会启动 BFS，而 BFS 会沿四邻边访问整个连通分量。不会重：第一次 BFS 已把该岛所有格子改为 '0'，后续扫描不可能再从同一岛启动第二次 BFS。",
        "pitfalls": "必须只使用四方向，不能把对角线算作连通。访问标记最好在入队时完成，而不是出队时，否则同一格可能被多个邻居重复入队。当前实现直接修改 grid 充当 visited；若调用方要求保留输入，应改用独立 visited。",
        "transfer": "这是 Week 3 的图论母模型：网格只是没有显式邻接表的图。LC-695 完全复用同一 BFS，只把“每个分量计 1”改成“分量内部累计面积”；Day 16 的多源 BFS 仍然沿用同一个 frontier，只改变起点集合和层的含义。",
        "memory": "外层找新岛，内层吃完整岛；入队即标记。",
    },

    695: {
        "pattern": "连通分量聚合：复用 LC-200 的 BFS，只改变分量内统计量",
        "model": "这题不需要学习第二套岛屿算法。LC-200 已经能准确找到一个连通分量的全部格子；现在只是在一次 BFS 内把访问到的格子数累计成 area，并在一个分量结束后用它更新 best。遍历结构不变，变化的只是分量聚合规则。",
        "visual": """LC-200 问：\n“我一共启动了几次 BFS？”\n\nLC-695 问：\n“每次 BFS 一共弹出了多少个陆地格？”\n\n例如某次 frontier：\n\nstart -> 1 -> 1\n         |\n         1 -> 1\n\n每弹出一个陆地：area++。\n队列清空时，area 就是这个连通分量的节点数，也就是岛屿面积。\n\n外层继续扫描下一分量，再令 area=0 重新统计。""",
        "core": "完全复用 LC-200 的“发现分量 + BFS 染色”；只在 BFS 内对每个访问格执行 area++，分量结束后更新最大值。",
        "formula": """对每个新分量 C：\narea(C) = BFS 过程中被访问的陆地格数量\nbest = max(best, area(C))\n\n不变量：\n- pending 与已经染色但尚未处理完的格子共同属于同一个岛；\n- area 等于当前岛中已经出队处理的格子数。""",
        "steps": [
            "扫描到新的 1 时令 area=0，并像 LC-200 一样入队且立即把它改为 0",
            "每弹出一个格子就执行 ++area；这个格子只会弹出一次，因此每块陆地只贡献一次面积",
            "继续四方向扩展，把同一岛的所有陆地都染成 0 并加入队列",
            "队列清空时当前连通分量已经完整遍历，用 best=max(best,area) 更新答案",
            "继续外层扫描；若全图没有陆地，best 保持 0",
        ],
        "proof": "LC-200 已保证一次 BFS 的访问集合恰好等于起点所在岛屿。这里每个被访问格只在出队时贡献一次 area，因此 area 恰好等于该岛格子数；对全部连通分量取最大即可得到最大岛屿面积。",
        "pitfalls": "不要在发现邻居和出队时同时计数，否则会重复。当前代码选择统一在出队时 ++area。和 LC-200 一样，入队即染色可阻止重复入队；当前实现会修改 grid。",
        "transfer": "掌握的是“遍历框架与聚合逻辑分离”：同一个连通分量 BFS 可以统计节点数、边界、颜色频次、周长或包围盒。以后看到网格新题，先判断连通关系是否变化，再判断只是分量内统计量变化。",
        "memory": "LC-200 找出整座岛；LC-695 只是给这座岛加一个 area 计数器。",
    },

    133: {
        "pattern": "图的深拷贝：遍历负责发现结构，old->new 映射负责保持对象身份",
        "model": "图深拷贝的难点不是 BFS，而是环和共享邻居：同一个原节点可能从多条边被再次遇到，但克隆图里必须始终对应同一个新对象。因此需要一张 original Node* -> clone Node* 的映射。它同时承担 visited 和对象身份索引两个角色。",
        "visual": """原图：\nA ----- B\n|       |\n+--- C -+\n\n如果从 A->B 创建一个 B'，又从 C->B 再创建一个 B''，得到的就不是原图的深拷贝，因为原图只有一个共享节点 B。\n\n所以第一次见到节点时就登记：\ncloneOf[A] = A'\ncloneOf[B] = B'\ncloneOf[C] = C'\n\n之后无论从哪条边再次遇到 B，都只引用同一个 cloneOf[B]。\n\n遍历一条原边 A -> B 时，克隆图只做：\nA'->neighbors.push_back(B')。""",
        "core": "每个原节点第一次出现时只创建一个克隆并登记映射；以后遍历原图边时，只用映射找到两端克隆并重建同样的邻接关系。",
        "formula": """映射不变量：\ncloneOf[x] 已存在 <=> 原节点 x 已被发现\ncloneOf[x] 是 x 唯一对应的克隆对象\n\n处理原边 original -> neighbor：\n若 neighbor 未登记：\n    cloneOf[neighbor] = new Node(neighbor->val)\n    neighbor 入队\n然后始终执行：\ncloneOf[original]->neighbors.push_back(cloneOf[neighbor])""",
        "steps": [
            "空输入直接返回 nullptr；否则先创建起点克隆，立刻写入 cloneOf，再把原起点入队",
            "BFS 每次弹出一个 original，遍历它的全部 neighbors",
            "若某 neighbor 尚未出现在 cloneOf，立即创建其克隆、登记映射并把原 neighbor 入队",
            "无论 neighbor 是否第一次出现，都把 cloneOf[neighbor] 接入 cloneOf[original] 的 neighbors",
            "遍历结束返回 cloneOf[node]；原图地址与克隆图地址完全独立，但拓扑关系保持一致",
        ],
        "proof": "每个原节点只在首次发现时创建一次克隆，因此不会因环或多条入边产生重复对象。每条原邻接关系在处理其起点邻接表时都会在对应克隆节点间重建，所以不会漏边；映射始终使用原节点指针作为身份键，确保共享关系保持不变。",
        "pitfalls": "关键是先登记映射再入队，否则环可能导致重复创建。不要把 node->val 当作通用对象身份；本题虽保证 val 唯一，但按 Node* 建映射才是可迁移的深拷贝模型。哈希表不是额外的 visited 再加一张 clone 表，而是一张表同时完成两件事。",
        "transfer": "它和带 random 指针链表、对象依赖图、AST/IR 克隆本质相同：先建立 old->new 身份映射，再按原引用关系重建新引用。LC-200 的 visited 只回答“见过没有”，本题的映射进一步回答“见过后对应哪个新对象”。",
        "memory": "图可以绕回来，所以不能见一次 new 一次；一个原对象永远映射到同一个新对象。",
    },

    207: {
        "pattern": "拓扑排序：把“先修依赖”变成有向边，反复删除当前入度为 0 的课程",
        "model": "课程表首先是一个有向图建模题：prerequisites=[a,b] 表示 b 必须先于 a，因此边必须是 b->a。能否完成全部课程，等价于依赖图是否无环。Kahn 算法不直接寻找环，而是不断拿走当前没有剩余前置依赖的节点；若最后仍有节点拿不走，剩余部分就被环互相卡住。",
        "visual": """例：\n0 <- 1 <- 2\n\n边方向按“先修 -> 后修”：\n2 -> 1 -> 0\n\n初始入度：\nindegree[2]=0\nindegree[1]=1\nindegree[0]=1\n\nready=[2]\n拿走 2，删除 2->1：1 的入度变 0，于是入队。\n再拿走 1，0 的入度变 0。\n最终三门课都能被拿走。\n\n若是：\n0 -> 1\n^    |\n|____v\n\n每个节点入度都 >0，ready 一开始就是空；它们彼此等待，没有任何合法第一步。""",
        "core": "把先修关系画成“先修课 -> 后修课”；入度 0 表示当前已经没有未完成前置依赖，持续删除这类节点，能删完全部节点就说明无环。",
        "formula": """对 prerequisite [a,b]：\nedge: b -> a\nindegree[a] += 1\n\nready = {v | indegree[v] == 0}\n\n删除 course 时，对每条 course -> dependent：\nindegree[dependent] -= 1\n若恰好变成 0：dependent 入队\n\n最终：\ncompleted == numCourses <=> 图中不存在有向环。""",
        "steps": [
            "建立邻接表 next，并按 b->a 的方向统计每门课 indegree；边方向反了会把依赖语义完全颠倒",
            "把所有初始 indegree==0 的课程放进 ready，包括没有任何依赖关系的孤立课程",
            "每次从 ready 取一门课，completed 加一；这代表它的所有先修都已经被移除，可以安全完成",
            "遍历它指向的后修课，把这些课程的剩余入度减一；只有在入度刚降到 0 时才入队一次",
            "队列耗尽后检查 completed；若小于 numCourses，剩余子图没有入度 0 节点，因此存在环",
        ],
        "proof": "任何入度为 0 的节点都没有尚未满足的先修，选择它一定合法。DAG 的任意非空子图都至少存在一个入度为 0 的节点，因此如果图无环，Kahn 过程一定能持续删除直到全部节点处理完。反过来，如果过程提前停下且仍有节点，每个剩余节点都至少有一条来自剩余集合的入边，沿前驱不断回溯必然重复某节点，从而形成有向环。",
        "pitfalls": "最容易错的是边方向：题目给 [a,b]，必须建立 b->a。孤立课程也属于需要完成的节点，所以应计入 completed。只有当入度从 1 降到 0 时入队，避免重复入队。Kahn 会破坏 indegree 数组；若后续还需要原入度，应复制一份。",
        "transfer": "从 LC-200/133 的“遍历可到达节点”升级到“遍历必须遵守依赖顺序”。软件包安装、构建系统、任务 DAG、流水线调度都可用同一模型。后续若题目要求实际课程顺序，只需把每次出队的 course 记录下来，就是拓扑序。",
        "memory": "[a,b] 读成 b->a；零入度先做，做完就释放后继。",
    },
}
