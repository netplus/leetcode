"""High-touch pedagogy rewrites for Week 2 / Day 12."""

PEDAGOGY_WEEK2_DAY12 = {
    20: {
        "pattern": "栈保存未闭合结构：最近开启，最先关闭",
        "visual": """把扫描过程看成一叠还没关上的括号：

输入：([{}])

读 '('  -> [(]
读 '['  -> [(, []
读 '{'  -> [(, [, {]
读 '}'  -> [(, []      只能关掉最近的 '{'
读 ']'  -> [(]
读 ')'  -> []

栈顶永远代表“下一只右括号唯一允许关闭的左括号”。""",
        "core": "右括号不是去历史里随便找同类型左括号，而是必须关闭最近一个尚未闭合的左括号。",
        "formula": """扫描完任意前缀后：
stack = 这个前缀中尚未闭合的左括号，按开启顺序保存
stack.top() = 下一次最先允许被关闭的左括号

遇到右括号 close：
stack 非空 && stack.top() == match(close)
才合法，然后 pop。

扫描结束还要求 stack.empty()。""",
        "steps": [
            "遇到左括号就压栈，表示开启了一个尚未完成的嵌套结构",
            "遇到右括号先检查栈是否为空，再检查栈顶是否是与它匹配的左括号；不匹配立即失败",
            "匹配成功就弹出栈顶；全部扫描结束后，只有栈为空才说明所有开启结构都已闭合",
        ],
        "memory": "左括号负责“欠一笔闭合账”，右括号只能还最近的一笔。",
        "proof": """不会漏：每个左括号进入栈后一直保留到自己真正被关闭，因此任何未闭合结构都不会消失。
不会错配：合法嵌套要求后开启的结构先结束；若当前右括号连栈顶都不能匹配，就不可能绕过这个更近的未闭合结构去匹配更早的括号。
不会残留：最终栈非空说明仍有左括号没有右括号与之配对，所以必须判非法。""",
        "pitfalls": "不能只统计三种括号数量是否相等，`([)]` 数量完全相等但嵌套顺序错误；遇到右括号必须先判空再访问 top；扫描完后别忘记检查残余左括号。",
        "transfer": "这是 Day 10 迭代 DFS 中“栈保存暂停现场”的同一思想：栈保存尚未完成的上下文。后续表达式求值、路径回退、HTML/XML 标签匹配都在复用这个模型。",
    },

    155: {
        "pattern": "栈顶携带前缀摘要：把历史最小值一起压栈",
        "visual": """普通栈只记“这一层放了什么”；Min Stack 让每一层再带一张摘要卡：

push -2 : (-2, min=-2)
push  0 : ( 0, min=-2)
push -3 : (-3, min=-3)  <- top

pop 后：
          ( 0, min=-2)  <- top

无需重新扫描下面所有元素，上一层早已保存了当时的最小值。""",
        "core": "每次 push 都把“到这一层为止的最小值”一起保存，因此 pop 天然回滚，getMin 只读栈顶。",
        "formula": """设第 i 层实际值为 value[i]，摘要为 min[i]：

min[0] = value[0]
min[i] = min(value[i], min[i-1])

栈顶 pair = {当前值, 当前整个栈的最小值}

因此：
top()    = stack.top().value
getMin() = stack.top().minimum""",
        "steps": [
            "push(val) 时读取旧栈顶最小值，并计算 minimum = min(val, oldMinimum)；空栈时 minimum=val",
            "把 {val, minimum} 作为一个整体压栈；pop 时也整体弹出，不做任何重新计算",
            "top 读取 pair 的实际值字段，getMin 读取同一 pair 的最小值字段，二者都是 O(1)",
        ],
        "memory": "不要在查询时回头找答案；在状态进入栈时，就把未来查询需要的摘要一起存好。",
        "proof": """归纳看每一层：底层摘要显然就是底层值；若上一层摘要已经是此前所有元素的最小值，新层取 min(newValue, oldMin)，便得到包含新元素后的全局最小值。
pop 不需要“恢复”任何变量，因为弹掉当前层后，上一层保存的摘要本身就是删除该元素之前的正确状态。
重复最小值也不会丢：每一层都独立记录自己的前缀最小值。""",
        "pitfalls": "若只额外保存一个全局 minimum，弹出当前最小值后就不知道第二小是谁；辅助最小栈若只在严格变小时压入，则重复最小值需要额外计数，而 pair-per-layer 方案天然避开这个坑。",
        "transfer": "这和持久化/回滚思想很接近：每一层保存进入该状态后的摘要。把 minimum 换成 maximum、gcd、按位与等，就得到一系列“栈 + 可回滚聚合状态”结构。",
    },

    739: {
        "pattern": "单调递减栈：新元素批量结算仍在等待的旧元素",
        "visual": """把每个还没等到更暖天气的日期看成一张“待结算订单”：

temperatures = [73, 74, 75, 71, 69, 72, 76]

扫描到 72 前，栈中可能是：
下标       2    3    4
温度      75   71   69
           |    |    |
          等待  等待  等待

72 到来：
69 < 72 -> 69 的订单完成，弹出
71 < 72 -> 71 的订单完成，弹出
75 > 72 -> 75 还不能完成，停止

栈从底到顶温度保持单调不增。""",
        "core": "栈只保留还没遇到更暖日子的下标；当前温度一旦更高，就连续结算栈顶那些终于等到答案的日期。",
        "formula": """栈中下标从底到顶递增，对应温度单调不增：
T[s0] >= T[s1] >= ... >= T[stop]

若当前 day 满足：
T[day] > T[stack.top()]
则：
answer[stack.top()] = day - stack.top()
并弹栈继续检查。

相等不能弹，因为题目要求 strictly warmer。""",
        "steps": [
            "从左到右扫描 day；在处理当前日之前，栈中只包含尚未找到更暖日期的历史下标",
            "当 temperatures[day] 严格大于栈顶温度时，当前 day 就是该栈顶日期的答案：记录距离并弹出；重复直到栈空或栈顶不再更冷",
            "把当前 day 压栈，让它成为新的待结算日期；扫描结束仍留在栈中的日期答案保持初始化的 0",
        ],
        "memory": "当前值不是来“维护排序”的，它是在帮助历史元素第一次确定答案；单调性只是把仍有资格等待的元素压缩在栈里。",
        "proof": """为什么当前 day 是被弹日期的第一个更暖日？因为我们按时间从左到右扫描；若此前已有更暖日，该下标早就会在那个时刻被弹，不可能一直留到现在。
为什么可以连续弹？栈顶是目前最晚、也最冷的一批未决日期；只要当前温度能超过它们，就能一次性结算。
每个下标只入栈一次、出栈至多一次，所以 while 虽然嵌套在 for 中，总操作量仍是 O(n)。""",
        "pitfalls": "必须存下标而非只存温度，因为答案是天数差；比较条件必须是 `>` 而不是 `>=`；不要看到双层循环就误判 O(n^2)，摊还分析的关键是每个下标最多弹一次。",
        "transfer": "这是 Day 7 LC-239“淘汰失去资格候选”的近亲：LC-239 用 deque 维护一个移动窗口里的最优候选；LC-739 用 stack 维护等待未来触发条件的未决候选。下一题 LC-84 会把“未来触发时结算”升级成同时确定左右边界。",
    },

    84: {
        "pattern": "单调递增栈：更矮柱出现时确定矩形的最终左右边界",
        "visual": """对每根柱 h，不要问“它能组成哪些矩形”，而问：
“如果把 h 当成矩形最低高度，它最多能向左右延伸到哪里？”

heights = [2, 1, 5, 6, 2, 3]

扫描到右侧的 2：
          [5][6]
          [5][6]
          [5][6]
       1  [5][6] 2

6 遇到右边第一个更矮的 2 -> 6 的右界确定
弹掉 6 后，5 也遇到右边第一个更矮的 2 -> 5 的右界确定

弹栈后的新栈顶，就是被弹柱左边第一个更矮的位置。""",
        "core": "柱子只有在遇到右侧第一个更矮柱时才真正“封口”；此时弹栈后的新栈顶同时给出它左侧第一个更矮柱，于是最大宽度一次确定。",
        "formula": """当下标 i 的 currentHeight < heights[top] 时，弹出 mid：

rightLess = i
leftLess  = stack.empty() ? -1 : stack.top()
height    = heights[mid]
width     = rightLess - leftLess - 1
area      = height * width

此时 [leftLess+1, rightLess-1] 内所有柱高都 >= height，
所以这是以 heights[mid] 为最低高度时能取得的最大宽度。""",
        "steps": [
            "维护柱高单调不降的下标栈；扫描 i=0..n，并把 i==n 看成高度 0 的右哨兵，确保最后残留柱也会被结算",
            "若当前高度低于栈顶柱高，就反复弹出 mid；当前 i 是 mid 右侧第一个更矮位置，弹栈后的新栈顶是 mid 左侧第一个更矮位置",
            "用 width=i-leftBoundary-1 计算以该柱为最低高度的最大矩形面积；完成所有需要结算的柱后，再把当前下标压栈",
        ],
        "memory": "LC-739 是“遇到更大值，答案出现”；LC-84 是“遇到更小值，边界封口”。单调栈本质都是等未来事件到来时一次结算历史状态。",
        "proof": """被弹柱 mid 的右侧第一个更矮位置就是当前 i：此前若出现更矮柱，mid 已经在那时被弹。
弹出 mid 后的新栈顶是左侧最近更矮柱；两者之间留下来的柱子都不低于 heights[mid]，因此这一整段都能支撑高度 heights[mid]。
再向左或向右扩一格就会碰到更矮柱，故此时得到的宽度已经是“以 mid 为最低柱”的最大宽度。每根柱只压栈一次、弹栈一次，总复杂度 O(n)。""",
        "pitfalls": "最容易错的是宽度：两侧更矮柱本身不能包含，因此是 `i-leftBoundary-1`；末尾哨兵用于强制结算递增尾巴；实现使用严格 `>` 弹栈，相等高度可保留，仍能在后续得到正确最大宽度。",
        "transfer": "直接承接 LC-739 的“未决候选 + 未来事件结算”。换个视角，本题是在为每个高度找左右第一个更小元素；这也是子数组最小值贡献、笛卡尔树等问题的核心边界模型。",
    },
}
