"""High-touch pedagogy overrides for Week 4 / Day 24.

Day 24 moves from binary-search elimination to greedy elimination: prove that a
local choice or a summarized frontier permanently dominates discarded
candidates, then keep only the information the future can still distinguish.
"""

PEDAGOGY_WEEK4_DAY24 = {
    455: {
        "pattern": "排序 + 双指针贪心：让最小够用资源匹配最小需求",
        "model": "把孩子胃口和饼干尺寸都从小到大排好。当前最小未满足孩子只有两种情况：最小剩余饼干太小，则它对所有剩余孩子都没用；若它已经够大，把它给当前最小胃口孩子不会浪费更大的饼干。于是每一步都能永久删除一个不再需要考虑的对象。",
        "visual": """g = [1,2,3]
s = [1,1,3]
排序后从最小端看：

孩子:  1   2   3
       ^ child
饼干:  1   1   3
       ^ cookie

cookie=1 能满足 child=1：
把这块最小够用饼干交出去。

剩下：
孩子:      2   3
           ^
饼干:      1   3
           ^

cookie=1 连胃口 2 都满足不了；
后面的孩子胃口只会更大，所以这块饼干可以永久丢弃。

最终 cookie=3 满足胃口 2，答案为 2。""",
        "core": "始终处理最小未满足需求：资源太小就丢掉，资源够用就立刻匹配，因为更大的资源应留给更难满足的需求。",
        "formula": """排序后维护：
child  = 当前最小未满足孩子下标
cookie = 当前最小未处理饼干下标

若 s[cookie] < g[child]：
    这块饼干无法满足 child，当然也无法满足任何更大的 g
    => 只移动 cookie

若 s[cookie] >= g[child]：
    用它满足 child
    => child++, cookie++

不变量：已经处理过的饼干中，不存在还能让当前或之后孩子额外获益的资源；child 之前的孩子已经以不牺牲最优解的方式满足。""",
        "steps": [
            "分别升序排序 g 和 s，让‘最容易满足的孩子’与‘最小剩余饼干’都位于双指针前端",
            "若当前饼干小于当前胃口，直接丢弃这块饼干并只移动 cookie，因为它对所有剩余孩子都不可能有用",
            "若当前饼干足够，立即分给当前孩子，同时移动 child 和 cookie；更大的饼干留给后面更大的胃口",
            "任一数组扫描完就停止，child 恰好等于已满足孩子数量",
        ],
        "proof": "当最小饼干 s 小于最小剩余胃口 g 时，因为后续胃口都 >=g，s 不可能进入任何可行匹配，丢弃不会漏解。当 s>=g 时，考虑任意最优方案：若 g 没被满足而 s 被拿去满足更大胃口，可以把 s 改给 g，不减少满足人数；若 g 由更大的饼干 t 满足而 s 给了另一孩子或未使用，也可以交换，让 s 给 g、把 t 留给原用途，仍不变差。因此总存在一个最优解包含当前贪心匹配，可以安全固定它。重复该交换论证即可得到全局最优。",
        "pitfalls": "排序会修改输入数组；每块饼干和每个孩子都只能使用一次。不要在饼干太小时移动 child：那相当于无理由放弃一个仍可能被后续大饼干满足的孩子。空饼干数组自然返回 0。",
        "transfer": "这是典型的有序资源匹配：最小够用资源服务最小需求。与 Day 9/双指针题类似，排序的价值是让一次比较能证明一整类候选永久无用。之后遇到‘资源有能力值、任务有最低需求、最大化匹配数’，优先尝试排序 + 交换论证。",
        "memory": "最小需求先服务；资源太小丢资源，资源够用两边一起走。",
    },

    55: {
        "pattern": "贪心前沿：把所有可达路径压缩成最远可达位置",
        "model": "从下标 0 出发，所有具体跳法很多，但判断‘能不能到终点’并不需要记住路径。扫描过程中，只要当前位置 i 没越过 farthest，它就是某条路径可以到达的位置；从它再跳会把连续可达前缀扩展到 max(farthest, i+nums[i])。未来只关心这条前沿能伸多远。",
        "derivation": """最自然的正确做法是 DFS/BFS：从每个可达位置 i 枚举 1..nums[i] 的所有下一跳，直到到达末尾或搜索完所有状态。它不会漏掉任何路径，但同一个位置可能由很多不同跳法到达，而且从这些路径继续向后的能力只由‘当前下标’决定；枚举具体路径制造了大量重复状态。

进一步观察，这题甚至不必为每个可达下标保存 visited。因为每次从可达位置 i 可以跳到 i+1..i+nums[i]，所有已发现位置会形成一个连续前缀 [0,farthest]。对于“是否最终可达”这个问题，前缀内部到底经由哪条路径到达已经失去意义；未来只需要知道最右边界 farthest。

于是把整片搜索状态压缩成一个整数：扫描所有 i<=farthest 的位置，并持续扩大 farthest。若扫描下标第一次超过 farthest，说明出现了不可跨越的空隙；若 farthest 覆盖 n-1，则成功。优化类型：重复状态消除 + 候选状态压缩。""",
        "visual": """nums = [2,3,1,1,4]

起点 0：最多到 2
可达前缀：
[0 1 2] 3 4
     ^ farthest=2

扫描 i=1，它本身已在可达前缀内：
1 + nums[1] = 4

可达前缀扩成：
[0 1 2 3 4]
        ^ farthest=4

此时已经覆盖终点。

反例 nums=[3,2,1,0,4]：
前沿最终停在 3。
扫描到 i=4 时：
0 1 2 3 | 4
      farthest=3
          i=4 > farthest
出现缺口，任何具体跳法都跨不过去。""",
        "core": "不枚举怎么跳，只维护所有已知可达路径共同产生的最右前沿 farthest；能扫描到的点负责继续扩张前沿。",
        "formula": """初始 farthest = 0。

扫描位置 i 前：
i <= farthest  <=>  i 已被某条路径覆盖，可以作为下一跳起点。

若 i > farthest：
    当前连续可达前缀已经结束，返回 false。

否则：
farthest = max(farthest, i + nums[i])。

不变量：处理完下标 0..i 后，farthest 是只使用这些已处理起点能够到达的最远位置，并且 [0,farthest] 是连续可达的。

若 farthest >= n-1，返回 true。""",
        "steps": [
            "令 farthest=0，表示起点本身已可达",
            "从左到右扫描 i；先检查 i 是否超过 farthest，超过则说明没有任何已知路径能到这里，立即失败",
            "只有确认 i 可达后，才用 i+nums[i] 更新 farthest，把从 i 出发新增的覆盖范围并入全局前沿",
            "一旦 farthest>=n-1 就立即成功；否则扫描结束后也说明所有位置均未出现缺口",
        ],
        "proof": "归纳来看，初始 [0,0] 可达。若 i<=farthest，则 i 位于已有连续可达前缀中，所以存在一条路径到 i；从 i 可到达 i..i+nums[i]，与原前缀相交，因此合并后仍是连续前缀 [0,newFarthest]。反之若 i>farthest，所有已经能作为起点的位置都已扫描过，而它们的最远贡献仍到不了 i；后面的下标自身也尚不可达，无法凭空产生新的跳跃，因此这个缺口永远无法跨越。只保留 farthest 不会漏掉任何可达性信息。",
        "pitfalls": "必须先确认 i<=farthest 再使用 nums[i]，否则会让一个实际不可达的位置错误扩张前沿。长度为 1 时起点就是终点。现有实现用 long long 保存 farthest 与 i+nums[i]，在更大约束下也避免加法溢出。",
        "transfer": "LC-45 在同一前沿模型上增加‘最少跳几次’：不能只保留全局 farthest，还要记录当前跳数这一层的边界 currentEnd。图论视角上，LC-55 把 BFS 的所有已发现节点压成一个连续区间；这种‘状态集合可由边界完整表示’的压缩思想也会出现在区间覆盖问题。",
        "memory": "路径不重要，可达前缀才重要：i 进得了前沿，才有资格把前沿推得更远。",
    },

    45: {
        "pattern": "贪心分层：把 Jump Game 的可达前沿升级成隐式 BFS 层",
        "model": "直接复用 LC-55 的 farthest，但这次还要知道最少跳数。把‘恰好 jumps 次跳跃能覆盖的最远边界’记为 currentEnd；扫描这一层所有可达起点时，用 farthest 收集‘再跳一次最多能到哪里’。只有扫描到 currentEnd，才真正进入下一层并把 jumps 加一。",
        "derivation": """一个自然且正确的做法是 BFS：下标是节点，从 i 到 i+1..i+nums[i] 都有一条边；BFS 第一次到达 n-1 的层数就是最少跳数。但显式枚举每个节点的所有下一跳，最坏会产生 O(n^2) 条边，而且队列里存着大量连续下标。

LC-55 已经告诉我们，这些可达节点往往能压成一个连续前沿。对最短跳数再保留一层信息即可：currentEnd 表示当前 BFS 层的右边界，farthest 表示扫描完整个当前层以后下一层的最右边界。层内不需要立刻决定‘到底从哪个位置跳’，因为在增加跳数之前，我们应该先比较这一层所有起点能把下一层推到哪里。

当扫描指针 i 到达 currentEnd，说明当前层所有候选都已考察完，此时才 jumps++ 并令 currentEnd=farthest。于是显式 BFS 队列和 O(n^2) 邻边枚举被两个边界变量替代。优化类型：BFS frontier 的区间压缩。""",
        "visual": """nums = [2,3,1,1,4]

0 跳：
[0]
currentEnd = 0

扫描 i=0：farthest=max(0,0+2)=2
到达 currentEnd，进入第 1 层：
[1 2]
  currentEnd=2

扫描这一整层：
i=1 -> farthest=max(2,1+3)=4
i=2 -> farthest 仍为 4

到达 currentEnd=2，才进入第 2 层：
[3 4]
    currentEnd=4

因此最少 2 跳。

关键：在 i=1 时虽然已经看到能到 4，
也不能把每个 i 都算成一次新跳；
跳数按“层”增加，不按“扫描位置”增加。""",
        "core": "把一次跳跃当作 BFS 一层：扫描完当前层 [上一边界+1,currentEnd] 的所有起点后，才用 farthest 建立下一层并增加一次跳数。",
        "formula": """jumps      = 已经确定的 BFS 层数
currentEnd = 使用 jumps 次跳跃能覆盖的当前层最右边界
farthest   = 扫描当前层期间，使用 jumps+1 次最多能覆盖的位置

对 i=0..n-2：
    farthest = max(farthest, i + nums[i])
    if i == currentEnd:
        jumps += 1
        currentEnd = farthest

不变量：在触发 i==currentEnd 之前，当前层的每个可达起点都被且只被扫描一次；farthest 汇总了这一整层所有下一跳的最大覆盖。""",
        "steps": [
            "初始化 jumps=0、currentEnd=0、farthest=0，把下标 0 看作第 0 层",
            "扫描当前层的每个下标 i，用 i+nums[i] 更新 farthest，但此时不要立即增加 jumps",
            "只有当 i==currentEnd，才说明本层所有起点都已比较完；此时 jumps++，并令 currentEnd=farthest 进入下一层",
            "循环只到 n-2，因为一旦某层已经覆盖终点就不需要再从终点发起下一跳，避免多计一次",
        ],
        "proof": "普通 BFS 在无权图中按层首次到达目标一定使用最少边数。本题从一层内所有连续可达下标出发，下一层的可达并集也是连续区间，其最右端就是这些 i+nums[i] 的最大值 farthest。因此可以只保存层边界而不保存队列中的每个节点。每次 jumps++ 前已扫描当前层所有可能起点，所以没有漏掉能让下一层更远的选择；而只有层结束才加一，也不会把同一次跳跃范围内的多个扫描位置重复计数。",
        "pitfalls": "不要每看到更大的 farthest 就 jumps++；跳数只在 i==currentEnd 时增加。循环到 n-2 而不是 n-1，否则长度大于 1 时到终点还可能多加一层。题目保证可达，所以现有实现无需额外处理 currentEnd 无法推进的死区；若迁移到无此保证的变体，应检测 farthest==currentEnd。",
        "transfer": "这是 LC-55 的直接升级：LC-55 只问最终前沿能否覆盖终点；LC-45 还要保留‘当前层边界’才能计最少步数。它本质上是隐式 BFS，可迁移到最少区间覆盖、最少加油次数等‘每一步能把连续前沿推进多远’的问题。",
        "memory": "LC-55 的 farthest + 一个 currentEnd：层内只收集，层尾才跳一次。",
    },

    134: {
        "pattern": "贪心候选淘汰：一次失败排除整段起点",
        "model": "先把每站压成净收益 gain[i]=gas[i]-cost[i]。假设当前候选起点 start 出发，currentTank 是从 start 累积到当前位置的剩余油量。若走到 i 后 currentTank<0，不只是 start 失败：start..i 中任何站作为起点都无法跨过 i，因此整个候选区间一次淘汰，下一候选直接跳到 i+1。",
        "derivation": """最直接的正确算法是把每个站都当起点模拟一整圈：从 s 出发逐站累加 gas[i]-cost[i]，一旦油量为负就判 s 失败，否则走满 n 站即成功。它显然不会漏答案，但最多 n 个起点各走 O(n) 站，最坏 O(n^2)。

浪费发生在失败模拟之间的大量重叠。设从 start 出发，直到 i 之前油量始终非负，而到 i 后第一次变负：
sum(start..i) < 0。
对于中间任意 j∈(start,i]，从 start 到 j-1 的累计因为此前未失败而 >=0，所以：
sum(j..i) = sum(start..i) - sum(start..j-1) <= sum(start..i) < 0。
也就是说 j 自己出发到 i 时会更早或同样没油，绝不可能成为答案。

因此一次失败可以把 start,start+1,...,i 全部永久排除，下一候选直接设为 i+1，不需要重新模拟这些起点。最后再用 total=sum(gain) 判断全局是否有足够总油量。优化类型：失败前缀的候选支配 / 批量淘汰。""",
        "visual": """gas  = [1,2,3,4,5]
cost = [3,4,5,1,2]
gain = [-2,-2,-2,+3,+3]

start=0：
到 0：tank=-2 < 0
=> 起点 0 失败，start=1

start=1：
到 1：tank=-2 < 0
=> 起点 1 失败，start=2

start=2：
到 2：tank=-2 < 0
=> 起点 2 失败，start=3

从 3 开始：
+3,+3,-2,-2,-2
前缀剩余：3,6,4,2,0
可以走完一圈。

更一般地，只要 start..i 第一次累计成负数：
start ........ i | i+1
  整段起点都失败  ^ 下一个候选""",
        "core": "局部油箱一旦变负，就证明当前失败段里的所有起点都不可能成功；整段跳过，同时用全局净收益 total 判断整圈是否存在解。",
        "formula": """gain[i] = gas[i] - cost[i]

total       += gain[i]
currentTank += gain[i]

若 currentTank < 0：
    start = i + 1
    currentTank = 0

为什么可整段淘汰：
在首次失败 i 前，对任意 j∈(start,i]：
sum(start..j-1) >= 0
而 sum(start..i) < 0
所以
sum(j..i)
= sum(start..i) - sum(start..j-1)
< 0。
故 j 也过不了 i。

全局必要且充分条件：
total = sum(gas)-sum(cost) >= 0。
扫描结束后若 total>=0，返回最后保留下来的 start；否则返回 -1。""",
        "steps": [
            "顺序扫描每个站，计算 gain=gas[i]-cost[i]，同时累加全局 total 与当前候选的 currentTank",
            "只要 currentTank<0，就证明从当前 start 到 i 的所有起点都失败，把 start 直接跳到 i+1，并把局部油箱重置为 0",
            "继续从新候选累积；此前淘汰的起点永久不再回看，因此每个下标只处理一次",
            "扫描结束后检查 total：若 total<0，整圈总供给不足必然无解；否则返回最后的 start",
        ],
        "proof": "每次局部失败时，上述前缀和关系证明 start..i 的所有起点都无法跨过 i，所以批量淘汰不会漏掉真正答案。扫描结束后，若 total<0，绕一圈最终消耗大于获得，无论从哪里出发都不可能成功。若 total>=0，所有被淘汰起点都已证明失败，而最后 start 到数组末尾的局部累计不会再变负；它从末尾绕回 0..start-1 时，所需的累计亏损由 total>=0 与此前失败段的总关系补足，因此该 start 可以完成整圈。",
        "pitfalls": "currentTank 只用于判断当前候选何时失败，不能代替 total；局部重置后 total 绝不能清零。净收益方向必须是 gas-cost。start 可能在某次失败后暂时变成 n，但若 total>=0，标准题设下最终可行候选会落在有效范围；现有约束和 int 范围也足以容纳总和，不过迁移到更大数值时可改 long long。",
        "transfer": "与 LC-53 最大子数组有共同结构：一个负贡献前缀面对相同未来只会拖累后续候选，因此可以丢弃；不同点是 LC-134 还必须满足环形全局守恒 total>=0。以后看到‘尝试每个起点会重复扫描，而某次失败能证明一整段起点都失败’，优先寻找这种批量淘汰证明。",
        "memory": "局部失败淘汰一整段，全局 total 决定有没有解。",
    },
}
