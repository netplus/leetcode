"""High-touch pedagogy overrides for Week 2 / Day 11."""

PEDAGOGY_WEEK2_DAY11 = {
    226: {
        "pattern": "树结构变换：先让左右子树完成职责，再交换当前节点的两个结果",
        "visual": """不要先把它理解成“每个节点 swap 一下”，先看递归职责：

原树：                 镜像后：
        4                       4
       / \\                     / \\
      2   7                   7   2
     / \\ / \\                 / \\ / \\
    1  3 6  9               9  6 3  1

站在节点 4，只需要提出两个子问题：
- 左边这棵 2 子树，请你自己完整镜像；
- 右边这棵 7 子树，请你自己完整镜像。

等两边都交回“已经镜像好的子树”后，4 只做最后一步：
原右子树放到 left，原左子树放到 right。

所以当前节点并不负责整棵树的所有细节，它只负责组合两个已经正确的子问题结果。""",
        "core": "递归函数返回“已经翻转完成的这棵子树”；当前节点只需交换两个已完成的子树，再把自己作为新子树根返回。",
        "formula": """定义 F(node)：返回以 node 为根、已经完全镜像后的子树根。

空树：
F(nullptr) = nullptr

非空节点：
left  = F(node->left)
right = F(node->right)
node->left  = right
node->right = left
return node

递归不变量：执行交换之前，left/right 已分别是原左右子树的完整镜像。""",
        "model": "复用 Day 10 的递归职责模型：子树先返回一个完整结果，父节点只负责组合，不跨层操心子树内部。",
        "steps": [
            "空节点没有结构可翻转，直接返回 nullptr",
            "递归翻转原左子树并保存返回根 left",
            "递归翻转原右子树并保存返回根 right",
            "把 right 接到 root->left、left 接到 root->right，然后返回 root",
        ],
        "memory": "子树先各自照镜子，父节点最后把左右位置对调。",
        "proof": "对任意节点，假设它的左右子树递归返回时都已经是各自原结构的镜像。当前节点交换这两棵镜像子树的位置后，左右方向也被反转，于是以当前节点为根的整棵子树就是原子树的镜像。叶子和空树显然成立，因此按树高归纳整棵树成立。每个节点只被唯一一次递归访问，不会漏也不会重复。",
        "pitfalls": "如果直接写 root->left = invertTree(root->right) 后又基于已经改过的 root->left 去处理另一边，可能覆盖原指针；本实现先把两个递归结果保存到局部变量最清晰。题目要原地修改结构，无需新建节点。",
        "transfer": "这与 Day 10 LC-105“左右子树先递归构造，再挂回根”是同一种组合式递归。后续遇到树结构变换，优先定义：递归返回的是一棵什么样的“已经处理好的子树”？",
    },

    236: {
        "pattern": "后序证据汇聚：子树向上报告 p/q，第一次左右同时有证据的位置就是 LCA",
        "visual": """把递归返回值看成“这棵子树交上来的证据”，而不是普通节点查找。

例如：
          3
         / \\
        5   1
       / \\ / \\
      6  2 0  8
        / \\
       7   4

若 p=5，q=4：
- 走到 5 时，当前节点自己就是 p，于是直接把 5 作为证据向上交；
- 虽然 4 在 5 的子树里，但题目允许“节点是自己的祖先”，因此 5 就已经是答案。

若 p=6，q=4：
在节点 5：
left  子树交回 6
right 子树交回 4
两边同时非空 -> 两条目标路径第一次在 5 汇合 -> LCA=5。

图像上就是：从 p、q 各往根画一条线，最低的交汇点就是答案。""",
        "core": "递归返回“当前子树里找到的目标证据”；若左右子树各交回一份证据，当前节点就是两条路径最低的汇合点。",
        "formula": """定义 L(node) 的返回语义：
- nullptr：这棵子树没有证据；
- p 或 q：当前节点本身就是目标，直接上报；
- 其他非空节点：代表这棵子树已经确定出的 LCA / 唯一目标证据。

left  = L(node->left)
right = L(node->right)

left != nullptr && right != nullptr  -> return node
只有一边非空                         -> return 非空那边
两边都空                             -> return nullptr

题目保证 p、q 都存在，所以根最终一定能得到答案。""",
        "model": "把 Day 10 的“子树向父返回状态”升级为“子树向父返回证据”；父节点只根据左右证据是否同时出现做决策。",
        "steps": [
            "遇到空节点返回 nullptr；若当前节点就是 p 或 q，直接返回当前节点作为证据",
            "后序递归获取左子树证据 left 和右子树证据 right",
            "若 left、right 都非空，说明 p/q 分居两侧，当前 root 就是最低汇合点，返回 root",
            "否则把唯一非空证据继续向父节点上报；两边都空则自然返回 nullptr",
        ],
        "memory": "左右都报到 -> 我就是汇合点；只报一边 -> 原样向上传。",
        "proof": "若 p、q 分居当前节点两侧，任何更低节点都不可能同时包含两者，因此当前节点就是最低公共祖先。若两者都在同一侧，另一侧返回空，答案已由包含两者的那棵子树递归确定并向上传递。若当前节点本身是 p 或 q，因为节点可以是自己的祖先，直接上报不会错。每个节点只访问一次，所以无遗漏、无重复搜索。",
        "pitfalls": "这里比较的是节点身份 root==p/root==q，而不是只比较 val；虽然本题值唯一，但 LeetCode 方法签名语义仍是节点。最容易误解的是“命中 p/q 就提前返回”：这并不会漏掉其后代中的另一个目标，因为如果另一个目标在其子树中，当前目标本身就应当是 LCA。",
        "transfer": "这是一类非常通用的后序汇聚：子树返回“证据”，父节点在证据首次同时出现时完成答案。Day 14 LC-124 也会用后序返回一条可向上的信息，同时在当前节点闭合一个全局候选答案。",
    },

    543: {
        "pattern": "树形 DP 双角色：向父只能交一条臂，当前节点却可以把左右两臂拼成完整路径",
        "visual": """直接复用 Day 10 / LC-104 的“子树向父返回高度”，但这次多问一个问题：
当前节点能不能把左右两条向下路径接起来？

        1
       / \\
      2   3
     / \\
    4   5

站在节点 2：
左臂：2 -> 4，下面贡献 leftHeight=1
右臂：2 -> 5，下面贡献 rightHeight=1

经过 2 的完整路径可以是：
4 -> 2 -> 5
长度（边数）= 1 + 1 = 2

但当节点 2 把信息交给父节点 1 时，不能同时把 4 和 5 两条路都带上去；
否则路径会在 2 处分叉，不再是一条简单路径。
所以向父只能返回更长的一条：
height(2) = 1 + max(1,1) = 2。""",
        "core": "当前节点可以用 leftHeight+rightHeight 闭合一条完整直径候选；但向父节点只能返回一条最长单臂高度。",
        "formula": """令 H(node) = 从 node 向下走到最深节点的最大“节点高度”，空节点 H=0。

left  = H(node->left)
right = H(node->right)

经过当前 node 的直径候选（边数）：
candidate = left + right
answer = max(answer, candidate)

向父返回：
H(node) = 1 + max(left, right)

两个角色必须分开：
全局候选可以左右都要；
返回父节点只能选择一边。""",
        "model": "把 LC-104 的高度递归作为可复用子程序，再额外在每个节点用左右高度闭合一次“任意点到任意点”的候选路径。",
        "steps": [
            "空节点高度为 0，作为递归基线",
            "后序计算 leftHeight 和 rightHeight，确保两棵子树的信息已经可用",
            "用 leftHeight + rightHeight 更新全局 diameter，表示以当前节点为最高点的最佳路径",
            "向父节点返回 1 + max(leftHeight, rightHeight)，因为父路径经过当前节点后只能继续进入一侧",
        ],
        "memory": "当前节点能“两臂抱满”，向父汇报时却只能“举一只手”。",
        "proof": "任意两节点之间的简单路径都有唯一的最高汇合节点。设这条最优路径的最高点为 x，那么它在 x 的左、右方向（某一侧也可能为空）所能取得的最长下行路径，正是在计算 x 时得到的 leftHeight/rightHeight，因此 candidate 会覆盖这条最优路径。遍历所有节点取最大值不会漏。每个节点高度只计算一次，所以不会重复。",
        "pitfalls": "直径按边数计，而这里空节点高度定义为 0，所以 leftHeight+rightHeight 已经正好是边数，不要再 +1。向父返回值绝不能是 left+right+1，那会允许一条路径在子节点处分叉。",
        "transfer": "这题是 Day 10 LC-104 的直接升级：LC-104 只关心“向上还能延伸多长”，LC-543 增加“在当前节点能闭合多长”。Day 14 LC-124 最大路径和会几乎原样复用这个双角色框架，只把高度换成路径和，并处理负贡献。",
    },

    98: {
        "pattern": "BST 全局约束下传：每深入一层，就把合法值域收紧成更小的开区间",
        "visual": """最容易犯的错误是只检查“左孩子 < 根 < 右孩子”。
看这棵树：

        5
       / \\
      1   7
         / \\
        4   8

局部看：
4 < 7，看起来像合法左孩子。
但 4 位于根 5 的右子树，必须满足“整个右子树都 > 5”，所以它其实非法。

因此节点 4 不是只带着父亲 7 的要求走下来，而是带着祖先一路传下来的区间：
root 5:     (-∞, +∞)
right 7:   (5, +∞)
left 4:    (5, 7)

4 不满足 5 < 4 < 7，于是立刻判 false。

可以把每个节点想成进入一扇越来越窄的门：祖先给出的上下界必须全部满足。""",
        "core": "不要只和父节点比较；递归携带当前子树允许的开区间 (lower, upper)，每个节点必须同时满足所有祖先留下的边界。",
        "formula": """validate(node, lower, upper) 的不变量：
当前 node 以及它整棵子树都处在祖先已经确定的合法上下文中，node 必须满足：

lower < node->val < upper

进入左子树：
validate(node->left, lower, node->val)
因为左边所有值都必须 < node->val

进入右子树：
validate(node->right, node->val, upper)
因为右边所有值都必须 > node->val

根节点初始范围：
(LLONG_MIN, LLONG_MAX)""",
        "model": "把树上的全局规则转成“上下文参数沿递归边下传”，每层只负责进一步收紧约束。",
        "steps": [
            "根节点从一个覆盖所有 int 值的开区间 (LLONG_MIN, LLONG_MAX) 开始",
            "空节点天然合法；非空节点先检查 lower < val < upper，不满足立即返回 false",
            "递归验证左子树，并把当前 val 作为新的严格上界",
            "递归验证右子树，并把当前 val 作为新的严格下界；左右都成立才返回 true",
        ],
        "memory": "不是问“我比爸爸大还是小”，而是问“我是否还在所有祖先共同留下的合法区间里”。",
        "proof": "若递归区间始终正确，那么左子树继承所有旧祖先限制并新增 val 这个上界，右子树同理新增下界，因此任意节点通过检查当且仅当它满足从根到自己的全部 BST 祖先约束。反过来，一棵合法 BST 的每个节点显然都位于这样逐层收紧得到的区间中，所以不会误杀。每个节点只检查一次，不会漏重。",
        "pitfalls": "BST 使用严格不等号，重复值非法。不能用 INT_MIN/INT_MAX 直接充当开区间哨兵，因为节点值本身允许等于 int 极值；用 long long 边界才能把所有合法 int 值包含进去。只比较直接左右孩子会漏掉跨层违规。",
        "transfer": "Day 10 LC-94 给出另一条等价视角：合法 BST 的中序序列必须严格递增。本题选择上下界法，是为了训练“把祖先上下文作为递归参数下传”；这种模型会迁移到范围约束、表达式作用域和路径条件验证。",
    },
}
